{
  "name": "hhh-backup-api",
  "version": "1.0.0",
  "private": true,
  "description": "Internal-network-only HTTP API wrapping backup.sh/restore.sh for the admin panel. Never exposed outside the Docker network.",
  "type": "module",
  "main": "server.js",
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.1",
    "multer": "^2.0.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
