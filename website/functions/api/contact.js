// DEPRECATED — unused. This was a Cloudflare Pages Function, written back
// when the site was briefly planned for Cloudflare Pages deploy. The site
// now self-hosts on Node/Docker (see website/Dockerfile + docker-compose.yml
// "website" service) with Astro's own server endpoint instead:
// src/pages/api/contact.ts. This whole functions/ directory has no effect
// on the Node/Docker deploy and can be deleted; it is kept only because this
// environment's filesystem bridge cannot delete files.
export {};
