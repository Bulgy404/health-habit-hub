# Review Scratch Notes — 2026-04-10

## Infrastructure

| Severity | File | Line/Section | Finding | Fix |
|----------|------|-------------|---------|-----|
| P1 | docker-compose.yml | app.environment | Hardcoded `MONGO_USER=admin` and `MONGO_PASSWORD=admin` override env_file values; literal "admin" credentials would be used even when `.env` sets stronger values | Fixed — removed both lines; `MONGO_DB` and `MONGO_HOST` kept as non-secret service-topology defaults (their env_file copies take the same precedence; keeping them in compose is harmless) |
| P1 | docker-compose.yml | proxy.command | `--api.insecure=true` exposes the Traefik dashboard on port 8080 with no authentication | Document: acceptable in local dev (port bound only to host); blocked behind TLS + auth middleware in prod compose; no `--api.insecure` flag appears in prod |
| P1 | docker-compose.yml | fuseki.environment | `ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}` — fallback default is the literal string `admin`; if `.env` is missing or the var unset, Fuseki starts with a well-known password | Document: operator must set `ADMIN_PASSWORD` in `.env` before first run; the same pattern appears in `docker-compose.local.yml` |
| P1 | docker-compose.yml | mongo.environment | `MONGO_INITDB_ROOT_USERNAME/PASSWORD` use `:-admin` fallback defaults; Mongo will initialise with `admin/admin` if vars are absent | Document: same issue as Fuseki fallback; mitigated in prod compose where fallbacks are removed entirely (`${MONGO_USER}` with no default) |
| P1 | docker-compose.yml | neo4j.environment | `NEO4J_AUTH` fallback password is `neo4j_password_change_me` — weak but clearly labelled; still reaches host on ports 7474 and 7687 | Document: operator must override before first run |
| P1 | docker-compose.yml | keycloak.environment | `KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD:-admin}` — same weak-default pattern; Keycloak admin console is exposed over HTTP on port 8080 | Document: acceptable in local dev; prod compose removes fallback |
| P2 | docker-compose.yml | mongo (line 101) | Port `27017:27017` is bound to all host interfaces (`0.0.0.0`), making MongoDB reachable from the network on developer machines | Document: bind to `127.0.0.1:27017:27017` for local dev; prod compose has no exposed Mongo port at all (internal network only) |
| P2 | docker-compose.yml | mongo-express (line 127) | Port `8081:8081` bound to all interfaces; mongo-express is protected only by basic auth with a hardcoded password default `AiGhe7ahngoh5eiz` committed in the file | Document: bind to `127.0.0.1`; prod compose has no exposed port (routed through Traefik only) |
| P2 | docker-compose.yml | keycloak (line 208) | Port `8080:8080` bound to all interfaces, exposing Keycloak admin console to the local network | Document: bind to `127.0.0.1` for local dev |
| P2 | docker-compose.yml | neo4j (lines 172–173) | Ports `7474` and `7687` bound to all interfaces, exposing Neo4j Browser and Bolt to the network | Document: bind to `127.0.0.1` for local dev |
| P2 | docker-compose.yml | backup.volumes | `/var/run/docker.sock:/var/run/docker.sock` mounted **without** `:ro` flag — backup service has full read-write access to the Docker socket, which is equivalent to root on the host | Document: mount as `:ro` if only container inspection is needed; prod compose already uses `:ro` |
| P2 | docker-compose.yml | translate | `libretranslate/libretranslate:latest` — unpinned image tag; prod compose pins to a specific version | Document: pin to a digest or explicit version tag for reproducible builds |
| P2 | docker-compose.yml | admin.environment | `NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-change-me-in-production}` — default value is a known string; sessions could be forged if operator forgets to set it | Document: operator must set before deploying; same pattern in `docker-compose.local.yml` |
| P2 | docker-compose.yml | admin.environment | `KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_ADMIN_UI_CLIENT_SECRET:-hhh-admin-secret-change-me}` — well-known default committed in file | Document: operator must set; same pattern in `docker-compose.local.yml` |
| P2 | stack.env | line 16 | `TRAEFIK_DASHBOARD_AUTH` contains a hashed password committed to git (`admin:$$apr1$$…`); the password itself is obscured but the hash is crackable offline | Document: rotate the password and set it only via Portainer env-var override, never commit to git; file comment says to override in Portainer but the default is still committed |
| P2 | stack.env | lines 7–8 | `DOMAIN=habit.wiwi.tu-dresden.de` and `SERVER_IP=141.76.16.16` are real infrastructure values committed to the repo | Document: low risk for a research project, but consider moving to Portainer-only vars |
| P2 | stack.env | line 12 | `ACME_EMAIL=felix.reinsch@tu-dresden.de` (real email) committed to git | Document: low risk; move to Portainer env-var if the repo becomes public |
| P2 | docker-compose.prod.yml | neo4j (lines 167–169) | Neo4j ports 7474 and 7687 are exposed to the host on the production compose file; Neo4j Browser should not be internet-reachable | Document: remove host port bindings in prod; use Traefik routing only |
| P2 | docker-compose.local.yml | redis (line 264) | Redis port `6379:6379` bound to all interfaces with no authentication (`redis-server --appendonly yes` only); any process on the local network can read/write the cache | Document: bind to `127.0.0.1` or add `--requirepass` for local dev |
| P2 | docker-compose.local.yml | app.environment | `KEYCLOAK_ADMIN_CLIENT_SECRET=${KEYCLOAK_ADMIN_CLIENT_SECRET:-CHANGE_THIS_KEYCLOAK_BACKEND_SECRET}` — default value is a known placeholder string | Document: operator must set before first run |

## Backend Middleware & Utils

| Severity | File | Line | Finding | Fix |
|----------|------|------|---------|-----|
| P0 | app/routes/internalRouter.js | ~16 | Timing-unsafe string equality (`!==`) on shared secret — vulnerable to timing side-channel attack | Fixed — timingSafeEqual from node:crypto |
| P1 | app/app.js | — | No security response headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy etc.) set on any response | Fixed — securityHeaders middleware registered first in app.js |
| P2 | app/middleware/auth.js | 91,123 | Dead code: `const ready = Promise.resolve()` created and attached as `authMiddleware.ready` but never read anywhere | Fixed — removed both lines |
| P2 | app/middleware/requestParser.js | 1 | Stale comment "doesnt work need to fix" left in production code; `bodyParser.json()` has no body-size limit set (default is 100 kb, which may be acceptable but is undocumented intent) | Document: remove comment; explicitly set `{ limit: '100kb' }` or document that the default is intentional |
| P2 | app/app.js | 73–74 | `bodyParser.urlencoded` and `bodyParser.json()` registered on the sub-router without explicit size limits; the API path registers `express.json()` without a limit too | Document: add `{ limit: '1mb' }` or appropriate size cap on all body parsers |
| P2 | app/app.js | 174 | `/api/internal` router is registered AFTER `httpServer.listen()` — technically valid in Express since routing is evaluated at request time, but unconventional and fragile; a developer could assume the handler is missing | Document: move `app.use('/api/internal', ...)` before `httpServer.listen()` |
| P2 | app/utils/config.js | 2 | `process.env.PATH` is overwritten by the `path` key — shadows the OS `PATH` environment variable name; this is a naming collision but harmless in practice since `config.path` is only used for the app base path | Document: rename to `appBasePath` or `basePath` to avoid confusion |
| P2 | app/utils/config.js | 44 | `getDbHeader()` references `this.db.name` which does not exist in the config object (should be `this.db.path`); this method would return `undefined` for the path header if called | Document: bug — `this.db.name` should be `this.db.path`; method appears unused but could cause silent errors if called |
| P2 | app/utils/healthCheck.js | 35–36 | Mongo connection URI is assembled from env vars inside `checkMongo()` rather than using the shared `config` object — a second source of truth for Mongo connection settings | Document: use `config.js` values for consistency |
| P2 | app/utils/localization.js | 19 | `fs.readdirSync` and `fs.readFileSync` called synchronously at startup — blocks the event loop during boot; acceptable at startup but worth noting | Document: acceptable for startup initialisation; no action required |
| P2 | app/middleware/staticFileMiddleware.js | 6 | `loadLanguageFiles()` is called at module load time as a side-effect, which makes the module non-idempotent and hard to test; language codes are then used to construct route regex | Document: consider accepting language codes as a parameter rather than relying on module-level side effect |

## Backend Routes — Admin

| Severity | File | Line | Finding | Fix |
|----------|------|------|---------|-----|
| P0 | app/db/adminQueries.js | 23 | Cypher label injection via `` SET d:`${newLabel}` `` string interpolation; callers must validate | Confirmed safe — whitelist exists in `adminParticipantService.js` lines 5–10 (VALID_GROUPS Set) and GROUP_LABEL_MAP restricts input to G1-G4 before mapping to label; `assignGroupLabel` is never called from `adminRouter.js` directly |
| P1 | app/routes/adminRouter.js | 1945–1946 | `new OId(studyId)` and `new OId(groupId)` in `POST /notifications/schedule` without try/catch — malformed IDs throw synchronous error producing 500 instead of 400 | Fixed — each ObjectId construction wrapped in individual try/catch returning 400 |
| P2 | app/routes/adminRouter.js | 220 | `POST /participants` returns HTTP 200 on success; should be 201 Created per REST semantics | Fixed — changed to `res.status(201).json(result)` |
| P1 | app/services/adminParticipantService.js | 62 | Plaintext participant password stored in MongoDB (`participants` collection) AND returned in plaintext via `POST /participants` JSON response body — data-at-rest AND in-flight exposure | Backlog — recommended fix: generate token card PDF at creation time, return only `tokenCardUrl`, store only a bcrypt hash; do not return or persist raw password |
| P2 | app/routes/adminRouter.js | 537–550 | `PUT /settings/:key` — the `key` URL parameter is used as-is as the MongoDB document key with no whitelist or format validation; an admin could insert arbitrary keys polluting the settings collection | Backlog — add a whitelist of valid setting keys (e.g. `['token_card_format']`) before updating |
| P2 | app/routes/adminRouter.js | 1014–1177 | Survey CRUD (GET/POST/PUT/PATCH for `/surveys`) and questionnaire CRUD contain inline DB queries and business logic rather than delegating to a service layer | Backlog — extract to `adminSurveyService.js` and `adminQuestionnaireService.js` for testability |
| P2 | app/routes/adminRouter.js | — | ~2050-line file; mix of Swagger docs, route handlers, and inline business logic makes the file hard to navigate | Backlog — split into domain sub-routers (surveys, notifications, studies, participants) |
| P2 | app/routes/adminRouter.js | 1734 | `POST /studies/:id/codes` performs no validation that `count` is a positive integer; a negative or non-numeric value is forwarded to `createCodes` | Backlog — validate `count` is a positive integer before passing to service |
| P2 | app/routes/adminRouter.js | 207,327,469,546,652,825 | Multiple `POST`/mutation endpoints return implicit 200; only `POST /participants` (line 220) and `POST /notifications/schedule` (line 1974) were corrected in this session | Backlog — audit all POST endpoints and change resource-creation responses to 201 |
| P2 | app/routes/adminRouter.js | 1941 | `const { ObjectId: OId } = await import('mongodb')` is a dynamic import inside a route handler; `ObjectId` is already statically imported at line 3 — redundant and inconsistent | Backlog — replace with the top-level `ObjectId` import |

## Backend Routes — Habits, Profile, Onboard, Questionnaires

| Severity | File | Line | Finding | Fix |
|----------|------|------|---------|-----|
| P1 | app/controllers/surveyController.js | 43 | Full `submission` object (including user ID and all survey answers) logged to stdout — PII exposure in logs | Fixed — replaced with redacted log emitting only `surveyId` and `userId` |
| P1 | app/routes/surveyRouter.js | 19–22 | Legacy `userId` cookie set without `secure` or `sameSite` flags — cookie transmitted over HTTP in non-production environments and susceptible to CSRF | Fixed — added `secure: process.env.NODE_ENV === 'production'` and `sameSite: 'lax'` |
| P1 | app/controllers/surveyController.js | 44–48 | `demographicsCompleted` cookie set without `secure` or `sameSite` flags — same issue as userId cookie above | Fixed — added `secure` and `sameSite: 'lax'` flags |
| P2 | app/controllers/surveyController.js | 22–26 | `renderSurvey` returns the raw MongoDB document including `_id` in the JSON response — internal field leaked to client | Fixed — destructure `_id` out before returning `surveyData` |
| P2 | app/routes/questionnaireResponsesRouter.js | 177 | `GET /questionnaire-responses/me/:slug` returns the raw MongoDB document including `_id`; the sibling `/me` route at line 126 already strips `_id` correctly | Fixed — added `const { _id, ...responseData } = responses[0]` destructuring |
| P2 | app/utils/SparqlDatabase.js | 173 | `context.value` (= `label.name` from user input) is interpolated directly into a SPARQL IRI fragment `hhh:${context.value}` without an explicit allowlist check; only labels that resolved to a known type via `LABEL_TYPE_MAP` reach this code path, so in practice only safe alphanumeric values (`TimeReference`, `PhysicalSetting`, etc.) appear here | Document — defended in practice by `LABEL_TYPE_MAP` type resolution + `label.type === 'context'/'behavior'` filter; recommend adding an explicit allowlist assertion in `_appendContext` / `_appendBehavior` for defence in depth |
| P2 | app/routes/habitsRouter.js | 336–370 | `POST /habits/:id/annotate` — `req.params.id` (the `habitId`) is stored to MongoDB without length or format validation; an attacker could store an arbitrarily long string as a habitId key in the `habit_annotations` collection | Backlog — validate habitId is a known UUID or bounded-length alphanumeric string before inserting |
| P2 | app/routes/onboardRouter.js | 73–78 | `POST /onboard` response includes plaintext `username` and `password` in the JSON body; these credentials are generated server-side (random UUID + 32-byte hex) and needed by the client to authenticate, but they are transmitted in plaintext over the API response | Document — acceptable for this anonymous-participant onboarding flow; ensure TLS is enforced in production (Traefik config already mandates HTTPS in prod) |
| P2 | app/routes/questionnairesRouter.js | 95–115 | `GET /questionnaires/:slug` — `slug` is passed directly to MongoDB `findOne({ slug, active: true })`; MongoDB query operators in the slug (e.g. `$where`) would be neutralised by the equality match, but there is no length or format validation on the slug parameter | Backlog — add a regex/length guard on `slug` before querying |
| — | app/routes/habitsRouter.js | all | No IDOR: all routes either operate on public/anonymous data or bind to `req.user.sub`; no per-user resource endpoints with a user-controlled userId param | No action required |
| — | app/routes/profileRouter.js | all | No IDOR: both GET and POST `/profile` bind to `req.user.sub` from the JWT; no userId param accepted from caller | No action required |
| — | app/routes/onboardRouter.js | all | No IDOR: no user-owned resources; endpoint creates a new anonymous user and returns credentials | No action required |
| — | app/routes/questionnaireResponsesRouter.js | all | No IDOR: `/me` and `/me/:slug` use `req.user.sub`; no cross-user access path | No action required |
| P2 | app/routes/questionnaireResponsesRouter.js | POST / | `POST /questionnaire-responses` returns `{ id: result.insertedId }` — exposes MongoDB internal ObjectId as `id`; inconsistent with the _id-stripping applied to GET routes in this task | Backlog — return a stable application-level identifier or omit the field |

## Backend Routes — Recommend, KB, StudyEnroll, Participant

| Severity | File | Line | Finding | Fix |
|----------|------|------|---------|-----|
| — | app/routes/recommendRouter.js | 69, 126 | IDOR guard on `GET /:userId/history` and `GET /:userId` — guard confirmed present at both lines: `!isPrivileged(req.user) && req.user?.sub !== req.params.userId` blocks participants from accessing other users' recommendations | No action required — guard in place |
| P1 | app/routes/recommendationsRouter.js | 56–62 | IDOR on `POST /:recommendation_id/feedback` — handler fetched `rec.userId` in the projection but never compared it to `req.user.sub`; any authenticated user could submit feedback on another user's recommendation (and invalidate their Redis cache) | Fixed — added ownership check after the lookup: `if (rec.userId !== userId) return res.status(403).json({ error: 'Forbidden' })` at line 63 |
| — | app/routes/recommendRouter.js | 182, 187 | `POST /classify` and `POST /generate` — no userId param, no IDOR risk; both proxy requests to Python recommender with the caller's own JWT | No action required |
| — | app/routes/kbRouter.js | all | No IDOR, no ObjectId construction, no user-scoped data; correctly gated to admin/researcher via `requireRole` in v1Router | No action required |
| — | app/routes/kbRouter.js | all | `sanitizeBody` global middleware in v1Router applies before this router; multipart upload path bypasses the JSON body parser entirely (raw stream piped), so sanitization of file contents is the Python API-service's responsibility | Document — acceptable; KB upload is admin/researcher only |
| — | app/routes/studyEnrollRouter.js | all | Duplicate enrollment: prevented atomically in `studyCodeService.redeemCode` via `findOneAndUpdate` with upsert + `$setOnInsert`; race condition between slot claim and enrollment insert handled with counter rollback (`$inc: { redemptionCount: -1 }`) | No action required — design is solid |
| — | app/routes/studyEnrollRouter.js | all | `skipCode` path: idempotent; concurrent requests resolved via atomic `$setOnInsert` upsert; `_skipCounter` incremented atomically so each concurrent caller receives a unique group index | No action required |
| — | app/routes/participantRouter.js | all | No IDOR: both `GET /questionnaires` and `POST /register-token` bind entirely to `req.user.sub`; no user-controlled userId param | No action required |
| — | app/routes/participantRouter.js | 78–86 | `_id` handling: response maps `q._id.toString()` to `id` field and does not include raw `_id` in output | No action required — clean |
| — | app/routes/participantRouter.js | 66 | `study.questionnaires` filtered with `instanceof ObjectId` before passing to `$in`; protects against corrupt data but means string IDs stored in that array would be silently dropped | Document — low risk; admin tooling should store ObjectId values in `questionnaires` array |
| P2 | app/routes/recommendationsRouter.js | 63 | IDOR guard does not include privileged-role bypass (`isPrivileged`) — inconsistent with `recommendRouter.js` which allows admins/researchers to access any user's data; admins cannot leave feedback on behalf of a participant | Backlog — make explicit: either add `isPrivileged` escape hatch or add comment stating admins are intentionally excluded from feedback submission |
| P2 | app/routes/recommendationsRouter.js | 56 | `findOne({ recommendation_id })` does not scope by `userId`; ownership is checked post-fetch rather than in the query — safer pattern is `findOne({ recommendation_id, userId })` to make authorization implicit | Backlog — harden query to include userId; 404 then masks IDOR attempts |
| P2 | app/tests/integration/recommendations.routes.test.js | — | No negative IDOR test: missing test asserting that USER_B gets 403 when posting feedback on USER_A's recommendation | Backlog — add IDOR regression test for the fix in this session |

## Backend Services

| Severity | File | Line | Finding | Fix |
|----------|------|------|---------|-----|
| P1 | app/services/notificationService.js | 182 | `dispatchDueNotifications` marked scheduled notification as `sent: true` even when `sendStudyNotification` threw an exception — the `updateOne` was outside the try/catch so a failed dispatch was silently swallowed and the notification would never be retried | Fixed — moved `updateOne` inside a conditional guarded by a `dispatched` flag; only marks sent on successful dispatch |
| — | app/services/notificationService.js | 142 | FCM invalid token cleanup confirmed present — `removeInvalidTokens` is called inside `sendStudyNotification` immediately after `sendToTokens` returns; tokens with `messaging/invalid-registration-token` or `messaging/registration-token-not-registered` error codes are deleted from `deviceTokens` collection | Confirmed safe — no action required |
| P2 | app/services/studyCodeService.js | 20–31 | `generateUniqueCodes` issues one `findOne` per generated code in a loop to check DB uniqueness — N+1 query pattern; for the default max batch of 100 codes this is 100 sequential queries | Backlog — for large batches, fetch all existing codes matching the generated set with a single `$in` query and filter in memory; acceptable at current scale (max 100 codes) |
| P2 | app/services/adminStatsService.js | 117–126 | `updateSetting` writes `key` and `value` directly to MongoDB without a whitelist; an admin could insert arbitrary keys polluting the `admin_settings` collection — same gap as the `PUT /settings/:key` route finding already documented | Backlog — enforce a whitelist of valid setting keys at the service layer (mirrors the route-layer recommendation) |
| P2 | app/services/adminHabitService.js | 12–13 | `buildHabitFilter` passes `from` and `to` query params through `new Date(from/to)` without validation; an invalid date string produces `Invalid Date` (NaN) which MongoDB stores as a special value and may produce unexpected query behaviour | Backlog — validate `from`/`to` are parseable ISO dates before constructing the filter; return a 400 if invalid |
| — | app/services/token_card_service.js | 13–107 | PDF is generated with user-controlled `userId`, `username`, and `password` passed to PDFKit `.text()` calls; PDFKit serialises these as raw PDF text objects — no HTML or script interpretation occurs; `username` is system-generated `p-<uuid>`, `password` is random base64url, `userId` is a UUID — no injection surface | Confirmed safe — no action required |
| — | app/services/adminParticipantService.js | 62 | Plaintext password stored in MongoDB — already documented under Backend Routes — Admin (P1 backlog) | Already tracked |
| — | app/services/habitDonationService.js | all | No N+1 patterns, no missing `await`, no unsafe input interpolation into DB queries; Neo4j writes use parameterised Cypher (`$uuid`, `$sentence`, etc.) throughout | No action required |
| — | app/services/studyService.js | all | Enrollment counts fetched with a single `$in` aggregation (not N+1); all ObjectId constructions wrapped in try/catch; soft-delete uses `isActive: false` consistently across read and write paths | No action required |
