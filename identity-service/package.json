{
  "name": "hhh-identity-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Identity register for verified-identity studies. Holds the subject-code <-> person mapping in its own database, encrypted, isolated from the research platform.",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test --test-force-exit \"tests/**/*.test.js\"",
    "lint": "npx eslint .",
    "format:check": "npx prettier --check ."
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "multer": "^2.0.0",
    "csv-parse": "^5.6.0",
    "pdfkit": "^0.18.0",
    "qrcode": "^1.5.4",
    "nodemailer": "^10.0.0",
    "pino": "^9.5.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
