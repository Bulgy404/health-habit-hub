# Local Mac development with Traefik hostnames and Docker-provider routing.
# iOS Simulator shares the Mac network stack, so localhost works directly.
# Usage: docker compose -f docker-compose.local.yml up
services:
  proxy:
    image: traefik:v3.6.1
    container_name: h3-2-proxy
    command:
      - --api.insecure=true
      - --api.dashboard=true
      - --api.debug=true
      - --log.level=INFO
      - --accesslog=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.network=h3-2-proxy
      - --entrypoints.web.address=:80
    restart: always
    ports:
      - "${TRAEFIK_HOST_PORT80:-80}:80"
      - "${TRAEFIK_HOST_PORT8080:-8888}:8080"
    networks:
      - h3-2-proxy
    volumes:
      - ${DOCKER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.proxy.rule=Host(`${PROXY_PATH:-proxy}.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.proxy.entrypoints=web
      - traefik.http.routers.proxy.service=proxy
      - traefik.http.services.proxy.loadbalancer.server.port=8080

  app:
    image: hhh/app
    container_name: h3-2-app
    build: ./app
    restart: on-failure:3
    env_file:
      - .env
    environment:
      - NODE_ENV=development
      - MONGO_HOST=mongo
      - KEYCLOAK_URL=http://keycloak:8080
      - KEYCLOAK_REALM=${KEYCLOAK_REALM:-hhh}
      - KEYCLOAK_CLIENT_ID=${KEYCLOAK_CLIENT_ID:-hhh-flutter}
      - KEYCLOAK_ADMIN_CLIENT_ID=${KEYCLOAK_ADMIN_CLIENT_ID:-hhh-backend}
      - KEYCLOAK_ADMIN_CLIENT_SECRET=${KEYCLOAK_ADMIN_CLIENT_SECRET:-CHANGE_THIS_KEYCLOAK_BACKEND_SECRET}
      - API_SERVICE_SECRET=${API_SERVICE_SECRET}
    ports:
      - "3000:3000"
    networks:
      - h3-2-proxy
      - h3-2-app
    command: node --watch main.js
    depends_on:
      - proxy
      - mongo
      - fuseki
      - neo4j
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    labels:
      - traefik.enable=true
      - traefik.http.routers.app.rule=Host(`${APP_PATH:-app}.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.app.entrypoints=web
      - traefik.http.routers.app.service=app
      - traefik.http.services.app.loadbalancer.server.port=3000

  mongo:
    image: mongo:8.2.6
    container_name: h3-2-mongo
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USER:-admin}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD:-admin}
      - MONGO_INITDB_DATABASE=surveyjs
    ports:
      - "27017:27017"
    volumes:
      - ./mongo/entrypoint:/docker-entrypoint-initdb.d
      - mongo-local-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.runCommand({ping:1})"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    networks:
      - h3-2-app

  neo4j:
    image: neo4j:5
    container_name: h3-2-neo4j
    restart: unless-stopped
    environment:
      - NEO4J_AUTH=${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-neo4j_password_change_me}
      - NEO4J_PLUGINS=["n10s"]
      - NEO4J_server_directories_import=/import
      - NEO4J_dbms_security_procedures_unrestricted=n10s.*
      - NEO4J_dbms_security_procedures_allowlist=n10s.*
      - EXTENSION_SCRIPT=/neo4j-extension.sh
    ports:
      - "7474:7474"
      - "7687:7687"
    networks:
      - h3-2-proxy
      - h3-2-app
    volumes:
      - neo4j-local-data:/data
      - neo4j-local-logs:/logs
      - ./fuseki/init:/import:ro
      - ./neo4j/extension.sh:/neo4j-extension.sh:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:7474"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    depends_on:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.http.routers.neo4j.rule=Host(`neo4j.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.neo4j.entrypoints=web
      - traefik.http.routers.neo4j.service=neo4j
      - traefik.http.services.neo4j.loadbalancer.server.port=7474

  keycloak:
    image: quay.io/keycloak/keycloak:26.5.5
    container_name: h3-2-keycloak
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - KEYCLOAK_ADMIN=${KEYCLOAK_ADMIN:-admin}
      - KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD:-admin}
      - KC_DB=dev-file
    command: start-dev --import-realm --health-enabled=true
    ports:
      - "8080:8080"
    networks:
      - h3-2-proxy
      - h3-2-app
    volumes:
      - keycloak-local-data:/opt/keycloak/data
      - ./keycloak/hhh-realm.json:/opt/keycloak/data/import/hhh-realm.json:ro
      - ./keycloak/hhh-user-profile.json:/opt/keycloak/bootstrap/hhh-user-profile.json:ro
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/9000 && printf \"GET /health/ready HTTP/1.0\\r\\nHost: localhost\\r\\n\\r\\n\" >&3 && cat <&3 | grep -q UP'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    depends_on:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.http.routers.keycloak.rule=Host(`keycloak.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.keycloak.entrypoints=web
      - traefik.http.routers.keycloak.service=keycloak
      - traefik.http.services.keycloak.loadbalancer.server.port=8080

  keycloak-init:
    image: quay.io/keycloak/keycloak:26.5.5
    container_name: h3-2-keycloak-init
    restart: "no"
    env_file:
      - .env
    volumes:
      - ./keycloak/hhh-user-profile.json:/opt/keycloak/bootstrap/hhh-user-profile.json:ro
    networks:
      - h3-2-proxy
      - h3-2-app
    depends_on:
      keycloak:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-lc"]
    command: >
      'until /opt/keycloak/bin/kcadm.sh config credentials
      --server http://keycloak:8080
      --realm master
      --user "$$KEYCLOAK_ADMIN"
      --password "$$KEYCLOAK_ADMIN_PASSWORD";
      do sleep 2; done &&
      /opt/keycloak/bin/kcadm.sh update realms/master -s sslRequired=none &&
      /opt/keycloak/bin/kcadm.sh update realms/hhh -s sslRequired=none &&
      /opt/keycloak/bin/kcadm.sh update users/profile -r hhh -f /opt/keycloak/bootstrap/hhh-user-profile.json &&
      BACKEND_ID=$$(/opt/keycloak/bin/kcadm.sh get clients -r hhh -q clientId=hhh-backend --fields id --format csv --noquotes) &&
      /opt/keycloak/bin/kcadm.sh update clients/$$BACKEND_ID -r hhh -s secret="$$KEYCLOAK_ADMIN_CLIENT_SECRET" &&
      SA_ID=$$(/opt/keycloak/bin/kcadm.sh get clients/$$BACKEND_ID/service-account-user -r hhh --fields id --format csv --noquotes) &&
      /opt/keycloak/bin/kcadm.sh add-roles -r hhh --uid $$SA_ID --cclientid realm-management --rolename realm-admin'

  admin:
    image: hhh/admin
    container_name: h3-2-admin
    build: ./admin
    restart: on-failure:3
    env_file:
      - .env
    environment:
      - NEXTAUTH_URL=http://admin.${PATH_SUFFIX:-localhost}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-change-me-in-production}
      - KEYCLOAK_CLIENT_ID=hhh-admin
      - KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_ADMIN_UI_CLIENT_SECRET:-hhh-admin-secret-change-me}
      # KEYCLOAK_ISSUER is used server-side (NextAuth JWT validation); must use the internal Docker
      # hostname so that token issuer claims match. KEYCLOAK_BROWSER_URL is used for the browser
      # redirect and must remain as localhost so the host browser can reach Keycloak.
      - KEYCLOAK_ISSUER=http://keycloak:8080/realms/hhh
      - KEYCLOAK_BROWSER_URL=http://localhost:8080
      - KEYCLOAK_INTERNAL_URL=http://keycloak:8080
    ports:
      - "3001:3001"
    networks:
      - h3-2-proxy
      - h3-2-app
    depends_on:
      proxy:
        condition: service_started
      keycloak:
        condition: service_healthy
      keycloak-init:
        condition: service_completed_successfully
    labels:
      - traefik.enable=true
      - traefik.http.routers.admin.rule=Host(`admin.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.admin.entrypoints=web
      - traefik.http.routers.admin.service=admin
      - traefik.http.services.admin.loadbalancer.server.port=3001

  translate:
    image: libretranslate/libretranslate:v1.9.5
    container_name: h3-2-translate
    restart: always
    env_file:
      - .env
    ports:
      - "5001:5000"
    networks:
      - h3-2-proxy
      - h3-2-app
    volumes:
      - translate-local-data:/root/.local/share/argos-translate/packages
    tty: true
    depends_on:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.http.routers.translate.rule=Host(`${TRANSLATE_HOST:-translate}.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.translate.entrypoints=web
      - traefik.http.routers.translate.service=translate
      - traefik.http.services.translate.loadbalancer.server.port=5000

  redis:
    image: redis:7-alpine
    container_name: h3-2-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-local-data:/data
    command: redis-server --appendonly yes
    networks:
      - h3-2-app

  recommender:
    image: hhh/recommender
    container_name: h3-2-recommender
    build: ./API-service
    restart: on-failure:3
    env_file:
      - .env
    environment:
      - API_SERVICE_SECRET=${API_SERVICE_SECRET}
    ports:
      - "8001:8000"
    networks:
      - h3-2-proxy
      - h3-2-app
    depends_on:
      - redis

  fuseki:
    image: hhh/fuseki
    container_name: h3-2-fuseki
    build: ./fuseki
    restart: always
    env_file:
      - .env
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
    ports:
      - "3030:3030"
    networks:
      - h3-2-proxy
      - h3-2-app
    volumes:
      - fuseki-local-data:/fuseki
    depends_on:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.http.routers.fuseki.rule=Host(`${FUSEKI_PATH:-fuseki}.${PATH_SUFFIX:-localhost}`)
      - traefik.http.routers.fuseki.entrypoints=web
      - traefik.http.routers.fuseki.service=fuseki
      - traefik.http.services.fuseki.loadbalancer.server.port=3030

volumes:
  mongo-local-data:
  neo4j-local-data:
  neo4j-local-logs:
  keycloak-local-data:
  translate-local-data:
  fuseki-local-data:
  redis-local-data:

networks:
  h3-2-proxy:
    name: h3-2-proxy
  h3-2-app:
    name: h3-2-app
