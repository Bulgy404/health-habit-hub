---
import { useT, links, type Lang } from '../i18n/ui';
import PhoneMock from './PhoneMock.astro';
import AppStoreBadges from './AppStoreBadges.astro';
import Homepage from '../assets/mockups/Homepage.png';
import Habits from '../assets/mockups/Habits.png';
import GraphView from '../assets/mockups/GraphView.png';
import Recommendations from '../assets/mockups/Recommendations.png';
import SRHI from '../assets/mockups/SRHI.png';
const { lang } = Astro.props as { lang: Lang };
const t = useT(lang);
const feats = [
  { img: Homepage, t: t('p_s1_t'), p: t('p_s1_p') },
  { img: Habits, t: t('p_s2_t'), p: t('p_s2_p') },
  { img: GraphView, t: t('p_s3_t'), p: t('p_s3_p') },
  { img: Recommendations, t: t('p_s4_t'), p: t('p_s4_p') },
];
---
<header class="p-hero">
  <div class="hero-bg"></div>
  <div class="wrap">
    <div class="p-hero-in reveal">
      <span class="eyebrow">{t('nav_participants')}</span>
      <h1>{t('p_hero_title')}</h1>
      <p class="lead">{t('p_hero_lead')}</p>
      <AppStoreBadges lang={lang} />
      <a class="weblink" href={links.app}>{t('p_web')} → <span>habit.wiwi.tu-dresden.de</span></a>
    </div>
  </div>
</header>

{feats.map((f, i) => (
  <section class={i % 2 ? 'feat alt' : 'feat'}>
    <div class="wrap feat-in">
      <div class="feat-txt reveal"><h2>{f.t}</h2><p>{f.p}</p></div>
      <div class="reveal"><PhoneMock src={f.img} alt={f.t} /></div>
    </div>
  </section>
))}

<section class="feat srhi">
  <div class="wrap feat-in narrow">
    <div class="reveal"><PhoneMock src={SRHI} alt={t('p_s5_t')} /></div>
    <div class="feat-txt reveal"><h2>{t('p_s5_t')}</h2><p>{t('p_s5_p')}</p></div>
  </div>
</section>

<section id="how" class="how">
  <div class="wrap">
    <div class="reveal"><span class="eyebrow">{t('p_how_eyebrow')}</span><h2 class="sec-title">{t('p_how_title')}</h2></div>
    <div class="steps">
      <div class="step reveal"><span class="num">1</span><h3>{t('p_how_1_t')}</h3><p>{t('p_how_1_p')}</p></div>
      <div class="step reveal"><span class="num">2</span><h3>{t('p_how_2_t')}</h3><p>{t('p_how_2_p')}</p></div>
      <div class="step reveal"><span class="num">3</span><h3>{t('p_how_3_t')}</h3><p>{t('p_how_3_p')}</p></div>
    </div>
  </div>
</section>

<section class="p-trust">
  <div class="wrap p-trust-in reveal">
    <div><h2>{t('p_trust_t')}</h2><p>{t('p_trust_p')}</p></div>
    <div class="p-trust-cta"><AppStoreBadges lang={lang} /></div>
  </div>
</section>

<style>
  .p-hero{position:relative;overflow:hidden;padding:84px 0 72px}
  .hero-bg{position:absolute;inset:0;background:radial-gradient(700px 400px at 80% -12%,rgba(78,198,0,.14),transparent 60%)}
  .p-hero-in{position:relative;max-width:36ch}
  .p-hero h1{font-size:clamp(36px,5vw,58px);margin:14px 0 0}
  .p-hero .lead{font-size:clamp(17px,2vw,20px);color:var(--muted);margin:22px 0 30px}
  .weblink{display:inline-flex;gap:8px;margin-top:20px;font-size:14px;color:var(--green-dark);font-weight:600}
  .weblink span{color:var(--muted);font-weight:400}
  .feat{padding:64px 0}
  .feat.alt{background:var(--surface)}
  .feat-in{display:grid;grid-template-columns:1fr 1fr;gap:56px;align-items:center}
  .feat.alt .feat-in{direction:rtl}
  .feat.alt .feat-in>*{direction:ltr}
  .feat-txt h2{font-size:clamp(24px,3vw,34px);max-width:16ch}
  .feat-txt p{color:var(--muted);margin-top:14px;font-size:16.5px;max-width:44ch}
  .srhi{background:var(--green-tint)}
  .narrow{grid-template-columns:.8fr 1.2fr}
  .how{background:var(--slate);color:#e2e8f0}
  .how .eyebrow{color:var(--green-soft)}
  .how .sec-title{color:#fff}
  .steps{display:grid;grid-template-columns:repeat(3,1fr);gap:24px;margin-top:44px}
  .step{background:#243044;border:1px solid #334155;border-radius:18px;padding:30px}
  .step .num{display:grid;place-items:center;width:38px;height:38px;border-radius:11px;background:rgba(69,183,0,.16);color:var(--green-soft);font-family:'Fraunces Variable',serif;font-size:20px;margin-bottom:18px}
  .step h3{color:#fff;font-size:20px}
  .step p{color:#94a3b8;font-size:14.5px;margin-top:8px}
  .p-trust{padding:72px 0}
  .p-trust-in{display:flex;justify-content:space-between;align-items:center;gap:36px;flex-wrap:wrap}
  .p-trust h2{font-size:clamp(24px,3vw,32px);max-width:18ch}
  .p-trust p{color:var(--muted);margin-top:12px;max-width:46ch}
  @media(max-width:900px){.feat-in,.narrow,.steps,.p-trust-in{grid-template-columns:1fr}.feat.alt .feat-in{direction:ltr}}
</style>
