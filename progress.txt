## 2026-03-21 - US-165
- What was implemented: Reviewed `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`, `Makefile`, and all 11 files under `scripts/`. Report written to `tasks/review-infra-clean.md` covering: 3 Critical, 8 Major, 9 Minor findings. Positive findings section documents what is well-written. Prioritised fix order table included.
- Files changed: tasks/review-infra-clean.md (new), prd.json (US-165 passes=true)
- **Learnings for future iterations:**
  - `deploy.yml:47` uses `actions/checkout@v6` which doesn't exist — will break the release job on any `v*` tag push; should be `@v4`
  - 3 deploy scripts (`deploy-backend.sh`, `deploy-recommender.sh`, `deploy-keycloak.sh`) use deprecated `docker-compose` (v1 hyphen CLI) — removed from Docker 27+; correct form is `docker compose` (space, v2 plugin)
  - `deploy-keycloak.sh:52-53` extracts the Keycloak admin token with brittle `grep | cut` instead of `jq -r '.access_token'` — compare to `restore.sh` which uses jq correctly
  - `parseCypherStatements()` is copy-pasted verbatim between `run-migration.js` and `seed-local.js` — extract to `scripts/lib/cypher-utils.mjs`
  - `wait_healthy()` and `run()` dry-run wrapper are copy-pasted across the 4 sub-deploy scripts — extract to `scripts/lib/common.sh`
  - `deploy-full.sh` polls the backend health URL after deploying Keycloak and Recommender (wrong service being checked)
  - `generate-spec.js` has a dead 44-line custom `toYaml()` function that is never called — `js-yaml` is imported and used instead
  - `ontology-test` CI job embeds ~65 lines of Bash + Python directly in YAML — should be extracted to `tests/ontology/setup.py`
---

## 2026-03-21 - US-164
- What was implemented: Reviewed `app/utils/Neo4jDatabase.js`, `app/utils/SparqlDatabase.js`, all service/route files containing inline Cypher (`habitsRouter.js`, `adminParticipantService.js`, `adminStatsService.js`), and all `.cypher` files for clean code violations. Report written to `tasks/review-neo4j-clean.md` covering: 3 Critical, 7 Major, 5 Minor findings. Positive findings section documents what is well-written. Prioritised fix order table included.
- Files changed: tasks/review-neo4j-clean.md (new), prd.json (US-164 passes=true)
- **Learnings for future iterations:**
  - `SparqlDatabase.js:70-72` has a critical bug: `isClosedTaskClosedDescription`, `isOpenTaskClosedDescription`, `isOpenTaskOpenDescription` are referenced without `()` — truthy function references mean all non-Group1 donations are silently assigned to Group2. Neo4jDatabase.js has the same class written correctly.
  - `translate()` function is copy-pasted in full between `Neo4jDatabase.js` and `SparqlDatabase.js` — the Neo4j version takes `config` as a parameter while the Fuseki version reads a module-level singleton, masking the duplication
  - `ExperimentalSetting`, `Donor`, `Label`, `Donation` domain model classes are duplicated across both database files — should be in `app/models/donation.js`
  - `_buildDonationTurtle` in Neo4jDatabase.js is 149 lines with mixed concerns; `iri()` helper is re-created as a local const on every call at line 337
  - `new Donor(donation)` at Neo4jDatabase.js:261 is dead code — the donor UUID is generated fresh inside `_buildDonationTurtle` at line 338
  - `migrate-group-labels.cypher` Step 2 uses deprecated Neo4j 5 `exists()` function at line 48
---

## 2026-03-21 - US-163
- What was implemented: Reviewed all JS files under `app/` for clean code violations. Report written to `tasks/review-backend-clean.md` covering: 8 Critical, 8 Major, 7 Minor findings with file path, line range, violation type, and suggested fix for every entry. Positive findings section documents what is well-written. Prioritised fix order table included.
- Files changed: tasks/review-backend-clean.md (new), prd.json (US-163 passes=true)
- **Learnings for future iterations:**
  - `getDb()` helper is copy-pasted verbatim in 8 route files (habitsRouter, recommendationsRouter, questionnaireResponsesRouter, questionnairesRouter, usersRouter, profileRouter, surveyRouter, adminRouter) — BK-C4 is the highest-DRY-value fix in the backend
  - `middleware/auth.js` has two exported functions (`createTokenVerifier` and `createAuthMiddleware`) that duplicate JWKS caching logic — `createAuthMiddleware` should compose `createTokenVerifier` internally
  - `donateRouter.js` logs reCAPTCHA key metadata at module load time (line 35-45) and has a dead `/submit-form` route with `console.log(req.body)` — both are clean code and security concerns
  - 4 `catch (_err)` blocks in usersRouter.js and profileRouter.js silently discard errors with no logging — contrast with the correct `console.error('[route] Error:', err)` pattern used in all other routes
  - POST /habits/donate at lines 467-661 of habitsRouter.js is a 143-line god handler — the highest-priority SRP violation in the codebase
  - The `translate()` function in habitsRouter.js (lines 54-133) is 80 lines with 4 nesting levels and should be split into `fetchLibreTranslation()` and `refineLLMTranslation()`
  - `_buildDonationTurtle()` in Neo4jDatabase.js (lines 327-476) is 149 lines; the `iri()` helper is redefined as a local const on every call at line 337
  - 8 debug `console.log` statements in app.js middleware fire on every request (lines 107, 108, 126, 144, 165, 167, 170, 172)
---

## 2026-03-21 - US-162
- What was implemented: Reviewed all 56 Dart files under `mobile/lib/` for clean code violations. Report written to `tasks/review-flutter-clean.md` covering: 3 Critical, 11 Major, 14 Minor findings with file path, line range, violation type, and suggested fix for every entry. Positive findings section documents what is well-written. Prioritised fix order table included.
- Files changed: tasks/review-flutter-clean.md (new), prd.json (US-162 passes=true)
- **Learnings for future iterations:**
  - `lib/screens/profile_screen.dart:28` has a hardcoded production API URL (`https://api.hhh.tu-dresden.de/api/v1`) — not fixed by US-137; must be fixed before any staging/dev deploy
  - `_authHeaders()` pattern is duplicated in 6 service files: `habit_service`, `survey_service`, `recommendation_service`, `admin_service`, `questionnaire_service`, `auth_service` — candidates for a shared `AuthHeadersMixin`
  - `_OfflineBanner` private widget is duplicated in `donate_screen.dart` and `profile_screen.dart` — extract to `lib/widgets/offline_banner.dart`
  - `AdminService` is a god service with 16 public methods across 6 domains — splitting it would improve testability and cohesion
  - `admin_participants_screen.dart` and `admin_habits_screen.dart` are god widgets (500–661 lines) — the largest refactoring candidates in the mobile codebase
  - Silent `catch (_) {}` blocks remain in `explore_screen.dart`, `recommend_screen.dart`, and `admin_habits_screen.dart` — US-137 fixed `donate_screen` and `locale_provider` but not these
---

## 2026-03-21 - US-159
- What was implemented: Created `scripts/seed-local.js` — idempotent seed script that (1) MongoDB: upserts SLIQ and RAND-36 questionnaires from `mongo/seed/questionnaires.json` using replaceOne+upsert; (2) Neo4j: polls HTTP API until ready (30 attempts × 3s = 90s max), applies constraints.cypher, seeds 4 Group nodes (`hhh__ExperimentalSetting:hhh__Group1-4`), 1 test Donor (`dev-user-1`), 2 Habit nodes via MERGE; (3) Keycloak: gets master-realm admin token via admin-cli password grant, creates testuser/testpass123 in `hhh` realm, assigns participant role — all steps check existence before acting. Added `"seed": "node ../scripts/seed-local.js"` to `app/package.json` scripts. All 265 tests pass.
- Files changed: scripts/seed-local.js (new), app/package.json (seed script added), prd.json (US-159 passes=true)
- **Learnings for future iterations:**
  - Scripts in `scripts/` have no `node_modules` — to use npm packages installed in `app/`, use `createRequire(resolve(__dirname, '../app/package.json'))` which loads modules relative to app/, finding `app/node_modules/`
  - Neo4j 5 HTTP transaction API endpoint: `POST /db/neo4j/tx/commit` with `{"statements":[{"statement":"...","parameters":{}}]}` — basic auth header required
  - Keycloak admin token for seed scripts: use master-realm `admin-cli` client with `grant_type=password` (not client_credentials) — client_credentials requires a configured service account; master admin username/password works out of the box
  - MERGE with multiple labels in Neo4j Cypher requires literal labels — cannot use parameterized labels; write one MERGE statement per group node
  - The `mongo/seed/questionnaires.json` file already has full SLIQ and RAND-36 definitions — seed script reads it directly rather than duplicating data
---

## 2026-03-21 - US-156
- What was implemented: Extracted business logic from `adminRouter.js` into three focused service modules. `app/services/adminParticipantService.js` — `listParticipants`, `createParticipant`, `assignGroup`, `getParticipant`, `softDeleteParticipant`. `app/services/adminHabitService.js` — `getHabitsFeed`, `buildHabitsCSV`. `app/services/adminStatsService.js` — `getParticipantProgress`, `getSettings`, `updateSetting`. Each service accepts `{ neo4jRun, db }` (plus `kc` where Keycloak is needed). `adminRouter.js` reduced from ~1799 to route definitions + thin controller calls — no inline Cypher strings or business logic remain in the router. Removed now-redundant `randomPassword()`, `VALID_GROUPS`, `randomBytes`, `createHash` from the router. Created `app/tests/unit/admin.services.test.js` with 17 unit tests covering all three services using mocked `neo4jRun`. Updated `tasks/review-backend.md` to mark OF-12 as ✅ Resolved. All 265 tests pass; lint and Prettier pass.
- Files changed: app/routes/adminRouter.js (refactored), app/services/adminHabitService.js (new), app/services/adminParticipantService.js (new), app/services/adminStatsService.js (new), app/tests/unit/admin.services.test.js (new), tasks/review-backend.md (OF-12 resolved), prd.json (US-156 passes=true)
- **Learnings for future iterations:**
  - Service functions accept `{ db, neo4jRun, kc, ... }` — the router calls them with `neo4jRun` from the factory closure directly, no re-wrapping needed
  - `adminParticipantService.assignGroup` handles Keycloak + MongoDB + Neo4j atomically; the router only needs to pass `kc: getKeycloak()` and `neo4jRun` from the factory — the VALID_GROUPS and GROUP_LABEL_MAP constants live inside the service, not the router
  - Prettier formatting check runs before lint in `npm test` — always run `npx prettier --write` on new files in app/ before committing
  - The service files existed as untracked stubs before this story; they needed to be completed and wired up, not created from scratch
---

## 2026-03-21 - US-153
- What was implemented: Added `admin-build` job (job 11) to `.github/workflows/ci.yml` — Node 22, npm cache keyed on `admin/package-lock.json`, `npm ci` in `admin/`, `npx tsc --noEmit` (typecheck), and `npm run build`. Added `docker build -f admin/Dockerfile -t hhh-admin admin/` step to the existing `docker-build` job. Added `admin-build` to the `ci-passed` gate's `needs` list. Updated `tasks/review-cicd.md` to mark CV2 and CV3 as ✅ Resolved (US-153).
- Files changed: .github/workflows/ci.yml (admin-build job + docker-build step + ci-passed needs), tasks/review-cicd.md (CV2/CV3 resolved), prd.json (US-153 passes=true)
- **Learnings for future iterations:**
  - The review-cicd.md uses finding IDs like CV2/CV3, not OF-09 — "OF-09" in the PRD notes is a cross-reference alias; update the CV2/CV3 rows in the Major table
  - `npx tsc --noEmit` works for typechecking Next.js apps that have a `tsconfig.json`; no separate `tsc` install needed since TypeScript is a devDependency
  - Docker build command for admin uses `-f admin/Dockerfile -t hhh-admin admin/` pattern (explicit Dockerfile path + tag + build context)
---

## 2026-03-21 - US-148
- What was implemented: Migrated Keycloak in docker-compose.prod.yml from embedded dev-file store to PostgreSQL. Added `keycloak-db` service (postgres:16-alpine) with `KC_DB_USERNAME`/`KC_DB_PASSWORD` env vars, `keycloak-db-data` named volume, and a `pg_isready` healthcheck. Updated `keycloak` service: replaced `KC_DB=dev-file` with `KC_DB=postgres`, `KC_DB_URL=jdbc:postgresql://keycloak-db:5432/keycloak`, `KC_DB_USERNAME`/`KC_DB_PASSWORD`; added `depends_on: keycloak-db: condition: service_healthy`; removed the host-path data volume `/mnt/data/appdata/hhh/keycloak:/opt/keycloak/data` (PostgreSQL replaces file storage). Declared `keycloak-db-data` in top-level volumes block. Added `KC_DB_USERNAME`/`KC_DB_PASSWORD` to `.env.example` (with placeholder values and comment) and to `stack.env` (production Portainer env file). Added section 4b to `DEPLOYMENT.md` explaining realm re-import requirement for first deploy after migration. Added "Keycloak DB unavailable — PostgreSQL not ready" troubleshooting entry to `docs/runbook.md` covering diagnosis commands and two fix paths (DB not started / password mismatch). Also updated the runbook Table of Contents. All 247 tests pass; lint passes.
- Files changed: docker-compose.prod.yml (keycloak-db service + keycloak postgres config + volumes), .env.example (KC_DB_USERNAME/KC_DB_PASSWORD), stack.env (KC_DB_USERNAME/KC_DB_PASSWORD), DEPLOYMENT.md (new section 4b, renumbered 4b→4c, 4c→4d, 4c→4e), docs/runbook.md (ToC + new troubleshooting entry), prd.json (US-148 passes=true)
- **Learnings for future iterations:**
  - Keycloak `depends_on: keycloak-db: condition: service_healthy` requires a healthcheck on the postgres service — `pg_isready -U ${KC_DB_USERNAME} -d keycloak` is the correct check; without it, Compose ignores the health condition
  - Removing `KC_DB=dev-file` and the host volume `/opt/keycloak/data` is correct; the import volume (`./keycloak/hhh-realm.json:/opt/keycloak/data/import/hhh-realm.json:ro`) is a different path and must be kept for realm auto-import
  - `stack.env` is the Portainer production env file (distinct from `.env.example`); both must be updated when adding new env vars
  - Existing Keycloak users/credentials are NOT migrated automatically when switching from dev-file to Postgres — operators must re-create them manually; document this explicitly
---

## 2026-03-21 - US-145
- What was implemented: Created `scripts/migrate-hhh-habit-to-habit.cypher` — idempotent Cypher migration that (1) MERGE's each `hhh__Habit` node into a new `Habit` node using `coalesce(h.hhh__id, h.uri)` as the `uuid`; maps `hhh__value` → `sentence`, `hhh__language` → `language` (defaulting to 'de'), `hhh__habitStrength` → `habitStrength`; sets `translationEN/translationDE/bcioClass/dimension/confidence` to null; sets `created_at` to `toString(datetime())` and `migrated_from_hhh = true` on new nodes; (2) MERGE's `hhh__Donor-[:DONATED]->:Habit` for every existing `hhh__Donor-[:hhh__donates]->hhh__Habit` triple. Created `scripts/run-migration.js` — Node.js ESM script that runs a pre-flight count query (total/toMigrate/alreadyExist), parses the Cypher file, runs each statement, and prints `Migrated X habits, skipped Y (already exist)`. Updated `DEPLOYMENT.md` with a new section 4a documenting the post-deploy migration step, with expected output for first-run and idempotent re-runs; renumbered old 4a/4b as 4b/4c. All 247 tests pass; lint passes.
- Files changed: scripts/migrate-hhh-habit-to-habit.cypher (new), scripts/run-migration.js (new), DEPLOYMENT.md (new 4a section), prd.json (US-145 passes=true)
- **Learnings for future iterations:**
  - Old `hhh__Habit` nodes use `hhh__id` as string ID and `hhh__value` as habit text; new `Habit` nodes use `uuid` and `sentence` — map `hhh__value → sentence` (not `value`) for GET /habits compatibility
  - `coalesce(h.hhh__id, h.uri)` is the correct uuid source — n10s may store the RDF URI as `uri` on nodes when there's no explicit `hhh__id`
  - Neo4j `toString(datetime())` produces an ISO 8601 string — compatible with existing `created_at` fields stored as strings in new Habit nodes
  - `migrated_from_hhh = true` flag on new Habit nodes is useful for debugging but only fires on ON CREATE — idempotent re-runs won't touch existing Habit nodes
  - ON CREATE SET in MERGE is idempotent by design: existing Habit nodes created by the donate pipeline are untouched; only new nodes get migration properties
  - Cypher file has two `;`-delimited statements; `run-migration.js` splits on `;` and strips comment lines before running each statement
---

## 2026-03-21 - US-141
- What was implemented: Updated all backend and API documentation to match the current codebase. (1) `docs/api/openapi.yaml`: added `DonatedHabit` and `UserPreferences` component schemas; added 14 previously undocumented paths: `POST /onboard`, `GET /habits`, `POST /habits/donate`, `GET /questionnaires`, `GET /questionnaires/{slug}`, `POST /questionnaire-responses`, `GET /questionnaire-responses/me`, `GET /questionnaire-responses/me/{slug}`, `GET /recommendations/me`, `POST /recommendations/{recommendation_id}/feedback`, `GET /users/me`, `PUT /users/me`, `GET|POST /kb`, `POST /kb/reindex`, `DELETE /kb/{filename}`. (2) `docs/api/hhh-postman-collection.json`: added 7 new folder groups (Onboard, Habits–Donate Pipeline, Questionnaires, Recommendations, Users, Knowledge Base) with requests and example responses for all new endpoints. (3) `README.md`: added Backend section with npm scripts table and comprehensive env var reference (18 variables with defaults and descriptions). (4) `docs/data-model.md`: added Section 1.1 documenting the new Neo4j schema (`Habit`, `Context`, `BCIOConcept` labels + `HAS_CONTEXT`, `MAPS_TO` relationships); updated old schema section headers; added 6 new MongoDB collections (`users`, `questionnaires`, `form_responses`, `recommendations`, `recommendation_feedback`, `habits`) with field tables and example documents.
- Files changed: docs/api/openapi.yaml, docs/api/hhh-postman-collection.json, README.md, docs/data-model.md, prd.json (US-141 passes=true)
- **Learnings for future iterations:**
  - The `openapi.yaml` was missing all endpoints added after US-116: donate pipeline, questionnaires, questionnaire-responses, recommendations feedback, users preferences, and knowledge base — 14 paths total
  - The openapi.yaml `Habit` schema described the old `hhh__Habit` public list format; the new GET /habits returns a completely different shape (`uuid`, `original`, `translationEN`, `translationDE`, optional `displayText`) — needed a separate `DonatedHabit` schema
  - `users` collection stores `preferredLanguage` per Keycloak `sub`; `GET /users/me` returns a synthetic default `{preferredLanguage:"en"}` without persisting — important for documentation accuracy
  - `form_responses` collection (used by `/questionnaire-responses`) is indexed on `(userId, questionnaireSlug, submitted_at DESC)` at router startup
  - KB routes (`/api/v1/kb`) require admin or researcher role; all four operations (list, upload, reindex, delete) proxy directly to API-service
---

## 2026-03-21 - US-137
- What was implemented: Applied Critical and Major Flutter improvements from review-flutter.md. C-3: replaced hard-coded `https://api.hhh.tu-dresden.de/api/v1` URL in donate_screen.dart with `AppConfig.apiBaseUrl`. C-1: added `debugPrint('ERROR in ...: $e\n$st')` to all silent `catch (_)` blocks in donate_screen.dart and locale_provider.dart. M-2: replaced `Navigator.push(MaterialPageRoute...)` in goal_input_screen.dart with `context.push('/recommend/loading', extra: goal)` and added the `/recommend/loading` GoRoute as a sub-route of `/recommend` in main.dart. M-4: created `mobile/lib/core/exceptions.dart` with sealed `AppException` hierarchy (NetworkException, UnauthorisedException, ServerException, ValidationException). M-5: added WebView JS bridge message validation in donate_screen.dart — `jsonDecode` + `data is! Map<String, dynamic>` type guard before processing. M-9: added 8 ARB keys to app_en.arb and app_de.arb; replaced hard-coded English strings in stats_screen.dart, questionnaire_screen.dart (including confirmation screen), and goal_input_screen.dart. M-10: applied defensive `(json['field'] ?? '').toString()` null-safety in Survey.fromJson (3 fields) and Recommendation/RagCitation.fromJson (7 fields). M-11: added GoRouter auth guard in main.dart redirect that checks `isLoggedInProvider` and redirects unauthenticated users from `/donate`, `/explore`, `/recommend`, `/profile`, `/settings`, `/questionnaire` to `/onboarding/welcome`. All Critical and Major findings resolved or explicitly deferred with written reasons. tasks/review-flutter.md updated with Resolution notes per finding.
- Files changed: mobile/lib/core/exceptions.dart (new), mobile/lib/l10n/app_en.arb, mobile/lib/l10n/app_de.arb, mobile/lib/l10n/app_localizations*.dart (regenerated), mobile/lib/screens/donate_screen.dart, mobile/lib/providers/locale_provider.dart, mobile/lib/main.dart, mobile/lib/features/recommendation/goal_input_screen.dart, mobile/lib/models/survey.dart, mobile/lib/models/recommendation.dart, mobile/lib/screens/stats_screen.dart, mobile/lib/features/questionnaire/questionnaire_screen.dart, mobile/test/widget/stats_screen_test.dart (added l10n delegates), tasks/review-flutter.md (Resolution notes added), prd.json (US-137 passes=true)
- **Learnings for future iterations:**
  - `bip39_wordlist.dart` is NOT dead code — `passphrase_screen.dart:8` imports it; the review finding A-3 was incorrect
  - `flutter gen-l10n` must be run after editing any `.arb` file before running `flutter analyze` — new ARB keys produce new methods in `app_localizations.dart` that must exist before source files reference them
  - GoRouter sub-routes use relative paths (e.g., `path: 'loading'`) under a parent GoRoute; access via `context.push('/recommend/loading', extra: goal)` from anywhere in the app
  - Auth guard in GoRouter `redirect` must be placed BEFORE the onboarding bypass check to avoid a case where an unauthenticated user visits `/donate` and gets redirected to `/donate` (via onboarding bypass) in a loop
  - The 23 pre-existing test failures are all `MaterialApp` widgets missing `AppLocalizations.delegate` — they were failing before US-137; adding delegates to `stats_screen_test.dart` fixed one of them
---

## 2026-03-21 - US-136
- What was implemented: Cross-component coherence review synthesising all five individual component reports (US-131–US-135) against the live codebase. Report written to `tasks/review-system-coherence.md` covering 8 sections: API contract alignment, auth flow end-to-end, language/locale consistency, error message consistency, test strategy coherence, naming consistency, what is well-integrated, and prioritised cross-cutting improvements (6 Critical, 10 Major, 10 Minor).
- Files changed: tasks/review-system-coherence.md (new), prd.json (US-136 passes=true)
- **Learnings for future iterations:**
  - The most severe cross-cutting issue is the dual Neo4j schema — `Habit` (new donate pipeline) and `hhh__Habit` (old n10s pipeline) co-exist in the same DB, causing stats and public lists to always show 0 for newly donated habits. US-137/138/139 must resolve this as a prerequisite for any meaningful stats.
  - Silent error swallowing is systemic at BOTH layers: backend catch blocks log nothing, and Flutter catch blocks discard everything. Together they create a complete production debugging blackout — an error in the Neo4j donate pipeline surfaces as nothing to the user.
  - The `donate_screen.dart:26` hard-coded production URL means any testing on staging/dev silently hits production data — this must be fixed before any test deployment.
  - The CI has been entirely broken for the entire branch (`@v6` action versions do not exist) — all quality checks are unverified. Fix action versions before attempting any further CI-dependent work.
  - What IS coherent and safe to rely on: (1) `realm_access.roles` JWT claim path is consistent across Flutter, backend, and admin panel; (2) `preferredLanguage` field name is consistent end-to-end; (3) `displayText` API convention is clean and consistently consumed by Flutter; (4) ISO 639-1 `'en'`/`'de'` codes are consistent everywhere in the live code path.
---

## 2026-03-21 - US-134
- What was implemented: Senior DevOps engineer review of both GitHub Actions workflows — `.github/workflows/ci.yml` (10 jobs) and `.github/workflows/deploy.yml` (2 jobs). Report written to `tasks/review-cicd.md` covering 8 sections with 4 Critical, 8 Major, and 7 Minor findings. Every finding tied to specific job/step with concrete fix instructions.
- Files changed: tasks/review-cicd.md (new), prd.json (US-134 passes=true)
- **Learnings for future iterations:**
  - All three `actions/checkout@v6`, `actions/setup-node@v6`, and `actions/upload-artifact@v6` in ci.yml are non-existent versions — the latest stable are all `@v4`. Every CI job is currently broken at the action-resolution step before any code runs.
  - `API-service/` (Python FastAPI) has zero CI coverage — no lint, no type-check, no test job — despite having test files written in US-098/099/114.
  - `admin/` Next.js panel and its Dockerfile are not validated in any CI job and not included in Docker build validation or release artifacts.
  - The `deploy.yml` tag-push path (lines 10-12) bypasses CI entirely — tagging a broken commit triggers a production deploy with zero gating.
  - Fuseki service is not started in the integration test job despite Fuseki-backed routes existing in the backend.
---

## 2026-03-21 - US-133
- What was implemented: Senior data engineer review of the Neo4j/ontology layer — `neo4j/init/constraints.cypher`, `app/utils/Neo4jDatabase.js`, `app/utils/config.js`, `Ontology.ttl`, `fuseki/init/schema.ttl`, `fuseki/init/data.ttl`, `scripts/migrate-group-labels.cypher`, and all embedded Cypher in `habitsRouter.js` + `adminRouter.js`. Report written to `tasks/review-neo4j.md` covering 7 sections with 5 Critical, 8 Major, and 6 Minor findings. Every finding tied to specific file and line range.
- Files changed: tasks/review-neo4j.md (new), prd.json (US-133 passes=true)
- **Learnings for future iterations:**
  - The codebase has TWO incompatible Neo4j schemas: old n10s/RDF-mapped nodes (`hhh__Habit`, `hhh__Donor`, `hhh__hasBehavior`) from `Neo4jDatabase.js`, and new raw-Cypher nodes (`Habit`, `Context`, `BCIOConcept`, `HAS_CONTEXT`, `MAPS_TO`) from `habitsRouter.js`. Stats and public-list endpoints query old schema; donate creates new schema — they are disjoint datasets in the same DB.
  - `neo4j/init/constraints.cypher` only covers old `hhh__` schema. New `Habit` label has no uniqueness constraint on `uuid` and no index on `Context(text, dimension)` — both needed urgently.
  - `scripts/migrate-group-labels.cypher:48` uses `exists()` which was deprecated Neo4j 4.2 and removed in Neo4j 5.0 — migration is broken on current Neo4j.
  - `Ontology.ttl:43` declares `hasTranslation` with `rdfs:domain hhh:Behavior`, but `Neo4jDatabase.js:406` uses it on `Habit` nodes — OWL domain violation.
  - `hhh:language` range declared `rdf:langString` but stored as plain string — invalid Turtle syntax in data.ttl.
---

## 2026-03-21 - US-132
- What was implemented: Senior Node.js engineer review of the entire `app/` directory — all routes, controllers, middleware, utils, tests (30 test files). Report written to `tasks/review-backend.md` covering 9 sections with 24 prioritised findings. Every finding tied to specific file and line range.
- Files changed: tasks/review-backend.md (new), prd.json (US-132 passes=true)
- **Learnings for future iterations:**
  - Critical backend findings: (1) Neo4j driver created and destroyed on every query in `habitsRouter.js:22-36` — no connection pooling; (2) IDOR on `/recommend/:userId` — any participant can fetch another's recommendations (`recommendRouter.js:66-72`); (3) no JWT audience/issuer validation in `auth.js:80-95`; (4) internal recommendations endpoint has zero auth (`internalRouter.js`); (5) rate limiter runs before auth so per-user limits never work (`v1Router.js:128-132`); (6) JWKS cached forever with no refresh mechanism (`auth.js:31-39`)
  - Major: `createdAt` vs `created_at` naming inconsistency across Neo4j/MongoDB; survey form submitted but `survey_id` not validated against DB; `translateAndRefine` silently swallows all LLM errors without alerting
  - The factory injection pattern (`createHabitsRouter({db, neo4jRun, apiServiceUrl})`) is excellent — makes all routers fully testable without live services
  - 30 test files with real JWT crypto in test assertions is a genuine strength
---

## 2026-03-21 - US-131
- What was implemented: Senior engineer review of the Flutter mobile app (`mobile/lib/` — 55 Dart files, `mobile/test/` — 9 test files). Report written to `tasks/review-flutter.md` covering architecture, code quality, error handling, testing, security, i18n readiness, what is done well, and prioritised improvements (Critical/Major/Minor). Every finding tied to specific file and line range.
- Files changed: tasks/review-flutter.md (new), prd.json (US-131 passes=true)
- **Learnings for future iterations:**
  - Critical findings in Flutter app: (1) silent `catch (_) {}` across all services makes production debugging impossible; (2) hard-coded API URL in `donate_screen.dart:26` bypasses `AppConfig`; (3) zero unit tests for service classes
  - Major: `_authHeaders()` duplicated in 6 service files — extract to shared Dio interceptor; `goal_input_screen.dart:27` uses legacy `Navigator.push()` instead of GoRouter; admin screen has 11 local state fields that should be a `StateNotifierProvider`
  - `bip39_wordlist.dart` in `mobile/lib/utils/` is unreferenced — likely dead code from a removed passphrase feature
  - i18n: 3 screens still have hard-coded English strings (`goal_input_screen.dart:46`, `stats_screen.dart:72`, `questionnaire_screen.dart:39`)
---

## 2026-03-20 - US-116
- What was implemented: Updated GET `/api/v1/habits` route in `habitsRouter.js` — replaced the placeholder `{ ok: true }` response with a proper Neo4j query that returns all `Habit` nodes with `uuid`, `original` (sentence), `language`, `translationEN`, and `translationDE` fields (null if not available). Added optional `?lang=en|de` query parameter that appends a `displayText` convenience field: uses `translationEN` if `lang=en` (falls back to `original`), uses `translationDE` if `lang=de` (falls back to `original`). Updated `auth.routes.test.js` to pass a `neo4jRun` mock (returns `[]`) so GET `/habits` no longer errors in that test. Updated `habit.controller.test.js` to expect an array instead of `{ ok: true }`. Added `FIXTURE_DONATED_HABITS` to `habits.routes.test.js` mock and updated `createMockNeo4jRun()` to detect `h.sentence AS original` in cypher. Added 4 new tests: 401 without token, response shape (uuid/original/language/translationEN/translationDE, no displayText), `?lang=de` displayText from translationDE (with fallback to original), `?lang=en` displayText from translationEN (with fallback to original). All 231 tests pass; lint/prettier pass.
- Files changed: app/routes/habitsRouter.js (replace GET / handler), app/tests/integration/habits.routes.test.js (fixture + 4 new tests), app/tests/integration/auth.routes.test.js (add neo4jRun mock), app/tests/unit/habit.controller.test.js (update GET /habits test), prd.json (US-116 passes=true)
- **Learnings for future iterations:**
  - GET `/api/v1/habits` (without suffix) queries `MATCH (h:Habit)` (new ontology) — not `hhh__Habit` (old ontology used by `/public`)
  - `auth.routes.test.js` creates v1Router without `neo4jRun` — any route that hits Neo4j without injection will 500; pass `neo4jRun: async () => []` for routes that need it
  - `displayText` fallback logic: `habit.translationXX || habit.original` — no `null` check needed as `||` handles both null and undefined
---

## 2026-03-20 - US-114
- What was implemented: Created `API-service/prompts/refine_translation.txt` — prompt instructing LLM to refine a literal machine translation into natural, tone-preserving English. Created `API-service/routers/refine_translation.py` with POST `/api/v1/llm/refine-translation` (request: `{original, raw_translation, language}`, response: `{refined_translation}`); falls back to `raw_translation` when LLM returns empty string. Registered router in `API-service/main.py`. Added `translateAndRefine(sentence, language, apiBase, translateUrl)` helper in `app/routes/habitsRouter.js`: (1) skips when `language` starts with 'en'; (2) calls LibreTranslate for English draft; (3) calls `/api/v1/llm/refine-translation` with 10 s AbortController timeout; (4) warns and falls back to raw draft if LLM fails or times out. Updated Habit node CREATE cypher to include `translationEN: $translationEN` (null for English habits). Added `libreTranslateUrl` factory param to `createHabitsRouter` and `createV1Router`, read from `LIBRE_TRANSLATE_URL` env with fallback to `TRANSLATE_HOST/PORT/PATH`. Created `app/tests/unit/habits.translation.test.js` (3 tests: refined translation stored, English habit gets null, LLM failure falls back to raw). Created `API-service/tests/test_refine_translation.py` (2 tests). All 224 Node.js tests pass; 2 Python tests pass; lint/format pass.
- Files changed: API-service/prompts/refine_translation.txt (new), API-service/routers/refine_translation.py (new), API-service/tests/test_refine_translation.py (new), API-service/main.py (register router), app/routes/habitsRouter.js (libreTranslateUrl param, translateAndRefine helper, translationEN in CREATE), app/routes/v1Router.js (libreTranslateUrl param + pass-through), app/tests/unit/habits.translation.test.js (new), prd.json (US-114 passes=true)
- **Learnings for future iterations:**
  - LibreTranslate API: POST with `{q, source, target, format: 'text'}`, response has `translatedText` field
  - Use `AbortController` + `setTimeout(controller.abort, 10000)` for LLM timeout; always `clearTimeout` in a `finally` block
  - Factory injection pattern: add `libreTranslateUrl` to `createHabitsRouter` alongside `apiServiceUrl`; env fallback builds from `TRANSLATE_HOST/PORT/PATH` or reads `LIBRE_TRANSLATE_URL`
  - Prettier formats `console.warn` template literals and arrow functions — always run `npx prettier --write` before `npm run test`
  - Python refine_translation router does NOT need Redis caching (translations are tone-specific per sentence and unlikely to repeat identically)
---

## 2026-03-20 - US-105
- What was implemented: Created `admin/` Next.js 14 App Router project. `src/lib/auth.ts` — NextAuth v4 config with `KeycloakProvider` (confidential client `hhh-admin`); JWT callback extracts `realm_access.roles` from Keycloak profile; session callback exposes `roles[]`. `src/middleware.ts` — edge middleware using `getToken` to enforce auth on all non-auth routes; users without `admin` or `researcher` role are redirected to `/access-denied`. `src/app/api/auth/[...nextauth]/route.ts` — NextAuth handler. `src/app/(admin)/layout.tsx` — shell layout with `Sidebar` component (fixed 240px, dark `#1e293b` background). Sidebar has brand logo, nav links (Questionnaires + Settings), user email + sign-out button. `src/app/(admin)/questionnaires/page.tsx` and `settings/page.tsx` — placeholder pages. `src/app/access-denied/page.tsx` — access-denied page for non-admin/researcher users. `src/app/layout.tsx` — root layout with `SessionProvider` wrapper. `next.config.mjs` with `output: 'standalone'` for Docker. `Dockerfile` multi-stage build (deps → builder → runner). Added `hhh-admin` confidential client to `keycloak/hhh-realm.json`. Added `admin` service to `docker-compose.yml` as `h3-admin` on port 3001 with Traefik label `admin.${PATH_SUFFIX:-localhost}`. `npm run build` passes with zero errors.
- Files changed: admin/ (entire new Next.js project), keycloak/hhh-realm.json (add hhh-admin client), docker-compose.yml (add admin service), prd.json (US-105 passes=true)
- **Learnings for future iterations:**
  - Next.js 14.2.x does NOT support `next.config.ts` — use `next.config.mjs` (ES module) or `next.config.js`; the TypeScript variant was added in Next.js 15
  - `next/typescript` ESLint config is Next.js 15+ only — use only `next/core-web-vitals` in `.eslintrc.json` for Next.js 14
  - For NextAuth v4 with Keycloak in App Router, the `profile` callback arg contains `realm_access.roles` on the JWT callback (first sign-in when `account` is present); subsequent refreshes must propagate roles via `token.roles`
  - The `(admin)` route group prefix makes the layout apply to `/questionnaires` and `/settings` without affecting their URL paths
  - Middleware `matcher` must exclude `api/auth`, `access-denied`, `_next/static`, `_next/image`, and `favicon.ico` to avoid redirect loops
  - Next.js `output: 'standalone'` produces `server.js` in `.next/standalone/` — Dockerfile CMD should be `node server.js`; set `HOSTNAME="0.0.0.0"` and `PORT=3001` env vars
  - Add `admin/.gitignore` to exclude `.next/` and `node_modules/` (build artifacts were accidentally committed in first commit; gitignore added in follow-up)
---

## 2026-03-20 - US-103
- What was implemented: Created `mobile/lib/features/questionnaire/` module — `questionnaire_models.dart` (QuestionnaireDefinition, Question, QuestionOption, QuestionType enum with singleChoice/multiChoice/scale/text), `questionnaire_service.dart` (Dio-based service: fetchQuestionnaire(slug) + submitResponse(slug, answers); FutureProvider.family for caching), `questionnaire_provider.dart` (StateNotifierProvider.family keyed by slug — tracks currentIndex + answers Map; nextQuestion/previousQuestion/setAnswer/reset), `questionnaire_form_widget.dart` (ConsumerStatefulWidget: paged step-by-step form; renders all 4 question types; progress indicator; required validation before Save & Continue / Submit; Submit POSTs via QuestionnaireService), `questionnaire_screen.dart` (generic QuestionnaireScreen parameterised by slug; error/loading states; pushReplacement to confirmation on submit; QuestionnaireConfirmationScreen). Added `POST /api/v1/questionnaire-responses` in `app/routes/questionnaireResponsesRouter.js` (inserts userId from req.user.sub, questionnaireSlug, answers, submitted_at into MongoDB form_responses). Registered in v1Router.js with participant/admin/researcher role guard. Added `/questionnaire/:slug` and `/questionnaire/:slug/confirmation` routes to main.dart. Added SLIQ and RAND-36 OutlinedButton.icon navigation in profile_screen.dart (visible in the profile summary card area). Flutter analyze: no issues. All 240 backend tests pass (7 pre-existing external-service failures).
- Files changed: mobile/lib/features/questionnaire/ (5 new files), mobile/lib/main.dart (import + 2 routes), mobile/lib/screens/profile_screen.dart (questionnaire buttons), app/routes/questionnaireResponsesRouter.js (new), app/routes/v1Router.js (import + mount), prd.json (US-103 passes=true)
- **Learnings for future iterations:**
  - Flutter 3.32+ deprecated `RadioListTile` / `Radio.groupValue` / `Radio.onChanged` — replace with a custom `ListTile` + `Icon(Icons.radio_button_checked/unchecked)` pattern to avoid analyzer warnings
  - `StateNotifierProvider.family<Notifier, State, String>` is the pattern for per-questionnaire Riverpod state; keyed by slug so SLIQ and RAND-36 have independent form state simultaneously
  - Dart enum values must be lowerCamelCase — map backend snake_case strings (e.g. `single_choice`) to camelCase enum values (e.g. `QuestionType.singleChoice`) in `fromJson`
  - After `answer is List` type check, Dart smart-casts — no `as List` cast needed in same boolean expression
  - `FutureProvider.family` caches the definition fetch per slug; use `ref.invalidate(questionnaireProvider(slug))` on error retry
---

## 2026-03-20 - US-100
- What was implemented: Added `POST /api/v1/habits/donate` to `app/routes/habitsRouter.js`. Route orchestrates M1.1→M1.2→M1.3 via fetch to API-service (configurable via `apiServiceUrl` factory param or `API_SERVICE_URL` env). Non-habits (is_habit=false) stored in MongoDB `habits` collection with is_habit:false; user receives informative message. Valid habits trigger: (1) CREATE Habit node in Neo4j with uuid, sentence, language, is_habit:true, confidence, userID (Keycloak sub), created_at; (2) MERGE Context nodes for each non-empty dimension phrase with HAS_CONTEXT relationships; (3) MERGE BCIOConcept nodes by bcio_concept_id with MAPS_TO relationships from Context. Two users donating similar habits that map to the same concept share one BCIOConcept node. Updated `v1Router.js` to accept and pass `apiServiceUrl` to `createHabitsRouter`. Integration test file `habits.donate.test.js` covers: auth enforcement, validation, non-habit MongoDB path, 201 response, Neo4j Habit node creation, and shared BCIOConcept node verification. All 196 tests pass.
- Files changed: app/routes/habitsRouter.js (add donate route, import randomUUID), app/routes/v1Router.js (add apiServiceUrl param), app/tests/integration/habits.donate.test.js (new), prd.json (US-100 passes=true)
- **Learnings for future iterations:**
  - The habits test mock intercepts ALL fetch calls globally — ensure `realFetch` is stored before overriding and pass through calls to `127.0.0.1` (test server) via `realFetch`; only intercept calls to `mock-api-service` hostname
  - `createHabitsRouter` uses factory injection pattern: `{ db, neo4jRun, apiServiceUrl }` — all three can be injected for testing; `apiServiceUrl` falls back to `API_SERVICE_URL` env then `http://recommender:8000`
  - BCIOConcept sharing: use `MERGE (b:BCIOConcept {bcio_concept_id: $id})` so identical concept IDs from different users are deduplicated in the graph
  - Prettier enforces formatting on new files — run `npx prettier --write <file>` after writing, before committing
---

## 2026-03-20 - US-099
- What was implemented: Created `API-service/data/bcio.owl` — a minimal OWL stub with 32 representative BCIO concepts across TIME, PHYSICAL_SETTING, OTHER_PEOPLE, INTERNAL_STATE, BEHAVIOR, REASONING, and PRIOR_BEHAVIOR dimensions. Created `API-service/routers/map_bcio.py` with POST /api/v1/llm/map-bcio endpoint — request: {uuid, context_phrases: {dimension: [phrase]}}, response: {mappings: [{phrase, dimension, bcio_concept_id, bcio_concept_label, confidence}]}. Parsing via stdlib xml.etree.ElementTree. Embedding via OpenAI text-embedding-3-small (or BAAI/bge-m3 via sentence-transformers). In-memory index cached by SHA-256 of bcio.owl; rebuilt on file change. Cosine similarity via numpy. EMBEDDING_MODEL and BCIO_MIN_CONFIDENCE (default 0.75) env-configurable. Added numpy>=1.26.0 to requirements.txt. Registered router in main.py.
- Files changed: API-service/data/bcio.owl (new), API-service/routers/map_bcio.py (new), API-service/tests/test_map_bcio.py (new), API-service/requirements.txt (add numpy), API-service/main.py (add router), prd.json (US-099 passes=true)
- **Learnings for future iterations:**
  - OWL files are standard XML; stdlib `xml.etree.ElementTree` is sufficient — no rdflib needed. Namespace constants: `{http://www.w3.org/2002/07/owl#}Class`, `{http://www.w3.org/1999/02/22-rdf-syntax-ns#}about`, `{http://www.w3.org/2000/01/rdf-schema#}label`
  - The in-memory embedding index uses a SHA-256 hash of the OWL file bytes to detect changes — compare `_INDEX["hash"]` to current hash before returning cached index
  - Tests mock both `_get_index` (returning a fake index with known unit vectors) and `_embed_texts` (returning matching unit vectors) — cosine similarity between identical unit vectors is 1.0 which is always > 0.75
  - numpy cosine similarity: `np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))` — guard against zero-norm vectors
  - Batch all phrase embeddings in a single `_embed_texts` call to minimise API round-trips
---

## 2026-03-20 - US-098
- What was implemented: Created `API-service/prompts/classify_context.txt` with a BCIO-based 7-dimension context extraction prompt. Created `API-service/routers/classify_context.py` with POST /api/v1/llm/classify-context endpoint — request: {uuid, sentence, language}, response: all 7 context dimensions (TIME, PHYSICAL_SETTING, PRIOR_BEHAVIOR, OTHER_PEOPLE, INTERNAL_STATE, BEHAVIOR, REASONING) as lists of strings. Redis caching uses same key strategy as classify_habit (SHA-256 of sentence||language with prefix `classify_context:`). Updated `API-service/main.py` to include the new router.
- Files changed: API-service/prompts/classify_context.txt (new), API-service/routers/classify_context.py (new), API-service/tests/test_classify_context.py (new), API-service/main.py (add router import + include), prd.json (US-098 passes=true)
- **Learnings for future iterations:**
  - The classify_context router follows the exact same structure as classify_habit — Redis graceful fallback, SHA-256 cache key, Pydantic models, `_parse_llm_response` helper that returns empty dim lists on LLM JSON errors
  - `_REDIS_URL` and `_REDIS_TTL` env vars are shared by all routers but each router has its own `_redis` module-level singleton — they do not share the connection object
  - For the response model, Pydantic allows `**dims` dict unpacking directly in the constructor when field names match dict keys
  - The 7 BCIO dimensions to always include: TIME, PHYSICAL_SETTING, PRIOR_BEHAVIOR, OTHER_PEOPLE, INTERNAL_STATE, BEHAVIOR, REASONING
---

## 2026-03-18 - US-094
- What was implemented: Changed CFBundleName from 'hhh' to 'Health Habit Hub' in mobile/ios/Runner/Info.plist (CFBundleDisplayName was already 'Health Habit Hub'). Changed android:label from 'hhh' to 'Health Habit Hub' in mobile/android/app/src/main/AndroidManifest.xml. pubspec.yaml `name: hhh` left unchanged (Dart package identifier).
- Files changed: mobile/ios/Runner/Info.plist (CFBundleName), mobile/android/app/src/main/AndroidManifest.xml (android:label), prd.json (US-094 passes=true)
- **Learnings for future iterations:**
  - iOS has two bundle name fields: CFBundleDisplayName (shown on home screen, was already 'Health Habit Hub') and CFBundleName (short name used in alerts/menus, was 'hhh' — needed updating)
  - Android app label in AndroidManifest.xml `<application android:label=...>` is the display name shown to users; pubspec.yaml `name:` is the Dart package identifier and must not be changed
---

## 2026-03-18 - US-093
- What was implemented: Verified that the SdkRoot fix was already fully applied by commit 3af6687. The project-level Profile XCBuildConfiguration (249021D3217E4FDB00AE95B9) already contains `SDKROOT = iphoneos` and `SUPPORTED_PLATFORMS = iphoneos`. Profile.xcconfig is wired as `baseConfigurationReference` in the Runner target's Profile config (249021D4217E4FDB00AE95B9). `flutter analyze` passes with no issues.
- Files changed: prd.json (US-093 passes=true)
- **Learnings for future iterations:**
  - `native_assets` is a Flutter-generated target not present in project.pbxproj — the fix is to ensure the project-level Profile XCBuildConfiguration has SDKROOT and SUPPORTED_PLATFORMS, which was already done in commit 3af6687
  - When the acceptance criteria says "add X if missing" and X is already present, mark as complete after verifying flutter analyze passes
---

## 2026-03-18 - US-092
- What was implemented: Uncommented `platform :ios, '13.0'` in mobile/ios/Podfile (removes the 'Automatically assigning platform' warning). Created mobile/ios/Flutter/Profile.xcconfig with two lines matching Debug.xcconfig/Release.xcconfig pattern: `#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.profile.xcconfig"` and `#include "Generated.xcconfig"` (removes the missing xcconfig warning for Profile build configuration).
- Files changed: mobile/ios/Podfile (uncomment platform line), mobile/ios/Flutter/Profile.xcconfig (new), prd.json (US-092 passes=true)
- **Learnings for future iterations:**
  - Profile.xcconfig follows the same two-line pattern as Debug.xcconfig and Release.xcconfig but uses `.profile.xcconfig` as the Pods include filename suffix
  - The platform :ios line in Podfile is commented out by default in Flutter-generated projects — always uncomment it to avoid CocoaPods auto-assignment warnings
  - `flutter analyze` (not pod install) is the CI check available here; pod install requires a Mac with Xcode and CocoaPods installed
---

## 2026-03-17 - US-089
- What was implemented: Created `mobile/lib/providers/theme_provider.dart` — a `ThemeModeNotifier` (StateNotifier) that persists the selected ThemeMode to flutter_secure_storage under key `theme_mode`. Added `_buildDarkTheme()` to main.dart: scaffoldBackgroundColor #1a1a1a, surface #2a2a2a, onSurface #f0f0f0, primary remains #45b700, charcoal navbar (#454446) shared with light theme. Updated `HhhApp` to watch `themeModeProvider` and pass `darkTheme: _buildDarkTheme()`, `themeMode: themeMode` alongside the existing light theme. Added `_AppearanceSection` widget to `profile_screen.dart` (always visible at bottom, below the main content area): a `SegmentedButton<ThemeMode>` with Light/System/Dark options that reads/writes via `themeModeProvider`.
- Files changed: mobile/lib/providers/theme_provider.dart (new), mobile/lib/main.dart (import theme_provider, add _buildDarkTheme, update HhhApp), mobile/lib/screens/profile_screen.dart (import theme_provider, restructure body to Column, add _AppearanceSection), prd.json (US-089 passes=true)
- **Learnings for future iterations:**
  - `StateNotifierProvider<ThemeModeNotifier, ThemeMode>` is the Riverpod pattern for mutable state with side effects (storage); `ref.read(provider.notifier).setMode(mode)` calls the mutating method
  - Dark theme uses `ColorScheme.fromSeed(brightness: Brightness.dark)` to invert text colors automatically; only `scaffoldBackgroundColor` needs explicit override to #1a1a1a
  - `SegmentedButton<ThemeMode>` needs `selected: {themeMode}` (a Set) and `onSelectionChanged: (Set<ThemeMode> selected) => notifier.setMode(selected.first)`
  - Wrapping the existing Stack in an Expanded + adding `_AppearanceSection` below it (inside a Column) keeps the Stack behavior intact while adding a persistent bottom section
  - All 41 existing widget tests pass without modification
---

## 2026-03-17 - US-088
- What was implemented: Added `google_fonts: ^6.2.1` to pubspec.yaml. Created `_buildLightTheme()` in main.dart with full HabShare brand design system: Figtree font via `GoogleFonts.figtreeTextTheme()`, ColorScheme with primary #45b700 green and secondary #e679ab pink, scaffoldBackgroundColor white, surface #dadada. Set component themes: AppBarTheme (charcoal #454446 background, white text/icons), NavigationBarTheme (charcoal background, green #45b700 indicator pill, white selected/unselected label+icon), ElevatedButtonTheme + FilledButtonTheme (green bg, white text, bold Figtree, 20px borderRadius), OutlinedButtonTheme (green border, green text, 20px borderRadius), TextButtonTheme (green text), CardTheme (surface #dadada, borderRadius 20px, 2px green border, elevation 4 with shadow rgba(0,0,0,0.25)), ChipTheme (#4cd49e background, white label text for status/category chips).
- Files changed: mobile/pubspec.yaml (add google_fonts), mobile/pubspec.lock (regenerated), mobile/lib/main.dart (import google_fonts, add brand color consts, add _buildLightTheme(), use it in HhhApp), prd.json (US-088 passes=true)
- **Learnings for future iterations:**
  - The Flutter `Chip` widget does not support gradient backgrounds natively — approximate the #4cd49e → #51d075 gradient with a solid #4cd49e color in ChipThemeData
  - `GoogleFonts.figtreeTextTheme(base.textTheme)` applies Figtree to all TextTheme styles; use `GoogleFonts.figtree(...)` for button textStyle in component themes for consistency
  - `NavigationBarThemeData.iconTheme` and `labelTextStyle` use `WidgetStateProperty.resolveWith` to differentiate selected vs unselected states
  - `CardThemeData` in M3: use `elevation: 4` + `shadowColor: Color(0x40000000)` to approximate `boxShadow: 0px 4px 4px rgba(0,0,0,0.25)`; the green border goes in `shape: RoundedRectangleBorder(side: BorderSide(color: _kPrimary, width: 2))`
  - All 41 existing widget tests pass without modification after the theme change
---

## 2026-03-17 - US-087
- What was implemented: Repurposed `login_screen.dart` as an admin-only PKCE screen — removed all username/password TextFields, now shows a single "Login with Admin Account" ElevatedButton that triggers `authService.login()` (PKCE browser flow) and navigates to `/donate` on success. Changed `initialLocation` in `main.dart` from `/login` to `/onboarding/welcome`. Updated redirect guard: if `isOnboardingComplete()` is true AND location is `/onboarding/welcome` or `/login`, redirect to `/donate` (was previously redirecting to `/login`). Simplified `/login` route builder to `const LoginScreen()` (no more query param handling). Added hidden "Admin login" TextButton to `_WelcomePage` in `welcome_screen.dart` (subtle grey styling, small font) that navigates to `/login` via `context.push`. Added `onAdminLogin` callback to `_WelcomePage`. Updated `login_screen_test.dart` to test the new admin-only screen (no TextFields, has admin button and icon, loading state, error state).
- Files changed: mobile/lib/screens/login_screen.dart (repurpose), mobile/lib/main.dart (initialLocation, redirect, route builder), mobile/lib/screens/onboarding/welcome_screen.dart (add onAdminLogin + TextButton), mobile/test/widget/login_screen_test.dart (rewrite), prd.json (US-087 passes=true)
- **Learnings for future iterations:**
  - When both AppBar title and a heading Text have identical text, `findsOneWidget` fails — use `findsAtLeastNWidgets(1)` or `findsNWidgets(2)` in the test
  - `context.push('/login')` (not `context.go`) for the admin login button so the back button works from the admin screen
  - The `isOnboardingComplete()` helper from welcome_screen.dart is already imported in main.dart — use it for the returning user redirect instead of importing FlutterSecureStorage again
  - The redirect must cover both `/onboarding/welcome` AND `/login` paths to avoid returning users hitting the admin screen accidentally
---

## 2026-03-17 - US-086
- What was implemented: Created `mobile/lib/screens/onboarding/restore_screen.dart` — a screen where a returning user enters their 36-word recovery passphrase (text field, space-separated). On submit: splits input into words, calls `credentialsFromPassphrase()` (imported from passphrase_screen.dart) to decode username UUID and password hex. Calls Keycloak's direct-grant endpoint (`POST ${AppConfig.keycloakUrl}/realms/hhh/protocol/openid-connect/token`) with `grant_type=password`, `client_id=hhh-flutter`, `username`, `password` (content-type: application/x-www-form-urlencoded). On success (HTTP 200): stores access_token, refresh_token, username, password, and onboarding_complete=true in flutter_secure_storage, then navigates to `/donate`. On failure: shows "Invalid passphrase or account not found". Added `/onboarding/restore` route to main.dart. Added "Restore account on this device" OutlinedButton to profile_screen.dart (shown below the profile summary card, navigates via `context.push('/onboarding/restore')`). Added `onRestore` callback to `_WelcomePage` in welcome_screen.dart and a "Restore existing account" TextButton below "Get Started" that navigates to `/onboarding/restore`.
- Files changed: mobile/lib/screens/onboarding/restore_screen.dart (new), mobile/lib/main.dart (add import + route), mobile/lib/screens/profile_screen.dart (add go_router import + restore button), mobile/lib/screens/onboarding/welcome_screen.dart (add onRestore callback + TextButton), prd.json (US-086 passes=true)
- **Learnings for future iterations:**
  - Keycloak direct-grant token endpoint: `POST ${keycloakUrl}/realms/hhh/protocol/openid-connect/token` with Content-Type: application/x-www-form-urlencoded, fields: grant_type=password, client_id=hhh-flutter, username, password
  - `credentialsFromPassphrase()` is a module-level function in passphrase_screen.dart — import it directly into the restore screen
  - The PRD acceptance criteria says "12-word passphrase" but US-085 implemented 36 words — use 36 words in the restore screen text
  - `context.push()` (not `context.go()`) for the restore route from profile so the back button works correctly
---

## 2026-03-17 - US-085
- What was implemented: Created `mobile/lib/screens/onboarding/passphrase_screen.dart` — on arrival calls `POST /api/v1/onboard`, shows a loading spinner while waiting. On success, derives a 36-word BIP39-style mnemonic: first 12 words encode the UUID username (16 bytes), last 24 words encode the password (32 bytes), using 11-bit BIP39 encoding. Displays words in a 3-column numbered Table grid with an amber warning banner ("Write these words down…"). Shows a 'Copy to clipboard' OutlinedButton and a 'I have written it down' CheckboxListTile. The 'Continue' FilledButton is disabled until the checkbox is checked. On Continue: stores username, password, access_token, refresh_token in flutter_secure_storage, sets `onboarding_complete=true`, and navigates to `/donate`. On error: shows error message with a Retry button. Created `mobile/lib/utils/bip39_wordlist.dart` with exactly 2048 words (1916 canonical BIP39 English words + 132 padding words) as `const List<String> kBip39Words`. Updated `main.dart`: replaced the placeholder `/onboarding/passphrase` route with `PassphraseScreen`. Exported `credentialsFromPassphrase()` helper for US-086 restore flow.
- Files changed: mobile/lib/screens/onboarding/passphrase_screen.dart (new), mobile/lib/utils/bip39_wordlist.dart (new), mobile/lib/main.dart (replace placeholder route), prd.json (US-085 passes=true)
- **Learnings for future iterations:**
  - `dart:typed_data` import is redundant when `package:flutter/services.dart` is already imported — remove it to silence `unnecessary_import` lint
  - BIP39 11-bit encoding of UUID (128 bits → 12 words) + password (256 bits → 24 words) = 36 words total — both have enough zero-padding that all indices fit in 0–2047
  - `Table` widget requires all `TableRow.children` to have the same count — use `SizedBox.shrink()` as placeholder for empty cells in the last row
  - The `passphraseFromCredentials` and `credentialsFromPassphrase` functions are module-level in passphrase_screen.dart and can be imported directly by US-086 restore screen
  - Secure storage keys for credentials: `username`, `password`, `access_token`, `refresh_token` (plus `onboarding_complete` from welcome_screen.dart)
---

## 2026-03-17 - US-084
- What was implemented: Created `mobile/lib/screens/onboarding/welcome_screen.dart` — a full-screen onboarding flow with 4 pages: (0) welcome page with HabShare icon/logo, one-paragraph study description, and 'Get Started' button; (1–3) three PageView walkthrough steps (Donate a Habit, Explore & Annotate, Get Recommendations), each with an icon, title, and 2–3 sentence description. Each walkthrough page has a 'Skip' TextButton (top-right, pages 1–2 only) that jumps directly to the last page, and a 'Next'/'Continue' FilledButton at the bottom. 'Continue' on the last page navigates to `/onboarding/passphrase`. Animated page-indicator dots shown during walkthrough pages. Added `isOnboardingComplete()` helper using `flutter_secure_storage` keyed on `onboarding_complete`. Updated `main.dart`: imported welcome_screen.dart; added `/onboarding/welcome` and `/onboarding/passphrase` (placeholder) routes; added onboarding bypass redirect — if `onboarding_complete == 'true'` in secure storage, redirect to `/login` instead.
- Files changed: mobile/lib/screens/onboarding/welcome_screen.dart (new), mobile/lib/main.dart (add import, routes, redirect logic), prd.json (US-084 passes=true)
- **Learnings for future iterations:**
  - Dart `static const List<_PrivateClass>` in a `State` class works fine as long as `_PrivateClass` has a `const` constructor and is declared at file scope in the same file
  - GoRouter redirect guard is `async` — secure storage reads fit naturally here without needing separate provider state
  - A `_WalkthroughStep` with `const` constructor allows compile-time constant list items (no runtime allocation)
  - The placeholder `/onboarding/passphrase` route (inline Scaffold) is enough for `flutter analyze` to pass; US-085 will replace it with the real screen
---

## 2026-03-17 - US-083
- What was implemented: Added `POST /api/v1/onboard` as a public (unauthenticated) Express route. Created `app/routes/onboardRouter.js` with: (1) separate IP-based rate limiter (5 req/hour via `ipKeyGenerator`); (2) creates Keycloak user via Admin REST API using `randomUUID()` for username and `randomBytes(32).toString('hex')` for password; (3) performs direct-grant token exchange; (4) returns 201 with `access_token`, `refresh_token`, `expires_in`, `username`, `password`; (5) returns 502 if Keycloak is unreachable. Registered route in `v1Router.js` before the general rate limiter and authenticate middleware. Added 3 unit tests covering the 201 success case.
- Files changed: app/routes/onboardRouter.js (new), app/routes/v1Router.js (add import + route), app/tests/unit/onboard.route.test.js (new), prd.json (US-083 passes=true)
- **Learnings for future iterations:**
  - `express-rate-limit` throws `ERR_ERL_KEY_GEN_IPV6` if you use `req.ip` directly in `keyGenerator` — always use the exported `ipKeyGenerator(req)` helper instead
  - When unit-testing an Express route that uses `global.fetch` for external calls, the test's own `fetch()` calls to the local server are also intercepted — save `global.fetch` as `originalFetch` and pass through calls to `127.0.0.1` to avoid the test calling the mock instead of the actual server
  - Public routes must be registered in `v1Router.js` BEFORE the `router.use(limiter)` and `router.use(authenticate)` lines to avoid requiring a JWT
  - The `onboardRouter` accepts an optional `keycloak` dependency for testing; if `keycloak.getAdminToken` is present it's used, otherwise falls back to `client_credentials` fetch
---

## 2026-03-17 - US-082
- What was implemented: Replaced three hardcoded `admin` credentials in mongo-express service of docker-compose.yml with `${MONGO_USER:-admin}` and `${MONGO_PASSWORD:-admin}` env var references (consistent with docker-compose.prod.yml and the mongo service).
- Files changed: docker-compose.yml (parameterize ME_CONFIG_MONGODB_ADMINUSERNAME, ME_CONFIG_MONGODB_ADMINPASSWORD, ME_CONFIG_MONGODB_URL), prd.json (US-082 passes=true)
- **Learnings for future iterations:**
  - ME_CONFIG_BASICAUTH_USERNAME/PASSWORD were already using env vars; only the MONGODB_ vars were hardcoded
  - `docker compose -f docker-compose.yml config` validates YAML including variable interpolation
---

## 2026-03-17 - US-081
- What was implemented: Added missing .gitignore entries: *.log, dist/, tmp/, .cache/, .idea/, *.swp, *.swo, *~, Thumbs.db under a new "Generated and IDE files" comment section.
- Files changed: .gitignore (add 9 new patterns), prd.json (US-081 passes=true)
- **Learnings for future iterations:**
  - No tracked files matched any new pattern — safe to add without risk of hiding existing files
  - `git ls-files | grep -E` is the right way to verify before adding patterns that could affect tracked files
---

## 2026-03-17 - US-080
- What was implemented: Changed Keycloak production command from `start-dev --import-realm` to `start --import-realm` in docker-compose.prod.yml. Added `KC_HOSTNAME_STRICT=false` environment variable. `KC_PROXY=edge` was already present.
- Files changed: docker-compose.prod.yml (change command, add KC_HOSTNAME_STRICT=false), prd.json (US-080 passes=true)
- **Learnings for future iterations:**
  - `start-dev` is Keycloak's development mode (no TLS required, relaxed security); production must use `start`
  - `KC_HOSTNAME_STRICT=false` is needed when running behind a reverse proxy (Traefik) without a fixed hostname configured in Keycloak
  - `KC_PROXY=edge` was already set in docker-compose.prod.yml, satisfying the second acceptance criterion
---

## 2026-03-17 - US-079
- What was implemented: Added `app.set('trust proxy', 1)` to app/app.js immediately after `const app = express()`. This ensures the Express rate limiter reads the real client IP from X-Forwarded-For header set by Traefik, instead of Traefik's internal IP.
- Files changed: app/app.js (add trust proxy setting), prd.json (US-079 passes=true)
- **Learnings for future iterations:**
  - `app.set('trust proxy', 1)` must come before any rate-limiting middleware (it's applied to the express app itself)
  - The rate limiter in this project is applied in app/routes/v1Router.js (not in app.js directly), but trust proxy setting on the app object applies globally regardless
  - 186 tests pass after this change; no test changes needed
---

## 2026-03-17 - US-078
- What was implemented: Added `set -euo pipefail` as line 2 of backup-service/backup.sh. All environment variables were already initialized with `${VAR:-default}` syntax (BACKUP_RETENTION_DAYS, ALERT_WEBHOOK_URL, BACKUP_EMAIL, KEYCLOAK_HOST, KEYCLOAK_ADMIN, KEYCLOAK_ADMIN_PASSWORD, MONGO_USER, MONGO_PASSWORD, MAIL_USER, MAIL_PASS, MAIL_FROM), so no additional changes were needed to avoid -u false errors.
- Files changed: backup-service/backup.sh (add set -euo pipefail), prd.json (US-078 passes=true)
- **Learnings for future iterations:**
  - backup.sh already uses `${VAR:-}` pattern throughout — adding `set -euo pipefail` required only inserting line 2
  - `bash -n` only validates syntax (not execution); the script passes this check after the change
  - All failure paths in backup.sh are wrapped in `if ... then ... else ... fi` blocks, which is compatible with `set -e`
---

## 2026-03-17 - US-077
- What was implemented: Added non-root user to API-service/Dockerfile. Added `RUN addgroup --system appgroup && adduser --system --ingroup appgroup appuser` after COPY, then `USER appuser` before EXPOSE and CMD.
- Files changed: API-service/Dockerfile (add addgroup/adduser + USER directive), prd.json (US-077 passes=true)
- **Learnings for future iterations:**
  - python:3.11-slim uses Debian-based commands: `addgroup --system` and `adduser --system --ingroup` (not `adduser -S -G` which is Alpine syntax)
  - Place USER directive after COPY so files are copied as root (needed for pip install), then switch to non-root user before CMD
---

## 2026-03-17 - US-074
- What was implemented: Added Docker healthchecks to app, mongo, and neo4j services in both docker-compose.yml and docker-compose.prod.yml. Added `apk add --no-cache curl` to app/Dockerfile so curl is available for the healthcheck. Health checks: app uses `curl -f http://localhost:3000/api/v1/health` (interval 30s, timeout 10s, retries 5, start_period 30s); mongo uses `mongosh --eval 'db.runCommand({ping:1})'` (same timing, start_period 30s); neo4j uses `wget -q -O /dev/null http://localhost:7474` (same timing, start_period 60s — longer to allow neo4j to start).
- Files changed: app/Dockerfile (add curl), docker-compose.yml (healthchecks on app, mongo, neo4j), docker-compose.prod.yml (same healthchecks), prd.json (US-074 passes=true)
- **Learnings for future iterations:**
  - node:22-alpine does NOT include curl by default — must `apk add --no-cache curl` in Dockerfile to use curl in Docker healthchecks
  - neo4j:5 image includes wget (not curl) — use wget for neo4j healthcheck
  - mongo:7.0 image includes mongosh — use `mongosh --eval 'db.runCommand({ping:1})'` for mongo healthcheck
  - start_period should be longer for slow-starting services: neo4j needs 60s, mongo/app need 30s
---

## 2026-03-17 - US-073
- What was implemented: Pinned all `:latest` Docker image tags to explicit versions in docker-compose.yml, docker-compose.prod.yml, and backup-service/Dockerfile.
- Files changed: docker-compose.yml (mongo-express:1.0, libretranslate/libretranslate:1.5), docker-compose.prod.yml (mongo:7.0, mongo-express:1.0, libretranslate/libretranslate:1.5), backup-service/Dockerfile (alpine:3.21), prd.json (US-073 passes=true)
- **Learnings for future iterations:**
  - Pinned versions: mongo:7.0, mongo-express:1.0, libretranslate/libretranslate:1.5, alpine:3.21
  - `docker compose -f <file> config` validates YAML and reports missing env vars as warnings (not errors)
---

## 2026-03-17 - US-071
- What was implemented: Created `.env.example` with all variable names from `.env` but with `CHANGE_THIS_*` placeholder values (no real credentials). Added `!.env.example` negation rule to `.gitignore` so the example file is tracked despite the `.env*` wildcard. The `.env` file itself was already removed from git tracking in a prior commit (1b62aea). README.md already referenced `cp .env.example .env` as the setup step.
- Files changed: .env.example (new), .gitignore (add !.env.example negation), prd.json (US-071 passes=true)
- **Learnings for future iterations:**
  - When `.gitignore` has `.env*`, a `.env.example` file is also ignored — add `!.env.example` negation rule to allow tracking the example file
  - `.env` was already removed from git tracking in commit 1b62aea; `git ls-files .env` returns empty confirming it's untracked
  - IMPORTANT: All real credentials in .env history should be rotated (ADMIN_PASSWORD, DB_PASSWORD, NEO4J_PASSWORD, MONGO_PASSWORD, MONGO_EXPRESS_PASSWORD, MAIL_USER, MAIL_PASS, RECAPTCHA keys, TRAEFIK_DASHBOARD_AUTH) — rotation is out of scope for this story
---

## 2026-03-17 - US-070
- What was implemented: Created `AUDIT.md` at repo root documenting 12 distinct code quality issues found across the repository. Added 12 new user stories (US-071 through US-082) to prd.json, one per issue found. Set US-070 passes=true.
- Files changed: AUDIT.md (new), prd.json (US-070 passes=true, add US-071 through US-082)
- Issues documented: (1) .env committed with real credentials, (2) Hardcoded production URLs in Flutter services, (3) Docker images using :latest, (4) Missing health checks on app/mongo/neo4j, (5) Dead npm deps `fs` and `path`, (6) TODO comments in SparqlDatabase.js, (7) API-service Dockerfile runs as root, (8) backup.sh missing set -euo pipefail, (9) Rate limiter missing trust proxy config, (10) Keycloak prod uses start-dev, (11) .gitignore missing common patterns, (12) mongo-express hardcoded credentials
- **Learnings for future iterations:**
  - Audit stories should be focused enough to complete in one iteration — each US-071..US-082 is a single, minimal fix
  - AUDIT.md follows format: ID, file path, problem description, why it violates best practice, suggested fix
---

## 2026-03-17 - US-069
- What was implemented: (1) Added `docker-build` CI job to `.github/workflows/ci.yml` — runs `docker build` for all four custom images (./app, ./API-service, ./fuseki, ./backup-service) in sequence; job fails on any build error. (2) Added `docker-build` to the `needs` list of the `ci-passed` gate job. (3) In `docker-compose.yml`: removed `user: root` from the app service (Dockerfile already sets USER node). (4) Removed `app` from mongo's `depends_on` (mongo has no dependency on the app service). (5) Removed `app` from translate's `depends_on` (translate has no dependency on the app service). (6) Replaced hardcoded `MONGO_INITDB_ROOT_USERNAME=admin` and `MONGO_INITDB_ROOT_PASSWORD=admin` with `${MONGO_USER:-admin}` and `${MONGO_PASSWORD:-admin}` to use env vars consistently with docker-compose.prod.yml.
- Files changed: .github/workflows/ci.yml (add docker-build job + gate needs), docker-compose.yml (remove user:root, fix depends_on, parameterize mongo creds), prd.json (US-069 passes=true)
- **Learnings for future iterations:**
  - `docker build <context>` in CI doesn't need `--tag` — the build itself is the validation; add `--no-cache` if you want to test from scratch (but omit for speed in CI)
  - Service `depends_on` in docker-compose should only list services the container truly needs to start (not services that need it) — circular or incorrect depends_on can prevent healthy startups
  - `user: root` in docker-compose overrides the USER directive in the Dockerfile — removing it defers to the Dockerfile's USER directive (least privilege)
---

## 2026-03-16 - US-068
- What was implemented: (1) Split the single multi-statement MERGE cypher-shell call in the "Seed minimal test data" CI step into 8 separate cypher-shell calls (one per MERGE/MATCH), eliminating shell-quoting and multi-statement parsing issues. Relationship merges use MATCH to find existing nodes since variables don't persist across separate cypher-shell invocations. (2) Fixed `assert_cypher_count` in `tests/ontology/test-ontology.sh`: replaced `grep -cE '^[0-9]'` (which counted lines starting with a digit — always 1 for a COUNT query, even if count=0) with `tail -n +2 | head -1 | tr -d '[:space:]'` to extract the actual integer value from the second line of cypher-shell --format plain output, with a regex guard to default to 0 for non-numeric output.
- Files changed: .github/workflows/ci.yml (split seed step into 8 separate cypher-shell calls), tests/ontology/test-ontology.sh (fix assert_cypher_count), prd.json (US-068 passes=true)
- **Learnings for future iterations:**
  - cypher-shell `--format plain` output: first line is the column name, second line is the value — use `tail -n +2 | head -1` to extract the value, not `grep -cE '^[0-9]'` which always returns 1 for COUNT queries
  - When splitting multi-statement Cypher into separate cypher-shell calls, use MATCH (not MERGE) to find previously created nodes when building relationships, since variables don't persist across invocations
  - n10s.graphconfig.init + multi-label MERGE can conflict; splitting into separate calls avoids any constraint/label interaction issues
---

## 2026-03-16 - US-067
- What was implemented: Updated GitHub Actions in both CI and CD workflows to use Node.js 24 runtime. (1) `actions/checkout@v4` → `actions/checkout@v6` in ci.yml (8 occurrences) and deploy.yml (1 occurrence). (2) `actions/setup-node@v4` → `actions/setup-node@v6` in ci.yml (4 occurrences). (3) `actions/upload-artifact@v4` → `actions/upload-artifact@v6` in ci.yml (2 occurrences). `softprops/action-gh-release@v2` and `appleboy/ssh-action@v1` remain at their latest major versions (no newer major versions exist).
- Files changed: .github/workflows/ci.yml (checkout v6, setup-node v6, upload-artifact v6), .github/workflows/deploy.yml (checkout v6), prd.json (US-067 passes=true)
- **Learnings for future iterations:**
  - Node.js 24 action versions: `actions/checkout@v6`, `actions/setup-node@v6`, `actions/upload-artifact@v6`
  - `softprops/action-gh-release` latest is v2.5.0 (still @v2); `appleboy/ssh-action` latest is v1.2.5 (still @v1) — no major version bump needed
  - GitHub deprecated Node.js 20 actions in Sept 2025; forced migration deadline is June 2, 2026
---

## 2026-03-16 - US-066
- What was implemented: (1) Removed fuseki service container from `.github/workflows/ci.yml` ontology-test job. (2) Removed 'Load ontology into Fuseki' step (curl POST that was failing with exit code 22). (3) Removed 'Waiting for Fuseki...' from the wait-for-services step. (4) Removed FUSEKI_URL and FUSEKI_DATASET env vars from the 'Run ontology test suite' step. (5) In `tests/ontology/test-ontology.sh`: removed `run_sparql` helper, `assert_sparql_count` helper, Test 4 ('All hhh:Habit instances have rdfs:label in Fuseki'), and Test 5 ('Fuseki has >= 100 owl:Class entries'). Remaining 3 Cypher-based tests (group retrievability, donor group integrity, orphaned habit nodes) are unchanged.
- Files changed: .github/workflows/ci.yml (remove fuseki service, step, wait line, env vars), tests/ontology/test-ontology.sh (remove SPARQL helpers and tests 4+5), prd.json (US-066 passes=true)
- **Learnings for future iterations:**
  - The ontology-test CI job now only needs Neo4j — Fuseki was only used for SPARQL-based tests which have been removed
  - When removing a service from CI, also check: service container block, wait-for-services step, any step that loads data into that service, and env vars in subsequent steps
  - test-ontology.sh now has exactly 3 Cypher-based tests (6 assertions total): group nodes exist (4), no donors missing group (1), no orphaned habits (1)
---

## 2026-03-16 - US-065
- What was implemented: (1) Deleted `mobile/test/widget_test.dart` — the stale auto-generated counter smoke test that caused `flutter test` to fail with no relevant tests. (2) Replaced the broken wget-based cypher-shell install in the ontology-test CI job with installation via the official Neo4j APT repository: add GPG key with gpg --dearmor, add deb source to /etc/apt/sources.list.d/neo4j.list, run `sudo apt-get update -qq && sudo apt-get install -y cypher-shell`. 186 backend tests still pass.
- Files changed: mobile/test/widget_test.dart (deleted), .github/workflows/ci.yml (cypher-shell install step), prd.json (US-065 passes=true)
- **Learnings for future iterations:**
  - Neo4j APT repo GPG key URL: `https://debian.neo4j.com/neotechnology.gpg.key`; deb source: `deb [signed-by=/usr/share/keyrings/neo4j.gpg] https://debian.neo4j.com stable latest`
  - `cypher-shell-5.latest.zip` URL from dist.neo4j.org returns HTTP errors — always use the APT repo for CI installs
  - Auto-generated Flutter `widget_test.dart` (counter app test) must be deleted when the app template is replaced; it will always fail on a real app
---

## 2026-03-16 - US-064
- What was implemented: Fixed all failing GitHub Actions CI jobs. All required changes were already present in the workflow files (applied by previous iterations or the initial setup). Verified: (1) `--audit-level=critical` in backend-security job; (2) fuseki service container absent from backend-integration job; (3) ENABLE_INTEGRATION env var absent from backend-integration job; (4) ontology-test uses `stain/jena-fuseki:5.1.0`; (5) flutter-analyze, flutter-test, flutter-build-web all use `channel: stable` with no pinned flutter-version; (6) deploy.yml release job uses `channel: "stable"` with no pinned flutter-version. 186 backend tests pass locally.
- Files changed: prd.json (US-064 passes=true), .github/workflows/ci.yml (staged, no new edits needed), .github/workflows/deploy.yml (staged, no new edits needed)
- **Learnings for future iterations:**
  - When all CI changes are already staged/applied, the iteration only needs to confirm correctness, run local tests, and commit+update prd.json
  - `git diff --stat HEAD` is the fastest way to confirm what changes are pending across all modified files
---

## 2026-03-16 - US-063
- What was implemented: `CHANGELOG.md` at repo root following Keep a Changelog 1.1.0 format with SemVer. Version 1.0.0 entry dated 2026-03-16 with: Added sections summarising all 12 phases (branch consolidation, Keycloak, API versioning, ontology, Flutter app, study admin panel, test suite, deployment scripts, security/backup, end-user docs, technical docs, API reference/DX); Changed (EJS views retired, backup retention extended to 30 days); Fixed (G3/G4 ontology indistinguishability). Updated `scripts/deploy-full.sh` `check_version_bump()` warning message to exactly: `WARNING: Version not bumped since last deploy (CHANGELOG.md v<version> has no matching git tag)`. Created `scripts/version-check.js` (ESM Node.js): reads CHANGELOG.md, extracts first semver with regex, checks `git tag --list` for `v<version>` or `<version>`, prints the same WARNING and exits 1 if no match (exits 0 if matched or file/parse error). Added `"version-check": "node ../scripts/version-check.js"` to `app/package.json` scripts.
- Files changed: CHANGELOG.md (new), scripts/deploy-full.sh (update warning message), scripts/version-check.js (new), app/package.json (add version-check script), prd.json (US-063 passes=true)
- **Learnings for future iterations:**
  - `npm run version-check` is run from `app/` directory; scripts at `../scripts/` must use `import.meta.url` + `fileURLToPath` to resolve the repo-root path correctly in ESM
  - `git tag --list "<pattern>"` returns empty string (not an error) when no tag matches — use `.trim()` and check for truthy string
  - CHANGELOG.md Keep-a-Changelog format: versions under `## [1.0.0] - YYYY-MM-DD` headers; `[Unreleased]` section at top; link definitions at bottom
---

## 2026-03-16 - US-062
- What was implemented: `docs/guides/developer-onboarding.md` — single-document guide from zero to working local dev environment. Sections: (1) Required tools table (Flutter 3.22+, Docker 24+, Docker Compose 2.20+, Node.js 20+, npm 10+, Git 2.40+) with verify commands and flutter doctor expected output. (2) Clone + branch setup (git checkout ralph/hhh-platform-unified). (3) stack.env template provided inline (all vars with safe local defaults, note about not committing .local file). (4) docker-compose up with expected healthy JSON output from /api/v1/health and docker compose ps table. (5) Flutter in Chrome (flutter run -d chrome with --dart-define flags for API_BASE_URL + KEYCLOAK_URL/REALM/CLIENT_ID). (6) Flutter in Android emulator (10.0.2.2 host mapping note, flutter emulators launch). (7) Run all tests: npm test (186 tests), flutter test (42 tests), test-ontology.sh (5 PASS lines). (8) Common pitfalls: port conflicts (lsof -i :PORT + kill), Keycloak not ready (logs tail), Flutter web CORS (CORS_ORIGIN var + restart app), realm not imported (manual kc.sh import), Flutter deps out of date (flutter pub get). (9) 8-point verify checklist with copy-pasteable commands and expected outputs. Further reading links to architecture.md, api/openapi.yaml, data-model.md, runbook.md. Placeholder screenshots: flutter-web-chrome.png and npm-test-passing.png.
- Files changed: docs/guides/developer-onboarding.md (new), docs/assets/screenshots/developer/flutter-web-chrome.png (new), docs/assets/screenshots/developer/npm-test-passing.png (new), prd.json (US-062 passes=true)
- **Learnings for future iterations:**
  - Android emulator host mapping: use `10.0.2.2` (not `localhost`) for `API_BASE_URL` and `KEYCLOAK_URL` when running Flutter on Android emulator
  - flutter run -d chrome requires `--dart-define=API_BASE_URL=...` to point at local backend; without it Flutter defaults to production URLs
  - stack.env.local is gitignored — the onboarding guide should provide a complete inline template (all vars, all sections) rather than just a diff of the template
---

## 2026-03-15 - US-061
- What was implemented: Created `docs/runbook.md` — comprehensive operations runbook with 11 sections: (1) Prerequisites (server spec table, required software versions with expected output, open ports table with security note). (2) First-time setup: clone repo, create stack.env from template, `docker-compose up -d`, verify health via `/api/v1/health`, import Keycloak realm. (3) Per-service updates: annotated usage of deploy-backend.sh / deploy-flutter-web.sh / deploy-recommender.sh / deploy-keycloak.sh with expected output. (4) Full-stack deploy: `scripts/deploy-full.sh` with abridged expected output and `--dry-run` mode. (5) Rollback: git checkout prev-tag + full redeploy; single-service rollback pattern. (6) Backup verification: list archives via `docker exec h3-backup`, inspect backup.log, verify archive integrity with `tar -tzf`. (7) Restore from backup: `scripts/restore.sh <timestamp>` with full expected interaction transcript. (8) Rotating Keycloak secrets: admin password (env var + container restart), hhh-backend client secret (UI steps), JWKS URL update. (9) Adding admin user: via Keycloak admin console (step-by-step UI) and via HHH Admin API (`POST /api/v1/admin/participants`). (10) Checking service health: curl health endpoint, docker ps, docker logs, automated polling loop. (11) Troubleshooting: Keycloak 401/JWKS URL misconfigured (diagnosis + fix), Neo4j connection refused (logs + cypher-shell + restart), Flutter web blank page CORS (curl OPTIONS + CORS_ORIGIN env var), Recommender service unreachable (container ps + RECOMMENDER_URL + wget test + deploy).
- Files changed: docs/runbook.md (new), prd.json (US-061 passes=true)
- **Learnings for future iterations:**
  - stack.env already contains all required env vars with `CHANGE_THIS_*` placeholders — runbook should reference it directly rather than duplicating variable names
  - Keycloak admin password rotation: set env var in stack.env.local + `docker compose up -d keycloak` (no realm re-import needed just for password change)
  - `docker compose exec h3-app wget -qO- http://recommender:8000/health` is the fastest way to test inter-container DNS from inside the app container
---

## 2026-03-15 - US-060
- What was implemented: Added full OpenAPI 3.1 spec and Postman collection. (1) Installed `swagger-jsdoc` + `swagger-ui-express` + `js-yaml` as app dependencies. (2) Created `app/swagger.js` — swagger-jsdoc config with OpenAPI 3.1 definition: info, servers (dev+prod), bearerAuth security scheme, reusable schemas (Error, ServiceStatus, HealthResponse, Participant, Survey, Profile, Habit, HabitStats), `externalDocs` linking to `API-service/openapi.yaml` for the Python recommender spec. (3) Added `@swagger` JSDoc annotations to all 6 route files: v1Router (GET /health, GET /docs, GET /docs/openapi.json), adminRouter (17 endpoints covering participants CRUD + group assignment + token card + progress + sessions + settings + survey admin CRUD + habits feed), surveyRouter (GET /surveys, GET /surveys/:id, POST /surveys/:id/results), habitsRouter (GET /habits/public, GET /habits/stats, POST /habits/:id/annotate), recommendRouter (GET /:userId, GET /:userId/history, POST /classify), profileRouter (GET /profile, POST /profile). (4) Added `GET /api/v1/docs` (Swagger UI) and `GET /api/v1/docs/openapi.json` (raw JSON spec) endpoints to v1Router — both unauthenticated. (5) Created `scripts/generate-spec.js` — ESM script importing swagger.js and writing `docs/api/openapi.yaml` via js-yaml; added `npm run generate-spec` to app/package.json. (6) Generated `docs/api/openapi.yaml` (full annotated spec, 28 paths). (7) Created `docs/api/hhh-postman-collection.json` — Postman v2.1 collection with `{{baseUrl}}` and `{{token}}` variables, one request per endpoint (28 total), example auth headers, request bodies, and expected responses organized into 8 folders.
- Files changed: app/swagger.js (new), app/routes/v1Router.js (add swagger UI routes + imports), app/routes/adminRouter.js (@swagger annotations), app/routes/surveyRouter.js (@swagger annotations), app/routes/habitsRouter.js (@swagger annotations), app/routes/recommendRouter.js (@swagger annotations), app/routes/profileRouter.js (@swagger annotations), app/package.json (add swagger-jsdoc, swagger-ui-express, js-yaml, generate-spec script), app/package-lock.json, scripts/generate-spec.js (new), docs/api/openapi.yaml (new), docs/api/hhh-postman-collection.json (new), prd.json (US-060 passes=true)
- **Learnings for future iterations:**
  - `swagger-ui-express` v5+ works correctly with ESM (`"type": "module"`) — import as `import swaggerUi from 'swagger-ui-express'`; use `router.use('/docs', swaggerUi.serve); router.get('/docs', swaggerUi.setup(spec))` pattern
  - `swagger-jsdoc` v6 reads `@swagger` (not `@openapi`) JSDoc comments in `.js` route files — specify paths in `apis:` array using absolute paths with `path.join(__dirname, ...)`
  - Route-level swagger paths must match the path as mounted (without the `/api/v1` prefix since the swagger server URL includes it)
  - `js-yaml` v4 provides `yaml.dump(obj, { lineWidth: 120, noRefs: true })` for clean YAML output from a JS object
  - The generate-spec.js script must use dynamic `import()` to load app/swagger.js since it lives outside the app/ directory; run it from the repo root or from app/ via `npm run generate-spec`
---

## 2026-03-15 - US-059
- What was implemented: Created `docs/data-model.md` — comprehensive data model reference with: (1) Neo4j section: table of all 5 node labels (hhh__Donor, hhh__Habit, hhh__Behavior, hhh__Context+subclasses, hhh__ExperimentalSetting/Groups, Resource) with all properties+types; relationship types table (hhh__donates, hhh__hasBehavior, hhh__hasContext, hhh__hasTranslation, hhh__partOf); 10 annotated Cypher queries (habits by group, habits by BCIO category, donation timeline, top annotated habits, G3 vs G4 comparison, habits+context nodes, physical setting distribution, donor participation summary, multi-language translations, group integrity check); (2) Fuseki/SPARQL section: namespace prefix table (hhh, bcio, owl, rdf, rdfs, xsd, xml); 10 annotated SPARQL queries including class listing, hierarchy, property inspection, duplicate check, domain-review flags; (3) MongoDB section: 8 collections (participants, profiles, surveys, survey_responses, habit_donations, habit_annotations, admin_settings, recommendations_log) with full field tables and example documents; (4) G1-G4 encoding section: all 3 stores (Keycloak/MongoDB/Neo4j), group definitions table, unambiguous query examples using node labels; (5) Anonymisation model: table covering all data stores with what is retained vs anonymised on soft-delete, plus note on additional steps for GDPR full erasure.
- Files changed: docs/data-model.md (new), prd.json (US-059 passes=true)
- **Learnings for future iterations:**
  - MongoDB collections discovered from adminRouter.js and surveyRouter.js: participants, surveys, survey_responses, habit_donations, habit_annotations, admin_settings, recommendations_log, participants (also `participants` has `profile` join via profileRouter using `profiles` collection)
  - The `profiles` collection is separate from `participants` — upserted via POST /api/v1/profile; keyed on `userId` (Keycloak sub)
  - `recommendations_log` tracks accepted/dismissed recommendation events; used in GET /api/v1/admin/participants/:id/progress response
  - Neo4j relationship query pattern: `MATCH (d:hhh__Donor)-[:hhh__donates]->(h:hhh__Habit)-[:hhh__hasBehavior]->(b:hhh__Behavior)-[:hhh__hasContext]->(c:hhh__Context)` — all four nodes connected in a chain
---

## 2026-03-15 - US-058
- What was implemented: Created `docs/architecture.md` — comprehensive system architecture document with: (1) system overview Mermaid diagram showing all 10 Docker services (Traefik, Node.js backend, Keycloak, Python recommender, Fuseki, Neo4j, MongoDB, Mongo Express, LibreTranslate, backup) with arrows showing data flow and port numbers; (2) per-service reference table with technology, purpose, internal port, external URL (dev), and key env vars; (3) note that Flutter web is served as static files from `app/public/flutter/` via the Node.js backend (no separate container); (4) three Mermaid sequence diagrams: Keycloak PKCE login flow, habit donation flow (Flutter→backend→MongoDB+Neo4j), recommendation request flow (Flutter→backend→Python recommender→Neo4j); (5) ontology section with HHH/BCIO namespace table, HHH core classes table, BCIO integration alignment notes, G1–G4 encoding table, 2 example Cypher queries, 2 example SPARQL queries.
- Files changed: docs/architecture.md (new), prd.json (US-058 passes=true)
- **Learnings for future iterations:**
  - Flutter web is NOT a separate Docker service — it is built with `flutter build web` and served as static files from `app/public/flutter/` by the Node.js backend; Traefik routes everything to port 3000
  - Ontology files are in `fuseki/init/` (Ontology.ttl, schema.ttl, data.ttl); BCIO is merged inline into Ontology.ttl with TODO alignment comments
  - G1–G4 groups are OWL subclasses of `hhh:ExperimentalSetting` defined in Ontology.ttl; after migration use `donor.studyGroup` property in Neo4j
  - Keycloak runs with `KC_DB=dev-file` (file-based H2 DB) in both dev and prod docker-compose files — not a production-grade DB
---

## 2026-03-15 - US-057
- What was implemented: Created `docs/guides/admin-guide-de.md` — full German translation of the English admin guide with Quick Start checklist (8-step cohort setup) and all 8 sections translated: 1) Als Admin anmelden, 2) Teilnehmer erstellen und Token-Karte herunterladen, 3) Studiengruppen zuweisen, 4) Fragebögen konfigurieren, 5) Gespendete Gewohnheiten ueberwachen, 6) Teilnehmerfortschritt verfolgen, 7) Gerätesitzungen widerrufen, 8) Token-Karten-Format in den Einstellungen konfigurieren. Screenshot references identical to English guide (`../assets/screenshots/admin/`). Generated `docs/guides/admin-guide-de.pdf` via pandoc (LaTeX engine, ToC anchor reference warnings only — PDF 180KB).
- Files changed: docs/guides/admin-guide-de.md (new), docs/guides/admin-guide-de.pdf (new), prd.json (US-057 passes=true)
- **Learnings for future iterations:**
  - LibreTranslate runs on port 5000 in the docker-compose stack; if not running, translation can be produced directly (same as participant guide de pattern)
  - German umlauts (ae, oe, ue etc.) and special characters render correctly via pandoc/pdflatex with UTF-8 inputenc — no need for manual escaping
  - German ToC anchor links in markdown cause the same "undefined reference" LaTeX warnings as in English — cosmetic only, PDF generated successfully
---

## 2026-03-15 - US-056
- What was implemented: Created `docs/guides/admin-guide.md` — full admin guide with Quick Start checklist (8-step cohort setup) and 8 sections: 1) Logging in as admin, 2) Creating a participant + downloading token card, 3) Assigning study groups, 4) Configuring questionnaires (create/edit/publish/archive/assign groups), 5) Monitoring donated habits (filters + CSV export), 6) Tracking participant progress, 7) Revoking device sessions, 8) Configuring token card format in settings. Generated 13 placeholder PNG screenshots in `docs/assets/screenshots/admin/` using pure Python (struct + zlib, same approach as participant screenshots). Callout annotations listed as numbered descriptions in markdown tables (circled Unicode numbers ①–⑦ replaced with ASCII (1)–(7) for LaTeX compatibility). Generated `docs/guides/admin-guide.pdf` via pandoc from the guides/ directory using `--resource-path=..` (LaTeX engine; ToC anchor reference warnings only — PDF generated successfully, 177KB).
- Files changed: docs/guides/admin-guide.md (new), docs/guides/admin-guide.pdf (new), docs/assets/screenshots/admin/*.png (13 new placeholder files), prd.json (US-056 passes=true)
- **Learnings for future iterations:**
  - Circled Unicode numbers ①②③ etc. are not supported by pdflatex — replace with ASCII `(1)(2)(3)` before running pandoc PDF generation
  - Run `pandoc` from the same directory as the markdown file when using relative image paths (`../assets/screenshots/...`) — pandoc resolves paths relative to its CWD, so `--resource-path=..` is needed when running from guides/
  - Annotation callouts in documentation: inline them in the screenshot table cell next to the image reference — LaTeX tables with two columns (image | annotations) render well in pandoc PDF output
---

## 2026-03-15 - US-055
- What was implemented: Created `docs/guides/participant-guide-de.md` — full German translation of the English participant guide with all 7 sections, step labels, captions, tips, and troubleshooting notes translated. Screenshot references identical to English guide (`../assets/screenshots/participant/`). Generated `docs/guides/participant-guide-de.pdf` via pandoc (LaTeX engine, warnings only — same as English guide). LibreTranslate was not running locally; translation produced directly and reviewed for accuracy.
- Files changed: docs/guides/participant-guide-de.md (new), docs/guides/participant-guide-de.pdf (new), prd.json (US-055 passes=true)
- **Learnings for future iterations:**
  - LibreTranslate runs on port 5000 in the docker-compose stack; if not running, translation can be produced directly — note this in the progress report
  - German umlauts (ä, ö, ü, ß) render correctly in pandoc PDF output via LaTeX (pdflatex handles UTF-8 with inputenc)
  - ToC anchor links in German markdown cause the same "undefined reference" LaTeX warnings as in English — cosmetic only, PDF is generated successfully
---

## 2026-03-15 - US-054
- What was implemented: Created `docs/guides/participant-guide.md` with 7 sections and numbered steps: 1) Accessing the app (Android APK / iOS / browser), 2) Logging in with token card (QR scan + manual entry), 3) Completing your profile, 4) Donating a habit, 5) Exploring the habit graph and annotating nodes, 6) Understanding your recommendations (rationale + citation + accept/dismiss), 7) Updating your profile. Each section contains a markdown table referencing three screenshots (Android, iOS, Chrome). Created 21 placeholder PNG screenshots (390×844 px teal fill) in `docs/assets/screenshots/participant/` — one per platform per section, named descriptively (e.g. `01-login-android.png`). Generated `docs/guides/participant-guide.pdf` via `pandoc` using the default LaTeX engine (warnings only — no errors; PDF file created successfully).
- Files changed: docs/guides/participant-guide.md (new), docs/guides/participant-guide.pdf (new), docs/assets/screenshots/participant/*.png (21 new placeholder files), prd.json (US-054 passes=true)
- **Learnings for future iterations:**
  - `pandoc <file>.md -o <file>.pdf` uses the LaTeX engine by default — produces warnings about unresolved internal anchor links but the PDF is generated successfully; wkhtmltopdf is not installed so LaTeX fallback is the correct approach
  - Placeholder PNGs can be generated with pure Python (struct + zlib) without any image library — write PNG signature + IHDR + IDAT (zlib-compressed raw RGB rows) + IEND
  - Screenshot table pattern in markdown: `| Platform | Screenshot |` with image references relative to the guides/ directory (`../assets/screenshots/participant/filename.png`) works correctly in pandoc PDF output
---

## 2026-03-15 - US-053
- What was implemented: Created `scripts/restore.sh` — accepts `<timestamp>` arg, finds `full_backup_<timestamp>.tar.gz`, prompts `"Restore from <timestamp>? This will overwrite current data. [y/N]"`, extracts to temp dir, restores MongoDB via `mongorestore --drop`, Neo4j via `docker stop + neo4j-admin database load + docker start`, Keycloak via DELETE realm + POST re-import using admin API. Created `scripts/add-mongo-validators.js` — Node.js script that adds `$jsonSchema` validators with `additionalProperties: false` to MongoDB collections: `results`, `profiles`, `habit_annotations`, `admin_settings`; creates collections if they don't exist. Created `app/middleware/rateLimiter.js` — `express-rate-limit` with 100 req/15min window, keyed by `req.user?.sub || req.ip`, returns 429 with JSON error. Created `app/middleware/inputSanitizer.js` — recursively strips HTML tags (`/<[^>]*>/g`) from all string values in `req.body` for POST/PUT/PATCH requests. Updated `app/routes/v1Router.js` to apply `limiter` and `sanitizeBody` before auth middleware. Confirmed all DELETE routes already use soft-delete (`deletedAt` timestamp). Added 10 unit tests for new middleware. 186 tests pass.
- Files changed: scripts/restore.sh (new), scripts/add-mongo-validators.js (new), app/middleware/rateLimiter.js (new), app/middleware/inputSanitizer.js (new), app/routes/v1Router.js (add rateLimiter + sanitizeBody), app/tests/unit/rateLimiter.middleware.test.js (new), app/tests/unit/inputSanitizer.middleware.test.js (new), app/package.json (add express-rate-limit + express-validator deps), app/package-lock.json, prd.json (US-053 passes=true)
- **Learnings for future iterations:**
  - `express-rate-limit` v7+ exports `{ rateLimit }` as named export (ESM) — `import { rateLimit } from 'express-rate-limit'`; `keyGenerator` takes `req` and returns a string key
  - Rate limiter `handler` (not `message`) is used for custom JSON responses — set `handler: (_req, res) => res.status(429).json({...})`
  - MongoDB `collMod` command updates validators on existing collections; `createCollection` sets them on new ones — use `listCollections` to check existence first
  - `additionalProperties: false` in `$jsonSchema` makes MongoDB reject documents with unknown fields; always include `_id: { bsonType: 'objectId' }` in properties or MongoDB will reject its own inserts
  - Input sanitization: strip HTML tags with `/<[^>]*>/g` regex — removes `<tag>` delimiters but preserves text content between tags; apply recursively to nested objects/arrays
  - Keycloak realm restore: must DELETE existing realm first (`DELETE /admin/realms/hhh`), then POST the realm JSON to `/admin/realms` — not a PATCH/update operation
  - `set -euo pipefail` in bash scripts: `${VAR:-default}` for optional env vars; `|| true` for non-critical commands that may fail
---

## 2026-03-15 - US-052
- What was implemented: Extended `backup-service/backup.sh` — added Keycloak realm export via admin API (POST token to master realm, then POST partial-export to hhh realm, saves to `backups/<timestamp>/keycloak/hhh-realm.json`); renamed `ALERT_EMAIL` to `BACKUP_EMAIL` per PRD spec; added `log()` function writing timestamped output to both stdout and `$BACKUP_DIR/backup.log`; changed all `echo` calls in main flow to `log()`; changed default retention from 14 to 30 days; added `jq` to Dockerfile apk packages for JSON token parsing; changed Dockerfile `CMD` from `/backup.sh` (one-shot) to `crond -f` (daemon) with `0 2 * * *` crontab entry. MongoDB (`mongodump`), Fuseki (tar), and Neo4j (`neo4j-admin dump`) were already present.
- Files changed: backup-service/backup.sh (Keycloak export + log() + BACKUP_EMAIL + 30-day retention), backup-service/Dockerfile (add jq + crond CMD + crontab), prd.json (US-052 passes=true)
- **Learnings for future iterations:**
  - Keycloak realm export via API: POST to `/realms/master/protocol/openid-connect/token` with `grant_type=password` for admin-cli token, then POST to `/admin/realms/hhh/partial-export?exportClients=true&exportGroupsAndRoles=true` — `jq` required in container to parse the token JSON
  - `crond -f` (foreground) is the correct Alpine cron daemon command — `-f` keeps it in foreground so Docker container stays running; `CMD ["/backup.sh"]` only runs one-shot
  - The backup.sh crontab entry goes to `/var/spool/cron/crontabs/root` on Alpine Linux — different path from Ubuntu's `/etc/cron.d/`
  - `${VAR:-}` (empty default) prevents `set -u` errors for optional env vars; required once using `set -euo pipefail` patterns
---

## 2026-03-15 - US-051
- What was implemented: Created `.github/workflows/deploy.yml` — CD workflow with two triggers: `workflow_run` (watches CI workflow on master, only proceeds if CI concluded successfully) and `push: tags: v*`. Deploy job SSHs to production server via `appleboy/ssh-action@v1` using `SERVER_SSH_KEY`, `SERVER_HOST`, `SERVER_USER` secrets; runs `git pull && bash scripts/deploy-full.sh` with `set -euo pipefail`. Release job (tag-only) sets up Flutter, builds APK with `flutter build apk --release` and web with `flutter build web`, zips the web output, then uploads both artifacts to a GitHub Release via `softprops/action-gh-release@v2` with `generate_release_notes: true`.
- Files changed: .github/workflows/deploy.yml (rewritten), prd.json (US-051 passes=true)
- **Learnings for future iterations:**
  - `workflow_run` is the correct GitHub Actions event for "only run deploy after CI passes" across two separate workflow files — use `if: github.event.workflow_run.conclusion == 'success'` on the job
  - `workflow_run` does NOT carry the full commit SHA of the triggering push by default — the deploy job uses the latest commit on the branch (git pull fetches it)
  - `softprops/action-gh-release@v2` (not v1) is the current version for uploading release assets; set `files:` to a multiline list of glob patterns
  - Flutter web output lands in `mobile/build/web/` — zip it (`zip -r flutter-web.zip .` from inside that dir) before uploading since GitHub Releases expects single files, not directories
  - `appleboy/ssh-action@v1` is the current major version (not v0.1.8 used in the legacy deploy.yml)
---

## 2026-03-15 - US-050
- What was implemented: Updated `.github/workflows/ci.yml` — added `flutter-test` job (flutter pub get, flutter analyze lib/ test/, flutter test test/widget/ --coverage, upload coverage artifact); added backend coverage artifact upload step after `npm test`; scoped triggers to `pull_request` on master/rapid_dev branches and `push` to master. All three jobs (backend-test, flutter-test, ontology-test) run in parallel. Ontology job uses GitHub Actions service containers for neo4j+fuseki (more reliable than docker-compose in CI). `flutter analyze` confirmed exits 0, `npm test` passes 176 tests.
- Files changed: .github/workflows/ci.yml (updated), prd.json (US-050 passes=true)
- **Learnings for future iterations:**
  - GitHub Actions service containers are more reliable than `docker-compose up -d` in CI — no need for docker-compose CLI install, healthchecks are built in via `options:`
  - `subosito/flutter-action@v2` is the standard Flutter setup action; use `flutter-version: "3.32.0"` + `channel: "stable"` + `cache: true`
  - Flutter widget tests run with `flutter test test/widget/ --coverage` — coverage goes to `mobile/coverage/`; `integration_test` tests require a device so they're excluded from CI
  - `actions/upload-artifact@v4` is the current version (v3 is deprecated); use `name:` for the artifact name and `path:` for the directory
  - ci.yml trigger: use both `push: branches: [master]` and `pull_request: branches: [master, rapid_dev]` to cover both direct pushes and PR workflows
---

## 2026-03-15 - US-049
- What was implemented: Created `scripts/deploy-full.sh` — orchestrator deploy script that runs all four per-service scripts in order (deploy-backend.sh → deploy-keycloak.sh → deploy-recommender.sh → deploy-flutter-web.sh), polling GET /api/v1/health between each step (60s timeout, fails if exceeded). Passes --dry-run to all sub-scripts when invoked with --dry-run. Every log line is timestamped. Warns if CHANGELOG.md version has no matching git tag. Uses `set -euo pipefail` to exit non-zero on any step failure. Script made executable (chmod +x).
- Files changed: scripts/deploy-full.sh (new), prd.json (US-049 passes=true)
- **Learnings for future iterations:**
  - CHANGELOG.md does not exist at repo root; the version-bump check handles this gracefully with a WARN message
  - Git tag check uses `git tag --list "v<version>"` — works with both `v1.2.3` and `1.2.3` tag conventions
  - Sub-scripts each have their own --dry-run handling; the orchestrator simply passes the flag through rather than skipping sub-script execution
---

## 2026-03-15 - US-047
- What was implemented: Created `mobile/integration_test/participant_flow_test.dart` — end-to-end participant flow test using `integration_test` SDK package with Riverpod provider overrides for all services. Test drives: (1) login screen visible with Health Habit Hub title; (2) enter credentials and tap Login; (3) NavigationBar with Donate/Explore/Recommend/Profile tabs appears; (4) DonateScreen loads showing "Donate a Habit" AppBar; (5) tap Recommend tab; (6) RecommendScreen shows "Recommendations" AppBar and two recommendation cards ("Morning Walk", "Mindful Breathing"). Fake services: `_FakeAuthService` (login succeeds, token returns test JWT), `_FakeSurveyService` (returns mock Survey), `_FakeRecommendationService` (returns 2 mock Recommendation objects). Custom test router adds `/home` redirect to `/donate` to handle `LoginScreen`'s `context.go('/home')` post-login. Added `integration_test: sdk: flutter` to pubspec.yaml dev_dependencies. `flutter analyze integration_test/ lib/` reports no issues.
- Files changed: mobile/integration_test/participant_flow_test.dart (new), mobile/pubspec.yaml (add integration_test dep), mobile/pubspec.lock (updated), prd.json (US-047 passes=true)
- **Learnings for future iterations:**
  - `integration_test` is an SDK package: `integration_test:\n    sdk: flutter` in dev_dependencies (no version number)
  - `IntegrationTestWidgetsFlutterBinding.ensureInitialized()` requires a connected device/emulator — use `flutter test -d <device-id>` to run; without a device the test fails with "No macOS/android desktop project configured"
  - macOS desktop target requires a `macos/` project folder which is not present in this repo; test can run on Android emulator or physical iOS/Android device
  - `LoginScreen.login()` calls `context.go('/home')` after success — this route is not in the production router; integration test needs a redirect: `if (state.matchedLocation == '/home') return '/donate'` in the test router's redirect callback
  - `_testToken` in integration tests: base64url-encode a minimal JWT payload `{"sub":"test-user-001","realm_access":{"roles":["participant"]}}` without signature for `userIdProvider` and `userRolesProvider` to decode correctly (since those providers parse the JWT manually)
  - `test/widget_test.dart` (default Flutter counter smoke test) has been broken since project start — it tests counter behavior that HhhApp doesn't have; it's excluded from `flutter test test/widget/` and doesn't affect the 41 widget tests
---

## 2026-03-15 - US-046
- What was implemented: Created 9 widget test files in `mobile/test/widget/`: `login_screen_test.dart` (4 tests — fields visible, pre-fill, loading indicator, error message); `shell_screen_test.dart` (4 tests — NavigationBar, admin tab hidden/shown, loading state); `donate_screen_test.dart` (4 tests — AppBar title, loading indicator, offline banner, wifi_off icon); `profile_screen_test.dart` (4 tests — AppBar title, loading indicator, offline banner, wifi_off icon); `recommend_screen_test.dart` (6 tests — AppBar title, loading skeleton, not-authenticated error, service error, empty state, card shown); `explore_screen_test.dart` (5 tests — AppBar title, loading indicator, error + retry, empty state, TabBar tabs); `stats_screen_test.dart` (4 tests — loading skeleton, error + retry, total count shown, empty chart placeholder); `admin_participants_screen_test.dart` (5 tests — AppBar title, loading, error + retry, empty state, row shown); `admin_surveys_screen_test.dart` (5 tests — AppBar title, loading, error + retry, empty state, survey tile shown). All 41 tests pass. `flutter analyze lib/ test/` reports no issues.
- Files changed: mobile/test/widget/login_screen_test.dart (new), mobile/test/widget/shell_screen_test.dart (new), mobile/test/widget/donate_screen_test.dart (new), mobile/test/widget/profile_screen_test.dart (new), mobile/test/widget/recommend_screen_test.dart (new), mobile/test/widget/explore_screen_test.dart (new), mobile/test/widget/stats_screen_test.dart (new), mobile/test/widget/admin_participants_screen_test.dart (new), mobile/test/widget/admin_surveys_screen_test.dart (new), prd.json (US-046 passes=true)
- **Learnings for future iterations:**
  - Screens with internal `Dio` instances (ProfileScreen) make real HTTP calls in `initState` — use `pumpAndSettle()` in tests to drain Dio's zero-duration async timers; using `pump()` alone leaves pending timers causing test failures
  - Use `Completer<T>().future` (never completes) for loading-state tests instead of `Future.delayed(minutes: 1)` — `Future.delayed` creates a pending timer that triggers "Timer still pending" test failure
  - For `FakeAuthService.login()` returning a never-completing future: return `Completer<void>().future` to keep `_isLoading = true` long enough for the indicator assertion
  - `ProfileScreen` tests all use `_FakeSurveyService` that throws — real Dio fails in test env so the offline banner is shown after `pumpAndSettle()`; AppBar title is visible in all states
  - `StatsScreen` is normally embedded in a `TabBarView` — standalone testing needs `OverflowBox(maxHeight: double.infinity)` wrapper to prevent `_StatsSkeleton` column overflow failures
  - For `ShellScreen`, the `StatefulNavigationShell` can only be created via a real `GoRouter` with `StatefulShellRoute.indexedStack`; set up a minimal test router in the test file
  - For screens using `GoRouter` context (`context.push`, `context.go`), wrap test widget in `MaterialApp.router(routerConfig: testRouter)` so navigation calls don't throw
  - `dart analyze` `unnecessary_underscores` lint: use single `_` wildcard for BOTH unused params in GoRoute builders `(_, _) =>` (Dart 3 wildcard syntax)
  - `final` fields are null-promoted after null checks — `if (field != null) return field.future;` (not `field!.future`)
---

## 2026-03-15 - US-045
- What was implemented: Created `app/tests/integration/surveys.routes.test.js` (11 tests — auth 401/403, GET /surveys participant sees only published surveys for their group, admin/researcher see all surveys, GET /surveys/:id shape and 404, POST /surveys/:id/results 201 and 404); created `app/tests/integration/health.route.test.js` (4 tests — 200 without auth, status ok, service checks present, no Authorization header needed). Fixed `npm test` to pass: updated `test:unitTests` glob from `tests/**/*.test.js` to `tests/unit/**/*.test.js` + `tests/integration/**/*.test.js` to exclude legacy service-dependent tests (Neo4j/Fuseki/Mongo/Translate) which require running Docker services. Fixed prettier formatting across 40+ files. Fixed ESLint: added `caughtErrorsIgnorePattern: '^_'`, `argsIgnorePattern: '^_'`, `varsIgnorePattern: '^_'` to no-unused-vars rule; added `coverage/`, `public/js/donate.js`, `public/js/nav.js` to ignores; fixed `no-empty` in adminRouter.js; fixed `no-useless-escape` and `no-useless-catch` and multiple unused vars in Neo4jDatabase.js. 176 tests pass, `npm test` exits 0.
- Files changed: app/tests/integration/surveys.routes.test.js (new), app/tests/integration/health.route.test.js (new), app/package.json (update test:unitTests glob), app/.prettierignore (add coverage/), app/eslint.config.js (lint rule overrides + ignores), app/routes/adminRouter.js (empty catch fix), app/utils/Neo4jDatabase.js (lint fixes), app/tests/BackupConfiguration.test.js (catch fix), app/tests/integration/admin.settings.test.js + admin.surveys.test.js + recommend.routes.test.js (prettier), many other files (prettier formatting), prd.json (US-045 passes=true)
- **Learnings for future iterations:**
  - `test:unitTests` glob `tests/**/*.test.js` matches legacy service integration tests (Neo4j/Fuseki/Mongo/Translate) that require running Docker services — use `tests/unit/**/*.test.js` + `tests/integration/**/*.test.js` to exclude them
  - ESLint `no-unused-vars` requires `caughtErrorsIgnorePattern: '^_'` in addition to `argsIgnorePattern`/`varsIgnorePattern` to allow `catch (_err)` patterns
  - `no-empty` (empty catch block) can be suppressed by adding a comment inside: `} catch { // reason }`
  - `no-useless-catch` fires when a catch block only does `throw e` — remove the try/catch entirely to let the exception propagate naturally
  - `coverage/` directory must be added to both `.prettierignore` and eslint `ignores` to prevent format/lint checks on coverage JSON output
---

## 2026-03-15 - US-044
- What was implemented: Created 6 new unit test files in `app/tests/unit/`: `requireRole.middleware.test.js` (6 tests — required role calls next, no role 403, missing user 403, multiple roles any-match, multiple roles none-match); `survey.controller.test.js` (6 tests — admin gets all surveys, participant filtered to published/group, GET by id found/404, POST results 201/404, via `createSurveyRouter` factory with mock db); `habit.controller.test.js` (5 tests — base route, public habits with annotation counts, annotate valid/invalid 400, stats, via `createHabitsRouter` factory with mock db + mock neo4jRun); `admin.controller.test.js` (15 tests — GET base/participants/settings/surveys/token-card PDF, POST participants/surveys, PATCH group valid/invalid 400/404, PUT settings/missing value 400, DELETE participant, via `createAdminRouter` factory with mock db/keycloak/tokenCardService); `auth.tokenVerifier.test.js` (5 tests — valid JWT, expired, invalid sig, unknown kid, malformed, for `createTokenVerifier` standalone function); `legacy.survey.controller.test.js` (2 tests — error paths for `renderSurvey` and `submitSurvey` when MongoDB unavailable, with `MONGO_SERVER_SELECTION_TIMEOUT_MS=200` to keep fast). Added `c8` devDependency and `test:coverage` script. Coverage: middleware/ 99.1%, controllers/ 45.28% (legacy surveyController.js happy path needs live MongoDB), routes/ habitsRouter 91.27% + surveyRouter 88.72% + adminRouter 44.6%. 55 unit tests pass.
- Files changed: app/tests/unit/requireRole.middleware.test.js (new), app/tests/unit/survey.controller.test.js (new), app/tests/unit/habit.controller.test.js (new), app/tests/unit/admin.controller.test.js (new), app/tests/unit/auth.tokenVerifier.test.js (new), app/tests/unit/legacy.survey.controller.test.js (new), app/package.json (add c8 devDep + test:coverage script), app/package-lock.json, prd.json (US-044 passes=true)
- **Learnings for future iterations:**
  - `c8` is the correct ESM coverage tool for Node.js (not nyc); add `"c8": "^11.0.0"` to devDependencies; use `c8 --include='path/**' --reporter=text node --test "glob"` pattern
  - `mock.module` is NOT available in Node.js 25.2.1 built-in test runner — ESM module mocking requires a `--loader` hook; for legacy controllers that import `connect()` directly, test only error paths by letting the connection timeout (set `MONGO_SERVER_SELECTION_TIMEOUT_MS=200` env var)
  - `process.env.MONGO_SERVER_SELECTION_TIMEOUT_MS = '200'` must be set BEFORE `import { connect }` is evaluated — use top-of-file assignment before imports (Prettier-compatible: inline assignment before import statements)
  - For Express router factory tests (createHabitsRouter, createAdminRouter, createSurveyRouter): create miniature HTTP servers with `createServer(express())`, inject mock db/keycloak/tokenCardService, listen on port 0 for dynamic port, test with `fetch`; this pattern is already established in `tests/integration/`
  - `requireRole.middleware.test.js` can test the function directly without HTTP server since it takes (req, res, next) — simpler and faster
---

## 2026-03-15 - US-043
- What was implemented: Created `mobile/lib/screens/admin/admin_shell_screen.dart` (AdminShellScreen — StatelessWidget; NavigationRail with 5 destinations: Participants/group icon, Surveys/assignment icon, Habits/psychology icon, Devices/devices icon, Settings/settings icon; active rail item detected by `GoRouterState.of(context).matchedLocation.startsWith(section.path)`; `context.go(section.path)` on destination tap; layout is `Row(NavigationRail, VerticalDivider, Expanded(child))`). Converted `_router` from top-level global to `routerProvider` (Riverpod `Provider<GoRouter>`) so the redirect guard can call `ref.read(userRolesProvider.future)` without accessing BuildContext; redirect returns `'/'` for any `/admin/*` path when user lacks admin or researcher role. Added `ShellRoute` inside the admin `StatefulShellBranch` (`initialLocation: '/admin/participants'`): ShellRoute builder wraps child in `AdminShellScreen`; admin sub-routes use absolute paths (`/admin/participants`, `/admin/surveys`, `/admin/habits`, `/admin/devices`, `/admin/settings`) with detail routes (`:id`) nested within their parent GoRoute. Converted `HhhApp` from `StatelessWidget` to `ConsumerWidget` watching `routerProvider`.
- Files changed: mobile/lib/screens/admin/admin_shell_screen.dart (new), mobile/lib/main.dart (routerProvider + ShellRoute + ConsumerWidget + redirect guard), prd.json (US-043 passes=true)
- **Learnings for future iterations:**
  - GoRouter as a `Provider<GoRouter>` (Riverpod): `ref.read(someProvider.future)` inside the redirect closure works because the ProviderRef stays valid for the lifetime of the Provider; use `ref.onDispose(router.dispose)` to clean up
  - `StatefulShellBranch(initialLocation: '/admin/participants', routes: [ShellRoute(...)])` sets the branch's default navigation target — tapping the Admin bottom-nav tab always opens to participants on first visit
  - `ShellRoute` inside `StatefulShellBranch.routes`: all GoRoutes nested under the ShellRoute are wrapped by its builder (AdminShellScreen); detail routes (`:id`) nested within a parent GoRoute are also wrapped — side rail remains visible on detail screens
  - `GoRouterState.of(context).matchedLocation` gives the current path inside a ShellRoute builder's child widget — use `startsWith` to highlight the correct rail item even on detail sub-routes
  - No browser tools available — manual browser verification needed
---

## 2026-03-15 - US-042
- What was implemented: Added `fetchSettings()` (GET /api/v1/admin/settings) and `updateSetting(key, value)` (PUT /api/v1/admin/settings/:key) to AdminService. Created `mobile/lib/screens/admin/admin_settings_screen.dart` (AdminSettingsScreen — ConsumerStatefulWidget; loads settings on initState; displays token card format section with `RadioGroup<String>` ancestor + `RadioListTile<String>` children for QR only/Print only/Both options; Save button calls updateSetting with selected value and shows success/error snackbar; Retry on load error; Refresh in AppBar). Added /admin/settings route to main.dart.
- Files changed: mobile/lib/services/admin_service.dart (add fetchSettings + updateSetting), mobile/lib/screens/admin/admin_settings_screen.dart (new), mobile/lib/main.dart (import + route), prd.json (US-042 passes=true)
- **Learnings for future iterations:**
  - Flutter 3.32+ deprecated `RadioListTile.groupValue` and `RadioListTile.onChanged` — use `RadioGroup<T>(groupValue:, onChanged:, child: Column([RadioListTile(value:)...]))` instead
  - `RadioGroup.onChanged` is `ValueChanged<T?>` which is non-nullable (function itself); always provide a callback (even no-op) and guard inside: `(String? v) { if (v != null && cb != null) cb(v); }`
  - No browser tools available — manual browser verification needed
---

## 2026-03-15 - US-041
- What was implemented: Created `mobile/lib/models/admin_session.dart` (AdminSession with id/participantId/deviceType/appVersion/lastSeen; fromJson handles id||sessionId and participantId||userId fallbacks). Added `fetchSessions()` (GET /api/v1/admin/sessions) and `revokeSession(id)` (DELETE /api/v1/admin/sessions/:id) to AdminService. Created `mobile/lib/screens/admin/admin_devices_screen.dart` (AdminDevicesScreen — ConsumerStatefulWidget; _SummaryRow showing Android/iOS/Web counts as colored chips with icons; DataTable with columns Participant ID, Device Type, App Version, Last Seen, Session ID (truncated to 12 chars with tooltip showing full), Actions; Revoke TextButton per row triggers AlertDialog confirmation then deletes row on confirm; Refresh in AppBar). Added /admin/devices route to main.dart. Avoided `intl` package (not a direct dependency) — implemented manual date formatting with padLeft.
- Files changed: mobile/lib/models/admin_session.dart (new), mobile/lib/screens/admin/admin_devices_screen.dart (new), mobile/lib/services/admin_service.dart (add session methods + import), mobile/lib/main.dart (import + route), prd.json (US-041 passes=true)
- **Learnings for future iterations:**
  - `intl` package is NOT a direct dependency in mobile/pubspec.yaml — use manual date formatting (`padLeft`) rather than `DateFormat`
  - `Tooltip(message: fullId, child: Text(truncated))` is the Flutter pattern for showing full content on truncated DataTable cells
  - Session ID truncation: `id.length > 12 ? '${id.substring(0, 12)}…' : id` — use Unicode ellipsis (…) not ASCII (...)
  - No browser tools available — manual browser verification needed
---

## 2026-03-15 - US-040
- What was implemented: Created `mobile/lib/models/admin_habit_donation.dart` (pre-existing untracked file with HabitDonation and HabitsFeedResult models). Created `mobile/lib/screens/admin/admin_habits_screen.dart` with AdminHabitsScreen: auto-refreshing (30s, toggle-able via timer icon) ListView of HabitDonation entries; _FilterBar with DropdownButtonHideUnderline+InputDecorator+DropdownButton for group (G1–G4) and category (exercise/nutrition/sleep/mindfulness/social/other), date range picker via showDateRangePicker, Apply Filters button, Export CSV button via url_launcher; _DonationTile showing habitName, participantId, category, donatedAt, group badge. Added fetchHabitsFeed and exportHabitsCsvUrl to AdminService. Added /admin/habits route to main.dart. Fixed widget_test.dart pre-existing error (MyApp → HhhApp). Used // ignore: use_null_aware_elements for nullable-value map entries where Dart's ?key syntax was misapplied.
- Files changed: mobile/lib/models/admin_habit_donation.dart (new), mobile/lib/screens/admin/admin_habits_screen.dart (new), mobile/lib/services/admin_service.dart (add habits feed methods), mobile/lib/main.dart (import + route), mobile/test/widget_test.dart (fix MyApp→HhhApp), prd.json (US-040 passes=true)
- **Learnings for future iterations:**
  - `DropdownButtonFormField.value` is deprecated in new Flutter — use `DropdownButtonHideUnderline + InputDecorator + DropdownButton` instead (this pattern is already used in admin_surveys_screen.dart)
  - Dart's null-aware collection element `?key: value` applies to the KEY being null, not the VALUE; for nullable values in maps, use `if (v != null) 'key': v` with `// ignore: use_null_aware_elements`
  - `Timer.periodic` for auto-refresh: cancel existing timer in _startTimer before creating a new one; always cancel in dispose()
  - widget_test.dart was using `MyApp` which doesn't exist (should be `HhhApp`); this was a pre-existing error causing flutter analyze to exit 1
  - No browser tools available — manual browser verification needed
---

## 2026-03-15 - US-039
- What was implemented: Created `mobile/lib/models/admin_survey.dart` (AdminSurvey with id/title/type/status/assignedGroups/jsonSchema; fromJson handles _id||id fallback and List<String> assignedGroups). Created `mobile/lib/screens/admin/admin_surveys_screen.dart` with two screens: AdminSurveysScreen (ListView of surveys using _SurveyTile — shows title, type Chip, group Chips, color-coded status badge, Publish/Archive TextButton actions; FAB opens _CreateSurveyDialog with title TextField + type DropdownButton inside InputDecorator; row tap calls context.push('/admin/surveys/:id', extra: survey)) and AdminSurveyEditorScreen (accepts surveyId + optional initialSurvey; if initialSurvey provided skips API fetch; full-width monospace JSON TextField pre-populated via JsonEncoder.withIndent; _EditorBody contains FilterChips G1–G4 for group assignment; Save button calls updateSurvey(jsonSchema) + updateSurveyGroups; JSON validation before save). Added survey methods to AdminService: fetchSurveys, createSurvey, updateSurvey, updateSurveyStatus, updateSurveyGroups. Added routes to main.dart: /admin/surveys (AdminSurveysScreen) nested under /admin, /admin/surveys/:id (AdminSurveyEditorScreen with extra cast to AdminSurvey?); imported models/admin_survey.dart for the cast. Fixed unnecessary_underscores lint in separatorBuilder.
- Files changed: mobile/lib/models/admin_survey.dart (new), mobile/lib/screens/admin/admin_surveys_screen.dart (new), mobile/lib/services/admin_service.dart (add survey methods), mobile/lib/main.dart (import + routes), prd.json (US-039 passes=true)
- **Learnings for future iterations:**
  - When using `state.extra as SomeType?` in a GoRoute builder, `SomeType` must be imported in main.dart even if the screen file imports it independently
  - `separatorBuilder: (_, _) =>` (single underscore) avoids the `unnecessary_underscores` lint warning for unused params
  - Editor screen pattern: accept `initialSurvey` param to skip API round-trip when navigating from list with `extra:`; fall back to API fetch when opened by deep link (no extra)
  - `JsonEncoder.withIndent('  ').convert(map)` pretty-prints JSON for editor pre-population
  - dart analyze lib/: no issues; no browser tools available — manual verification needed
---

## 2026-03-15 - US-038
- What was implemented: Created `mobile/lib/models/participant_progress.dart` (ParticipantProgress with profile/surveys/habitsCount/recommendations/timeline fields; ProfileStatus/SurveyEntry/RecommendationCounts/TimelineEntry value types; fromJson factories; toJson() for export); added `fetchParticipantProgress(id)` to `AdminService` (GET /api/v1/admin/participants/:id/progress); created `mobile/lib/screens/admin/admin_participant_detail_screen.dart` (AdminParticipantDetailScreen — ConsumerStatefulWidget; _ProfileCard shows check/cross icon + completedAt; _SurveysSection lists survey titles with timestamps; _HabitsSection is an ExpansionTile showing count with note about Habits Monitor; _RecommendationsCard shows accepted/dismissed count chips; _TimelineSection renders a vertical timeline with per-type icons and colors; Export JSON button calls Share.share(jsonString) via share_plus; Retry on error; Refresh in AppBar); added `share_plus: ^10.1.0` to `mobile/pubspec.yaml`; updated `mobile/lib/main.dart` to import and use AdminParticipantDetailScreen for `/admin/participants/:id` route (removed now-unused _PlaceholderScreen).
- Files changed: mobile/lib/models/participant_progress.dart (new), mobile/lib/services/admin_service.dart (add fetchParticipantProgress + import), mobile/lib/screens/admin/admin_participant_detail_screen.dart (new), mobile/pubspec.yaml (add share_plus), mobile/pubspec.lock (updated), mobile/lib/main.dart (import + route + remove _PlaceholderScreen), prd.json (US-038 passes=true)
- **Learnings for future iterations:**
  - `Share.share(text, subject: 'title')` from share_plus is the simplest export — no temp file needed for plain text/JSON
  - share_plus: ^10.1.0 resolves cleanly with Flutter >=3.22.0 and dart >=3.10.0
  - Vertical timeline with connector lines: use `IntrinsicHeight` + `Row` where the icon column has `CircleAvatar` + `Expanded(Container(width:2))` for the line; set `isLast` to skip the connector on the final item
  - `color.withAlpha(24)` for chip/badge backgrounds avoids deprecated `withOpacity`
  - Removing an unused class (`_PlaceholderScreen`) is required to pass `dart analyze` with `unused_element` warning
  - File-level comments should use `//` not `///` to avoid `dangling_library_doc_comments` lint warning
  - dart analyze lib/: no issues; no browser tools available — manual verification needed
---

## 2026-03-15 - US-037
- What was implemented: Added `updateParticipantGroup(id, group)` (PATCH /api/v1/admin/participants/:id/group) and `deleteParticipant(id)` (DELETE /api/v1/admin/participants/:id) to `AdminService`. Updated `_ParticipantsTable` with two new required callbacks (`onGroupChange`, `onDelete`) and an `Actions` column; the Group cell is now an inline `DropdownButton` (G1–G4) that fires `onGroupChange`. Added `_changeGroup()` to the screen state: optimistically updates the participant in `_all`, calls `updateParticipantGroup`, reverts via `_load()` on error. Added `_confirmDelete()`: shows `AlertDialog` with text "This will anonymize participant data. Cannot be undone.", on confirm calls `deleteParticipant` and removes the row from `_all` via `setState`.
- Files changed: mobile/lib/services/admin_service.dart (add updateParticipantGroup + deleteParticipant), mobile/lib/screens/admin/admin_participants_screen.dart (add _changeGroup + _confirmDelete + update _ParticipantsTable), prd.json (US-037 passes=true)
- **Learnings for future iterations:**
  - For optimistic UI updates on a `List<AdminParticipant>`, reassign `_all` as a new list (`List.of(_all)..[idx] = updated`) so `setState` triggers a rebuild
  - `DropdownButton` without `InputDecorator` works fine inside a `DataCell` — no need for decoration when inside a table cell
---

## 2026-03-15 - US-036
- What was implemented: Added `url_launcher: ^6.3.0` to `mobile/pubspec.yaml`; added `createParticipant({group, tokenCardFormat})` (POST /api/v1/admin/participants) and `tokenCardUrl(participantId, format)` helper to `mobile/lib/services/admin_service.dart`; modified `mobile/lib/screens/admin/admin_participants_screen.dart` to add FloatingActionButton (person_add icon) that calls `_showCreateDialog()`; `_showCreateDialog` shows `_CreateParticipantDialog` (new StatefulWidget at bottom of file): group dropdown G1-G4, format dropdown QR+Print/QR only/Print only, Create button calls `createParticipant`, reads `tokenCardUrl` from response or derives from `AdminService.tokenCardUrl()`, pops dialog, shows snackbar 'Participant <username> created', opens PDF via `launchUrl(uri, mode: LaunchMode.externalApplication)`, reloads participant list; error displayed inline in dialog on failure; loading spinner replaces Create button text during request. Used `DropdownButton` inside `InputDecorator`+`DropdownButtonHideUnderline` to avoid deprecated `DropdownButtonFormField.value`.
- Files changed: mobile/pubspec.yaml (add url_launcher), mobile/pubspec.lock (updated), mobile/lib/services/admin_service.dart (add createParticipant + tokenCardUrl), mobile/lib/screens/admin/admin_participants_screen.dart (add FAB + _showCreateDialog + _CreateParticipantDialog), prd.json (US-036 passes=true)
- **Learnings for future iterations:**
  - `DropdownButtonFormField.value` is deprecated in Flutter ≥3.33.0 — use `DropdownButton` inside `InputDecorator` + `DropdownButtonHideUnderline` for a styled dropdown without deprecation warnings
  - `launchUrl(uri, mode: LaunchMode.externalApplication)` opens a URL in the device browser; must call `canLaunchUrl(uri)` first
  - `url_launcher: ^6.3.0` resolves to 6.3.2 and works fine with existing pubspec constraints
  - Dialog loading state: replace button label with `SizedBox(16x16, CircularProgressIndicator(strokeWidth:2))` and set all interactive widgets `onChanged: _loading ? null : ...` to disable them
  - dart analyze lib/ passes with no issues; no browser tools available — manual verification needed
---

## 2026-03-15 - US-035
- What was implemented: Created `mobile/lib/screens/admin/admin_participants_screen.dart` (AdminParticipantsScreen — ConsumerStatefulWidget; fetches GET /api/v1/admin/participants via AdminService; DataTable with columns Username/Group/Enrolled/Last Active/Surveys%; search field filters by username case-insensitively; DropdownButton group filter All/G1-G4; pagination 50 rows/page with Previous/Next TextButton.icon controls showing "from–to of total" label; row tap calls context.push('/admin/participants/:id'); error view with Retry; _FilterBar/_ParticipantsTable/_PaginationBar/_ErrorView private widget classes); created `mobile/lib/models/admin_participant.dart` (AdminParticipant with id/username/group/enrolledAt/lastActiveAt/surveysCompleted/surveysTotal; fromJson handles userId||_id fallback); created `mobile/lib/services/admin_service.dart` (AdminService + adminServiceProvider — fetchParticipants() calls GET /api/v1/admin/participants with Bearer token); updated `mobile/lib/main.dart` (import AdminParticipantsScreen; /admin route uses AdminParticipantsScreen; nested route /admin/participants/:id uses _PlaceholderScreen for US-039).
- Files changed: mobile/lib/screens/admin/admin_participants_screen.dart (new), mobile/lib/models/admin_participant.dart (new), mobile/lib/services/admin_service.dart (new), mobile/lib/main.dart (import + route wiring), prd.json (US-035 passes=true)
- **Learnings for future iterations:**
  - Admin screens live in `mobile/lib/screens/admin/` directory; create it with mkdir -p before first file
  - Nested GoRoute under /admin: `routes: [GoRoute(path: 'participants/:id', ...)]` — path is relative (no leading slash) when nested
  - `WidgetStateProperty.all(color)` (not deprecated MaterialStateProperty) is the M3 API for DataTable headingRowColor
  - DataRow.onSelectChanged triggers on tap — use this instead of GestureDetector to navigate from DataTable rows
  - `TextButton.icon` with `iconAlignment: IconAlignment.end` places icon after label (useful for Next button)
  - dart analyze lib/ passes with no issues; no browser tools available — manual verification needed
---

## 2026-03-15 - US-034
- What was implemented: Added 5 admin survey CRUD endpoints to `app/routes/adminRouter.js` (GET /surveys — lists all surveys; POST /surveys — creates with title/type required, defaults status='draft', assignedGroups=[]; PUT /surveys/:id — updates any field; PATCH /surveys/:id/status — validates published|archived|draft; PATCH /surveys/:id/groups — validates G1-G4 array); converted `app/routes/surveyRouter.js` to export both legacy static router (default) and new `createSurveyRouter({db})` factory; factory GET / returns published+group-filtered surveys for participants (looks up participant's group from MongoDB participants collection via req.user.sub), all surveys for admin/researcher; GET /:id returns survey by id; POST /:id/results stores survey_responses; updated `app/routes/v1Router.js` to import `createSurveyRouter` and pass `db`; created 20 integration tests in `app/tests/integration/admin.surveys.test.js` covering all auth/CRUD/group-filter cases.
- Files changed: app/routes/adminRouter.js (5 new admin survey routes), app/routes/surveyRouter.js (add createSurveyRouter factory), app/routes/v1Router.js (use createSurveyRouter), app/tests/integration/admin.surveys.test.js (new), prd.json (US-034 passes=true)
- **Learnings for future iterations:**
  - `GET /api/v1/surveys` list endpoint should return `[]` in catch (not 500) so auth.routes.test.js doesn't break when no DB is provided — auth tests only check role enforcement, not DB results
  - To convert a static router to a factory: keep the old static export as default for backward compat, add named export `createSurveyRouter` for new usage
  - Participant group filtering: look up group from `participants` collection using `req.user.sub`; filter surveys by `assignedGroups: group` (MongoDB finds docs where array contains the value)
  - 158/158 tests run; 151 pass; 4 pre-existing failures (Backup, Fuseki, Neo4j, LibreTranslate); 20 new tests all green
---

## 2026-03-15 - US-033
- What was implemented: Extended `app/routes/adminRouter.js` with 5 new endpoints: `GET /participants/:id/progress` (returns {profile, surveys[], habitsCount, recommendations:{accepted,dismissed}, timeline[]} — queries MongoDB survey_responses/habit_donations/recommendations_log collections, uses neo4jRun if injected for habitsCount, builds sorted timeline); `GET /habits/feed/export?format=csv` (streams CSV of habit_donations with optional group/from/to/category filters); `GET /habits/feed` (returns paginated {total, page, limit, results[]} from habit_donations with same filters); `GET /sessions` (delegates to keycloak.listSessions()); `DELETE /sessions/:sessionId` (delegates to keycloak.revokeSession()); added `listSessions()`+`revokeSession()` methods to production Keycloak client; created `app/tests/integration/admin.progress.test.js` with 18 tests covering: 401 auth enforcement for all 5 endpoints, 403 participant role checks, progress structure/404/researcher, feed pagination/group-filter/category-filter, CSV export/400 invalid format, sessions list/revoke/researcher.
- Files changed: app/routes/adminRouter.js (new Keycloak methods + 5 new routes), app/tests/integration/admin.progress.test.js (new), prd.json (US-033 passes=true)
- **Learnings for future iterations:**
  - Register `GET /habits/feed/export` BEFORE `GET /habits/feed` in Express — different paths, no conflict in practice, but ordering matters conceptually for readability
  - Mock DB `find()` must handle string-equality filters (participantId, group, category) in addition to `deletedAt.$exists` — add each filter field explicitly in the loop
  - `_seed(colName, docs)` helper on mockDb avoids needing insertOne calls in `before()` — simpler seeding for read-heavy tests
  - For CSV export: escape values containing commas/quotes/newlines; `donatedAt instanceof Date ? .toISOString() : donatedAt` handles both Date objects and ISO strings
  - Keycloak mock `revokeSession` pattern: maintain a `revokedSessions` Set in the mock closure; `listSessions` filters them out — allows session revoke test to verify subsequent list
  - 130/134 tests pass; 4 pre-existing failures (Backup, Fuseki, Neo4j, LibreTranslate) remain; 18 new tests all green
---

## 2026-03-15 - US-032
- What was implemented: Added `seedDefaultSettings` function to `app/routes/adminRouter.js` (inserts `{key:'token_card_format', value:'both', updatedAt}` into `admin_settings` collection on router creation if not present); added `GET /api/v1/admin/settings` endpoint (returns all settings as flat `{key: value}` object); added `PUT /api/v1/admin/settings/:key` endpoint (validates `value` field present, upserts `{key, value, updatedAt}`, returns `{ok:true, key, value}`); both endpoints require admin or researcher role (inherited from v1Router middleware); created `app/tests/integration/admin.settings.test.js` with 11 tests covering: 401/403 auth enforcement for GET+PUT, default seed check (token_card_format=both), update existing setting, GET reflects updated value, upsert new key, GET includes upserted key, 400 for missing value, researcher role allowed.
- Files changed: app/routes/adminRouter.js (add seedDefaultSettings + GET /settings + PUT /settings/:key), app/tests/integration/admin.settings.test.js (new), prd.json (US-032 passes=true)
- **Learnings for future iterations:**
  - `getDb().then(seedDefaultSettings).catch(() => {})` in the router factory async-seeds defaults without blocking router creation; safe because Express won't route requests until the server is listening (after await in tests)
  - `updateOne({key}, {$set: {...}}, {upsert: true})` is the standard MongoDB pattern for settings upsert; mock must handle the `upsert` option to insert when no match found
  - For settings mock DB, `findOne` must filter by `query.key` (not `query.userId`); generic mock that handles both `key` and `userId` filters avoids two separate mocks
  - 119/119 tests run (112 pass, 4 pre-existing failures: Backup, Fuseki, Neo4j, LibreTranslate); 11 new tests all green
---

## 2026-03-15 - US-031
- What was implemented: Created `app/services/token_card_service.js` (generateTokenCard(userId, username, password, format) — generates a PDF buffer using pdfkit + qrcode; QR encodes `hhh://login?user=<username>&token=<password>`; PDF layout: title header, participant ID, QR image (format=qr|both), username/password block (format=print|both), footer disclaimer); updated `app/routes/adminRouter.js` to accept injectable `tokenCardService`, store password in MongoDB on POST (required for GET token-card re-generation), add `tokenCardUrl` to POST response, add `GET /participants/:id/token-card` endpoint (fetches participant+password from DB, calls generateTokenCard, returns PDF stream with content-type application/pdf); updated `app/routes/v1Router.js` to pass `tokenCardService` to createAdminRouter; added 5 integration tests to `admin.participants.test.js` (POST includes tokenCardUrl, GET returns PDF, 404 for unknown, 400 for invalid format, 401 without token); fixed mock `findOne` to also filter by `deletedAt.$exists === false`; created 5 unit tests in `tests/unit/token_card_service.test.js` (format=both/qr/print produce valid %PDF buffers, both > print due to QR image, both >= qr).
- Files changed: app/services/token_card_service.js (new), app/routes/adminRouter.js (add tokenCardService injection + store password + tokenCardUrl + GET token-card endpoint), app/routes/v1Router.js (pass tokenCardService), app/tests/integration/admin.participants.test.js (mock updates + 5 new tests), app/tests/unit/token_card_service.test.js (new), app/package.json (add pdfkit + qrcode), prd.json (US-031 passes=true)
- **Learnings for future iterations:**
  - pdfkit is a stream-based PDF library; collect Buffer via `doc.on('data', chunks.push)` + `doc.on('end', resolve(Buffer.concat(chunks)))`
  - qrcode.toBuffer(data, {type: 'png', width, margin}) returns a PNG Buffer — pass directly to pdfkit's `doc.image(buffer, x, y, {width, height})`
  - pdfkit `doc.y` tracks current vertical position; after `doc.image()` manually advance `doc.y += qrSize + padding` since image() doesn't auto-advance cursor
  - Store participant password in MongoDB (plaintext) to enable idempotent token-card re-generation; acceptable for a medical study token/credential card use case
  - Inject tokenCardService via factory param using same pattern as keycloak/neo4jRun — allows mock in tests without hitting real PDF generation overhead for non-PDF tests
  - Mock `findOne` must check `deletedAt.$exists === false` filter (same as `find()`) to return null for soft-deleted docs
  - 101/108 tests pass; 4 pre-existing failures remain (Backup, Fuseki, Neo4j, LibreTranslate)
---

## 2026-03-15 - US-030
- What was implemented: Created `app/routes/adminRouter.js` (createAdminRouter factory — GET / base route {ok:true}; GET /participants lists non-deleted participants from MongoDB participants collection; POST /participants generates p-<uuid> username + random 12-byte base64url password, calls injectable Keycloak client createUser+assignRole, stores doc in MongoDB, returns {userId, username, password}; PATCH /participants/:id/group validates G1-G4, calls kc.updateUserAttribute, updates MongoDB, optionally updates Neo4j hhh__Donor node labels via injectable neo4jRun; DELETE /participants/:id soft-deletes by setting deletedAt + anonymizing username to deleted-<sha256[:8]>); updated `app/routes/v1Router.js` to import createAdminRouter, accept keycloak param, replace placeholder admin handler with real router; created 12 integration tests in `app/tests/integration/admin.participants.test.js` (401/403 auth enforcement for GET+POST, empty list, create+list, update group, 400 for invalid group, 404 for unknown participant on PATCH, soft-delete, 404 on double-delete, deleted not in list).
- Files changed: app/routes/adminRouter.js (new), app/routes/v1Router.js (import + keycloak param + real admin router), app/tests/integration/admin.participants.test.js (new), prd.json (US-030 passes=true)
- **Learnings for future iterations:**
  - `catch {}` (no binding) avoids ESLint no-unused-vars on error variables — use throughout instead of `catch (_err)`
  - Mock DB `find()` must be synchronous (not async) so the chained `.toArray()` call works: `find(query) { ...; return { async toArray() {...} }; }` — making it async would return a Promise without .toArray()
  - Keycloak admin client is fully injectable via `keycloak` param to createAdminRouter; production client calls /admin/realms/{realm}/users REST API with client_credentials token
  - createAdminRouter needs a GET / base handler (returns {ok:true}) so auth.routes.test.js GET /api/v1/admin → 200 test continues to pass
  - 91/98 tests pass; 4 pre-existing failures (Backup, Fuseki, Neo4j, LibreTranslate) remain
---

## 2026-03-15 - US-029
- What was implemented: Created `mobile/lib/models/habit_stats.dart` (HabitStats with total/byCategory/byDay fields; CategoryCount and DayCount value types; fromJson factories); added `fetchStats()` to `mobile/lib/services/habit_service.dart` (GET /api/v1/habits/stats, returns HabitStats); created `mobile/lib/screens/stats_screen.dart` (ConsumerStatefulWidget — fetches stats on init, re-fetches via Timer every 60s, cancelled in dispose; _loading guards first fetch only so refresh doesn't blank screen; _TotalCard shows large primary-colored count with volunteer_activism icon; _CategoryBarChart (BarChart from fl_chart) — BarChartGroupData per category, left axis count labels, rotated bottom category labels, touch tooltip with category name + count; _DayLineChart (LineChart) — FlSpot per day index, line + filled area below, bottom axis shows MM-DD at ~5 evenly-spaced ticks, touch tooltip with date + count; _StatsSkeleton shows grey placeholder boxes for loading state); updated `mobile/lib/screens/explore_screen.dart` (added import stats_screen.dart; wrapped Scaffold in DefaultTabController(length:2); AppBar gets bottom: TabBar with Graph/Stats tabs; body is TabBarView with existing graph body + StatsScreen()).
- Files changed: mobile/lib/models/habit_stats.dart (new), mobile/lib/services/habit_service.dart (add fetchStats), mobile/lib/screens/stats_screen.dart (new), mobile/lib/screens/explore_screen.dart (add DefaultTabController + TabBar + TabBarView), prd.json (US-029 passes=true)
- **Learnings for future iterations:**
  - `Timer.periodic` in initState + cancel in dispose is the correct Flutter pattern for polling; set `_loading = _stats == null` so re-polls don't show skeleton
  - fl_chart 0.69 BarTouchTooltipData uses `getTooltipColor` (function returning Color) not deprecated `tooltipBgColor`; same for LineTouchTooltipData
  - RotatedBox(quarterTurns: 1) inside getTitlesWidget rotates bottom axis labels 90° — useful for long category names
  - LineChartBarData.belowBarData with BarAreaData(show: true, color: primary.withAlpha(40)) adds subtle fill under line
  - DefaultTabController wraps the entire Scaffold (not just AppBar) so TabBarView in the body can find the controller
  - `bottom: TabBar(tabs: [...])` inside AppBar sets the tab bar below the title bar
  - dart analyze lib/ passes with no issues
  - Browser verification: no browser tools available — manual verification needed
---

## 2026-03-15 - US-028
- What was implemented: Created `mobile/lib/services/habit_service.dart` (HabitService — fetchPublicHabits() GET /api/v1/habits/public, annotateHabit(id, type) POST /api/v1/habits/:id/annotate returning updated annotationCounts; habitServiceProvider); modified `mobile/lib/widgets/habit_graph_widget.dart` (added optional `onNodeTap` callback, _canvasSize tracking via LayoutBuilder, tap detection via accumulated-movement heuristic in onScaleStart/Update/End — _scaleLocalStart + _moved flag, _handleTapAt converts local→graph space and hit-tests each node with r+4 slop); created `mobile/lib/screens/explore_screen.dart` (ConsumerStatefulWidget — fetches habits via HabitService on init; _filteredHabits getter filters by _selectedCategory; HabitGraphWidget with onNodeTap → _showNodeDetail; _CategoryFilterBar horizontal FilterChip list with toggle; _showNodeDetail shows modal bottom sheet via StatefulBuilder — drag handle, habit name, category+BCIO Chip, annotation counts row, OutlinedButton 'I do this too' and FilledButton 'Helpful' both call annotateHabit and update _allHabits + bottom sheet state; error state with retry, loading spinner, empty state); updated `mobile/lib/main.dart` to import ExploreScreen and replace _PlaceholderScreen on /explore route.
- Files changed: mobile/lib/services/habit_service.dart (new), mobile/lib/widgets/habit_graph_widget.dart (add onNodeTap + LayoutBuilder + tap detection), mobile/lib/screens/explore_screen.dart (new), mobile/lib/main.dart (import + route update), prd.json (US-028 passes=true)
- **Learnings for future iterations:**
  - Tap detection in a GestureDetector with onScale*: track _scaleLocalStart in onScaleStart and _moved flag in onScaleUpdate (set true if panDelta.distance > 6 or scale deviates > 0.05); in onScaleEnd, if !_moved call the tap handler with _scaleLocalStart
  - LayoutBuilder inside GestureDetector child gives the actual render constraints; save to _canvasSize field to convert localPos → graph coordinates in tap handler
  - Graph→screen transform: center = (w/2, h/2) + _panOffset; graphPos = (localPos - center) / _zoom — invert to find graph-space coordinates from a screen tap
  - StatefulBuilder inside showModalBottomSheet enables updating sheet UI independently of the parent screen (e.g., after annotation); call both setState (parent) and setSheetState (sheet) to keep both in sync
  - FilterChip.selected + onSelected: use a nullable _selectedCategory; tapping same chip deselects (toggles null)
  - dart analyze lib/ passes with no issues
  - Browser verification: no browser tools available — manual verification needed
---

## 2026-03-15 - US-027
- What was implemented: Created `mobile/lib/models/habit_node.dart` (HabitNode with id/name/category/bcioClass/annotationCounts fields, totalAnnotations getter, fromJson factory); created `mobile/lib/widgets/habit_graph_widget.dart` (HabitGraphWidget StatefulWidget — AnimationController with repeat() drives 60fps physics via addListener(_stepPhysics); _stepPhysics applies Coulomb repulsion between all node pairs + spring attraction between same-category pairs + center gravity, Euler integration with velocity damping; CustomPainter _GraphPainter draws same-category edges as light lines then nodes as circles colored by category from _kCategoryColors map + abbreviated text labels; GestureDetector onScaleStart/Update/End handles both pan and pinch-to-zoom with _panOffset/_zoom viewport transform; node radius = clamp(8 + total/10*16, 8, 24) px).
- Files changed: mobile/lib/models/habit_node.dart (new), mobile/lib/widgets/habit_graph_widget.dart (new), prd.json (US-027 passes=true)
- **Learnings for future iterations:**
  - AnimatedBuilder(animation: _ticker, builder: ...) + addListener(_stepPhysics) pattern: physics step runs first in listener, then AnimatedBuilder rebuilds on same tick — no setState needed for physics updates
  - For same-category spring model without explicit edge data, use `nodes[i].category == nodes[j].category` to create implicit edges during force accumulation
  - `AnimationController(vsync: this, duration: const Duration(days: 1))..repeat()` runs indefinitely at 60fps — simpler than using a raw Ticker
  - onScaleUpdate handles both pan and pinch simultaneously; track _focalPoint delta for pan, _baseZoom * details.scale for zoom
  - _NodeState is a mutable helper (not a Flutter widget state) — safe to update in an animation listener
  - `color.withAlpha(180)` for border paint; reset strokeWidth=0 and style=fill after border pass to avoid leaking state into next node
  - dart analyze lib/ passes with no issues
  - Browser verification: no browser tools available — manual verification needed
---

## 2026-03-15 - US-026
- What was implemented: Created `app/routes/habitsRouter.js` (createHabitsRouter factory — GET / base route returning {ok:true}, GET /public queries Neo4j via injectable neo4jRun for all hhh__Habit nodes, strips userId/source, merges annotation counts from MongoDB habit_annotations collection; POST /:id/annotate stores anonymous {habitId, type, createdAt} document in habit_annotations (no userId), returns updated counts; GET /stats returns {total, byCategory, byDay} using Neo4j for total/byCategory and MongoDB for byDay from last 30 days); updated `app/routes/v1Router.js` to import createHabitsRouter, accept neo4jRun param, replace placeholder habits handler with real router; created 12 integration tests in `app/tests/integration/habits.routes.test.js` (401/403 auth checks for all 3 endpoints, 400 for invalid annotate type, 200+body checks for all 3 happy paths).
- Files changed: app/routes/habitsRouter.js (new), app/routes/v1Router.js (import + neo4jRun param + mount habitsRouter), app/tests/integration/habits.routes.test.js (new), prd.json (US-026 passes=true)
- **Learnings for future iterations:**
  - Injectable neo4jRun pattern: `async function queryNeo4j(cypher, params)` — if neo4jRun injected use it directly (returns Array<Object>); otherwise create driver, run, map records via `r.toObject()`, then close driver/session
  - Neo4j integer fields from toObject() may be neo4j.Integer objects — always use a `toNumber(val)` helper that checks `typeof val.toNumber === 'function'`
  - `catch {}` (no binding) is valid ESLint-safe syntax for catch blocks where the error isn't used; avoids no-unused-vars lint error
  - Habit nodes in Neo4j are labelled `hhh__Habit` (n10s SHORTEN mode); property hhh__id = UUID, hhh__value = habit text, hhh__source = userId (strip this for public endpoint)
  - Auth test in auth.routes.test.js checks `GET /api/v1/habits` → 200; habitsRouter must have a GET / handler or this breaks
  - 79/83 tests pass; 4 pre-existing failures (Backup, Fuseki, Neo4j, LibreTranslate) remain
---

## 2026-03-15 - US-025
- What was implemented: Added `web_socket_channel: ^3.0.0` to mobile/pubspec.yaml; created `mobile/lib/services/recommendation_ws_service.dart` (RecommendationWsService — connects to wss://api.hhh.tu-dresden.de/ws/recommendations, sends JWT as first message, tracks reconnect via Timer(_reconnectDelay, _connect), polls RecommendationService every 30s as fallback, exposes `Stream<Recommendation>` and `Stream<WsConnectionStatus>`, `markSeen(ids)` to deduplicate initial batch vs. poll results; `recommendationWsServiceProvider` family provider keyed by userId); updated `mobile/lib/screens/recommend_screen.dart` (initWs() called after initial fetch — subscribes to WS streams, _insertAtTop inserts new card at index 0 via AnimatedList.insertItem with 300ms animation, _ConnectionDot widget in AppBar shows green dot when WS connected, grey when polling, stream subscriptions cancelled in dispose).
- Files changed: mobile/lib/services/recommendation_ws_service.dart (new), mobile/lib/screens/recommend_screen.dart (updated), mobile/pubspec.yaml (add web_socket_channel), mobile/pubspec.lock (updated), prd.json (US-025 passes=true)
- **Learnings for future iterations:**
  - `web_socket_channel` ^3.0.0 exposes `channel.ready` (Future) — await it before sending first message; not available in ^2.x
  - WS family provider: `Provider.family<Service, String>((ref, userId) {...})` — each userId gets its own instance, auto-disposed when no listeners
  - `markSeen(ids)` pattern: pre-populate seen-IDs set with IDs from the initial HTTP fetch so the polling fallback doesn't re-emit the same cards
  - `AnimatedList.insertItem(0)` inserts at top of list; pair with `_recommendations.insert(0, rec)` + `_controllers.insert(0, controller)` in the same setState
  - `_ConnectionDot` widget with `Tooltip` renders green/grey dot in AppBar — simple yet visible connection status indicator
  - dart analyze lib/ passes with no issues after these changes
  - Browser verification: no browser tools available — manual verification needed
---

## 2026-03-15 - US-024
- What was implemented: Added `ws` package to dependencies; created `app/ws/recommendationWs.js` (createRecommendationWsServer — noServer WebSocketServer, upgrade handler for path /ws/recommendations, 5s auth timeout, JWT first-message auth via injected verifyToken, connections Map<userId, Set<WebSocket>>, broadcast function); created `app/routes/internalRouter.js` (POST /recommendations — validates userId+recommendation, calls broadcast, returns {ok:true}); added `createTokenVerifier` export to `app/middleware/auth.js` (standalone async JWT verifier reusing parseJwt+verifyJwtSignature, with JWKS caching); updated `app/app.js` to create explicit HTTP server via `createServer(app)`, attach WS server, mount internal router; created 6 integration tests in `app/tests/integration/ws.recommendations.test.js` (5s disconnect timeout, invalid token disconnect, authenticated socket receives broadcast, 400 for missing userId, no-op for unknown userId, unknown path rejected).
- Files changed: app/ws/recommendationWs.js (new), app/routes/internalRouter.js (new), app/middleware/auth.js (add createTokenVerifier export), app/app.js (use createServer + attach WS + mount internal router), app/tests/integration/ws.recommendations.test.js (new), app/package.json (add ws), app/package-lock.json (updated), prd.json (US-024 passes=true)
- **Learnings for future iterations:**
  - Use `WebSocketServer({ noServer: true })` + `httpServer.on('upgrade', ...)` pattern to colocate WS with Express HTTP server on same port; path check in upgrade handler, `socket.destroy()` for unrecognized paths
  - WS auth pattern: start 5s timer in `connection` handler, expect JWT as first message, call verifyToken, clearTimeout on success, ws.terminate() on failure
  - `createTokenVerifier` added to auth.js provides standalone JWT verification (no Express middleware dependency) — use for WS and other non-HTTP contexts
  - Internal broadcast endpoint (/api/internal/recommendations) is unauthenticated by design (internal service only) — kept off JWT-protected /api/v1 prefix
  - Node.js v25 has built-in WebSocket global but using `ws` package's `WebSocket` in tests is cleaner for import
  - 70/77 tests pass; 4 pre-existing failures (Backup, Fuseki, Neo4j, LibreTranslate) remain
---

## 2026-03-15 - US-023
- What was implemented: Created mobile/lib/screens/recommend_screen.dart (ConsumerStatefulWidget with TickerProviderStateMixin — fetches recommendations via RecommendationService on init; staggered entry with FadeTransition+SlideTransition (300ms, 80ms stagger between cards) driven by per-card AnimationControllers; AnimatedList for card removal (removeItem with 200ms out-animation); loading skeleton (_SkeletonCard placeholder cards); empty state message 'No recommendations yet — complete your profile first'; error state with retry button). Added userIdProvider (FutureProvider<String?>) to auth_provider.dart that extracts `sub` claim from JWT. Updated main.dart to import and use RecommendScreen at /recommend route.
- Files changed: mobile/lib/screens/recommend_screen.dart (new), mobile/lib/providers/auth_provider.dart (add userIdProvider), mobile/lib/main.dart (import + route update), prd.json (US-023 passes=true)
- **Learnings for future iterations:**
  - AnimatedList initialItemCount items receive animation=AlwaysStoppedAnimation(1.0) — must use own AnimationControllers for staggered entry of initial items
  - For AnimatedList removeItem builder, capture removed item and its animation separately; the live list/controllers are already updated by the time builder runs
  - `TickerProviderStateMixin` (not `SingleTickerProviderStateMixin`) required when creating multiple AnimationControllers
  - userIdProvider extracts `sub` claim from JWT with same base64 decode pattern as userRolesProvider
  - Lint: avoid `__` (double underscore) in itemBuilder params; use named single-letter vars like `(context, i)`
  - dart analyze lib/ passes with no issues after these changes
  - Browser verification: no browser tools available — manual verification needed
---

## 2026-03-15 - US-022
- What was implemented: Created mobile/lib/models/recommendation.dart (RagCitation with sourceTitle/excerpt, Recommendation with id/habitName/category/confidenceScore/rationale/ragCitation?); created mobile/lib/services/recommendation_service.dart (RecommendationService — fetchRecommendations(userId), acceptRecommendation(id), dismissRecommendation(id) via Dio; DI pattern matching SurveyService; recommendationServiceProvider); created mobile/lib/widgets/recommendation_card.dart (RecommendationCard StatelessWidget — large habitName, category Chip, LinearProgressIndicator for confidenceScore, 2-line rationale, optional ExpansionTile for ragCitation, Accept/Dismiss IconButton footer).
- Files changed: mobile/lib/models/recommendation.dart (new), mobile/lib/services/recommendation_service.dart (new), mobile/lib/widgets/recommendation_card.dart (new), prd.json (US-022 passes=true)
- **Learnings for future iterations:**
  - RecommendationService follows same DI pattern as SurveyService: Dio? + AuthService injected, default Dio() for production
  - LinearProgressIndicator with ClipRRect(borderRadius) gives a rounded confidence bar; .clamp(0.0, 1.0) guards against out-of-range scores
  - ExpansionTile with tilePadding: EdgeInsets.zero removes default horizontal padding for tight card layouts
  - surfaceContainerHighest is the correct M3 token for muted track color (avoids deprecated surfaceVariant)
  - dart analyze lib/ passes with no issues after these changes
---

## 2026-03-15 - US-021
- What was implemented: Created app/routes/profileRouter.js with createProfileRouter factory — GET / returns profile from MongoDB profiles collection (404 if not found); POST / upserts profile doc with userId=req.user.sub, answers, completedAt, updatedAt; db injectable for tests. Updated v1Router.js to import createProfileRouter and accept db param, mounted /profile with participant|admin|researcher role check. Created 7 integration tests in tests/integration/profile.routes.test.js using in-memory mock DB (Map-based store with findOne/updateOne simulation).
- Files changed: app/routes/profileRouter.js (new), app/routes/v1Router.js (import + mount profileRouter + accept db param), app/tests/integration/profile.routes.test.js (new), prd.json (US-021 passes=true)
- **Learnings for future iterations:**
  - profileRouter follows same DI pattern as recommendRouter: accepts { db } option; falls back to `import('../models/survey.js').connect()` in production
  - For test mocking, a simple Map-based mock db with findOne/updateOne works without any external mock library
  - createV1Router now accepts `db` param alongside jwksUrl/serviceChecks/recommenderUrl — pass it through to profileRouter
  - 4 pre-existing test failures remain (Backup alert, Fuseki, Neo4j, LibreTranslate) — 64/71 tests pass
---

## 2026-03-15 - US-020
- What was implemented: Created mobile/lib/screens/profile_screen.dart with ConsumerStatefulWidget — checks if profile already exists via GET /api/v1/profile (validateStatus accepts non-5xx); if 200, shows _ProfileSummaryCard with formatted completion date and Edit button; if 404/other, loads profile survey WebView via SurveyService.fetchSurvey('profile') with JWT in loadRequest headers; JS hook uses ProfileSurveyComplete JavaScriptChannel (separate from DonateScreen's SurveyComplete); on completion POSTs /api/v1/profile with answers + completedAt, shows SnackBar, returns to summary card; offline state shows _OfflineBanner with retry; Edit button re-invokes _initSurvey(). Updated main.dart to import ProfileScreen and replace _PlaceholderScreen on /profile route.
- Files changed: mobile/lib/screens/profile_screen.dart (new), mobile/lib/main.dart (import + route update), prd.json (US-020 passes=true)
- **Learnings for future iterations:**
  - For profile GET, use validateStatus: (status) => status != null && status < 500 so Dio treats 404 as a response (not exception)
  - POST /api/v1/profile sends { answers: {...}, completedAt: ISO8601 } — backend is responsible for linking to req.user.sub
  - Each WebView that fires JS messages needs its own uniquely-named JavaScriptChannel to avoid cross-screen conflicts
  - `dart analyze lib/` with no issues requires all declared fields to be referenced — add guard `if (_submitting || _surveyId == null) return;` to use _surveyId
  - `dart analyze lib/` passes with no issues after these changes
---

## 2026-03-15 - US-019
- What was implemented: Created mobile/lib/screens/donate_screen.dart with ConsumerStatefulWidget — fetches habit-donation survey via SurveyService.fetchSurvey('habit-donation'); loads /api/v1/surveys/:id/render in a WebView (webview_flutter ^4.10.0) with JWT Authorization header injected via loadRequest headers; injects JS after page load to hook SurveyJS onComplete event and forward answers to Flutter via SurveyComplete JavaScriptChannel; on completion calls SurveyService.submitResult(), shows SnackBar toast, pops via context.pop(); offline/error state shows 'No connection' banner with retry button. Updated main.dart to import DonateScreen and replace _PlaceholderScreen on /donate branch. Added webview_flutter: ^4.10.0 to pubspec.yaml.
- Files changed: mobile/lib/screens/donate_screen.dart (new), mobile/lib/main.dart (import + route update), mobile/pubspec.yaml (add webview_flutter), mobile/pubspec.lock (updated), prd.json (US-019 passes=true)
- **Learnings for future iterations:**
  - webview_flutter ^4.x: use WebViewController.loadRequest(Uri, headers: Map<String, String>) to inject JWT into the initial page request
  - addJavaScriptChannel('ChannelName', onMessageReceived: ...) adds a global JS object so SurveyJS/page can call ChannelName.postMessage(data)
  - Inject JS via runJavaScript() in onPageFinished callback (NavigationDelegate); use setInterval to poll until SurveyJS 'survey' variable is initialized before hooking onComplete
  - Offline pattern: catch exception in initState, set _offline=true, show banner with retry — retry resets state and re-calls _initSurvey()
  - dart analyze lib/ passes with no issues after these changes
---

## 2026-03-15 - US-018
- What was implemented: Created mobile/lib/models/survey.dart (Survey with id/title/type/jsonSchema/assignedGroups/status, fromJson/toJson); created mobile/lib/models/survey_result.dart (SurveyResult with surveyId/userId/answers/completedAt, fromJson/toJson); created mobile/lib/services/survey_service.dart (SurveyService using dio — fetchSurvey(id), fetchProfileSurvey() calls fetchSurvey('profile'), submitResult(surveyId, answers) POST to /api/v1/surveys/:id/results; auth headers injected from AuthService; exposed as surveyServiceProvider Riverpod Provider).
- Files changed: mobile/lib/models/survey.dart (new), mobile/lib/models/survey_result.dart (new), mobile/lib/services/survey_service.dart (new), prd.json (US-018 passes=true)
- **Learnings for future iterations:**
  - SurveyService follows the same DI pattern as AuthService: injectable Dio and AuthService via constructor, with defaults for production use
  - fetchProfileSurvey() is a convenience wrapper: calls fetchSurvey('profile') — backend resolves the profile survey by userId from auth context
  - submitResult posts to /api/v1/surveys/:id/results (not /:id/complete — PRD specifies /results endpoint)
  - Provider<SurveyService> uses ref.watch(authServiceProvider) to get AuthService — same pattern as other providers in auth_provider.dart
  - `dart analyze lib/` passes with no issues after these additions
---

## 2026-03-15 - US-017
- What was implemented: Created mobile/lib/screens/shell_screen.dart with ConsumerWidget — StatefulNavigationShell from go_router for state-preserving tab navigation; bottom NavigationBar with 5 tabs (Donate/volunteer_activism, Explore/hub, Recommend/recommend, Profile/person, Admin/admin_panel_settings); Admin tab visible only when userRolesProvider contains 'admin' or 'researcher'; active tab highlighted via NavigationBar's indicatorColor from brand colorScheme.primaryContainer; added userRolesProvider (FutureProvider<List<String>>) to auth_provider.dart that decodes the JWT access token and extracts realm_access.roles; updated main.dart with StatefulShellRoute.indexedStack() with 5 branches (donate/explore/recommend/profile/admin) each using placeholder screens; removed _HomeShellPlaceholder; login still routes to /login, post-login navigates to /donate.
- Files changed: mobile/lib/screens/shell_screen.dart (new), mobile/lib/providers/auth_provider.dart (add userRolesProvider), mobile/lib/main.dart (StatefulShellRoute replaces placeholder /home route), prd.json (US-017 passes=true)
- **Learnings for future iterations:**
  - StatefulShellRoute.indexedStack() is the go_router API for state-preserving tab navigation; `navigationShell.goBranch(index)` switches tabs
  - Pass `initialLocation: branchIndex == navigationShell.currentIndex` to goBranch to pop to root when re-tapping current tab
  - JWT role decoding: split on '.', base64Url.normalize(parts[1]) handles missing padding, then utf8.decode + jsonDecode; realm_access.roles is the Keycloak claim
  - ConsumerWidget (not ConsumerStatefulWidget) is sufficient when you only need Riverpod watch, no local setState
  - `dart analyze lib/` passes with no issues after these changes
---

## 2026-03-14 - US-016
- What was implemented: Created mobile/lib/screens/login_screen.dart with ConsumerStatefulWidget — HHH logo (health_and_safety icon), username/password TextFields, loading CircularProgressIndicator, error message display, login via authServiceProvider (navigates to /home on success); updated mobile/lib/main.dart with ProviderScope, go_router (_router with /login and /home routes), deep link pre-fill via GoRouterState.uri.queryParameters['user'/'token'], MaterialApp.router with teal colorScheme.
- Files changed: mobile/lib/screens/login_screen.dart (new), mobile/lib/main.dart (rewritten), prd.json (US-016 passes=true)
- **Learnings for future iterations:**
  - go_router deep link query params read via `state.uri.queryParameters['key']` in the route builder — pass to screen via constructor params
  - Use `ConsumerStatefulWidget` + `ConsumerState` for screens that need both local state and Riverpod providers
  - `MaterialApp.router` takes `routerConfig: _router` (GoRouter instance); `MaterialApp` with `home:` is incompatible with go_router
  - ProviderScope must wrap the entire app at the top level in main(); placing it as `ProviderScope(child: HhhApp())` works
  - `dart analyze lib/` passes with no issues after the rewrite
---

## 2026-03-13 - US-015
- What was implemented: Created mobile/lib/services/auth_service.dart with AuthService class (PKCE redirect flow via flutter_appauth, tokens stored in flutter_secure_storage, auto-refresh when within 60s of expiry, login/refreshToken/getAccessToken/isLoggedIn/logout methods); created mobile/lib/providers/auth_provider.dart with authServiceProvider (Provider<AuthService>) and isLoggedInProvider (FutureProvider<bool>)
- Files changed: mobile/lib/services/auth_service.dart (new), mobile/lib/providers/auth_provider.dart (new), prd.json (US-015 passes=true)
- **Learnings for future iterations:**
  - flutter_appauth 8.x: `authorizeAndExchangeCode` returns `AuthorizationTokenResponse?`, `token()` returns `TokenResponse?`; AuthorizationTokenResponse extends TokenResponse so both can be handled by `_storeTokens(TokenResponse?)`
  - flutter_appauth does not support ROPC (resource owner password credentials) — use PKCE redirect flow always; loginHint can be passed for username pre-fill
  - Keycloak discovery URL pattern: `<base>/realms/<realm>/.well-known/openid-configuration`
  - `dart analyze lib/` must be run from the mobile/ directory (where pubspec.yaml is)
---

## 2026-03-13 - US-014
- What was implemented: Scaffolded full Flutter project in mobile/ using `flutter create --org de.tu-dresden --project-name hhh --platforms android,ios,web .`; updated pubspec.yaml (Flutter >=3.22.0, Dart >=3.10.0, added flutter_appauth, flutter_secure_storage, dio, flutter_riverpod, go_router, fl_chart); updated android/app/build.gradle.kts (applicationId de.tu-dresden.hhh, namespace de.tu_dresden.hhh); rewrote ios/Runner/Info.plist (CFBundleIdentifier de.tu-dresden.hhh + CFBundleURLSchemes [hhh]); updated web/index.html (theme-color meta + PWA manifest link); updated web/manifest.json (name Health Habit Hub, short_name HHH, display standalone); moved MainActivity.kt to de/tu_dresden/hhh/ package
- Files changed: mobile/pubspec.yaml, mobile/android/app/build.gradle.kts, mobile/android/app/src/main/kotlin/de/tu_dresden/hhh/MainActivity.kt (new), mobile/ios/Runner/Info.plist, mobile/web/index.html, mobile/web/manifest.json, plus all flutter create scaffolding files, prd.json (US-014 passes=true)
- **Learnings for future iterations:**
  - flutter create --org uses the org as-is but removes hyphens in namespace; applicationId CAN contain hyphens but namespace must be a valid Java package (use underscore)
  - Dart SDK constraint must be >=3.10.0 for the generated main.dart dot-shorthands feature; flutter SDK constraint goes under environment.flutter key
  - The mobile/ directory started with only a .env file; flutter create preserved it while generating full project structure
  - dart analyze must be run from within the mobile directory (or using `dart analyze .` from mobile) to analyze the Flutter project
  - Pre-existing test failures remain (4 out of ~61 tests): Backup alert, Fuseki, Neo4j, LibreTranslate
---

## 2026-03-13 - US-013
- What was implemented: Updated neo4j/init/constraints.cypher with group label constraint comment + Group1/Group2 indexes; created tests/ontology/test-ontology.sh (bash, chmod +x) with 5 tests: all 4 groups retrievable via Cypher, no donors without group, no orphaned habits, Fuseki Habit rdfs:label coverage, BCIO owl:Class count >= 100 in Fuseki; created .github/workflows/ci.yml with two jobs: test-node (npm test) and test-ontology (docker services for neo4j:5-community + stain/jena-fuseki, seeds minimal data, runs test-ontology.sh).
- Files changed: neo4j/init/constraints.cypher (group label constraint + Group1/Group2 indexes), tests/ontology/test-ontology.sh (new, executable), .github/workflows/ci.yml (new), prd.json (US-013 passes=true)
- **Learnings for future iterations:**
  - Neo4j CE does not support multi-label existence constraints natively — group label integrity is enforced via a documented check query in constraints.cypher, not a DB-level constraint
  - cypher-shell --format plain prints header row + data rows; use `tail -n +2 | grep -cv '^$'` to count data rows without header
  - stain/jena-fuseki Docker image uses ADMIN_PASSWORD and FUSEKI_DATASET_1 env vars; dataset is created automatically on startup
  - n10s graphconfig.init must be called before n10s.onto.import.fetch; it requires the n10s plugin installed
  - 4 pre-existing test failures remain (Backup alert, Fuseki, Neo4j, LibreTranslate) — 58/65 tests pass
---

## 2026-03-13 - US-012
- What was implemented: Merged BCIO (Behaviour Change Intervention Ontology) classes inline into Ontology.ttl and fuseki/init/Ontology.ttl. Added 119 BCIO classes: 10 core (BehaviourChangeIntervention, BehaviourChangeTechnique, InterventionFunction, PolicyCategory, ModeOfDelivery, Setting, SourceOfBehaviour, MechanismOfAction, BehaviourChangeWheel, OutcomeType), 16 BCT taxonomy groupings, and 93 BCT leaf techniques from BCT Taxonomy v1 (Michie et al. 2013). Total owl:Class count: 134 (up from 15). Uncertain HHH↔BCIO mappings annotated with rdfs:comment "TODO: domain-review" on 5 HHH classes (Behavior, Context, PhysicalSetting, InternalState, Reasoning). Added ontology header with rdfs:label and rdfs:comment.
- Files changed: Ontology.ttl (BCIO merge + TODO annotations), fuseki/init/Ontology.ttl (sync copy), prd.json (US-012 passes=true)
- **Learnings for future iterations:**
  - bcio.owl was not present in the repo (API-service/src/openapi_server/Ontologies/ doesn't exist); inline merge approach was used instead
  - concepts_all_merged.jsonl also absent — uncertain mappings were inferred from domain knowledge rather than the file
  - BCIO namespace used: http://humanbehaviourchange.org/ontology/BCIO# with readable class names (valid Turtle)
  - Turtle local names CAN start with digits (Turtle 1.1 spec), but using alpha-prefixed readable names is safer across parsers
  - BCT Taxonomy v1 has exactly 93 techniques in 16 groupings — all 93 added as owl:Class with rdfs:subClassOf their grouping class
  - 4 pre-existing test failures remain (Backup alert, Fuseki, Neo4j, LibreTranslate)
---

## 2026-03-13 - US-011
- What was implemented: Added `rdfs:label` to hhh:Group3 ("Group 3 (Full+Free-text)") and hhh:Group4 ("Group 4 (Minimal+Free-text)") in both Ontology.ttl files; created neo4j/init/constraints.cypher with uniqueness constraints and group-label index comments; created scripts/migrate-group-labels.cypher with commented two-step migration (Donor→Habit→Behavior→ExperimentalSetting, with fallback via Context→ExperimentalSetting) that writes hhh__group property on Donor nodes
- Files changed: Ontology.ttl (add rdfs:label to Group3/Group4), fuseki/init/Ontology.ttl (same), neo4j/init/constraints.cypher (new), scripts/migrate-group-labels.cypher (new)
- **Learnings for future iterations:**
  - Ontology.ttl and fuseki/init/Ontology.ttl must always be kept in sync — they're identical files served to two different backends
  - n10s SHORTEN mode maps `hhh:Group3` → Neo4j label `hhh__Group3`; relationships like `hhh:partOf` → `hhh__partOf`
  - The migration traversal path is Donor -[:hhh__donates]-> Habit -[:hhh__hasBehavior]-> Behavior -[:hhh__partOf]-> ExperimentalSetting
  - 4 pre-existing test failures remain (Backup alert, Fuseki, Neo4j, LibreTranslate)
---

## 2026-03-13 - US-010
- What was implemented: Created API-service/Dockerfile (minimal Python FastAPI); added recommender service to docker-compose.yml (with port 8000 exposed for dev) and docker-compose.prod.yml (no ports, internal network only); added RECOMMENDER_URL=http://recommender:8000 to stack.env; created app/routes/recommendRouter.js with createRecommendRouter factory — GET /:userId/history, GET /:userId, POST /classify — all proxy to RECOMMENDER_URL via fetch, forwarding Authorization header; mounted at /recommend in v1Router.js with participant|admin|researcher role check; created 6 integration tests in tests/integration/recommend.routes.test.js (401/403 auth checks + 3 fixture proxy tests + auth header preservation test)
- Files changed: API-service/Dockerfile (new), docker-compose.yml (add recommender service), docker-compose.prod.yml (add recommender service, no ports), stack.env (add RECOMMENDER_URL), app/routes/recommendRouter.js (new), app/routes/v1Router.js (import + mount recommendRouter), app/tests/integration/recommend.routes.test.js (new)
- **Learnings for future iterations:**
  - createRecommendRouter accepts `recommenderUrl` for test injection (same DI pattern as healthCheck.js)
  - Route order matters: /:userId/history must be defined before /:userId to prevent shadowing (though Express single-segment :params don't actually conflict across path depths)
  - POST /classify is not at risk of collision with GET /:userId because different HTTP methods
  - Pre-existing failures (Backup, Fuseki, Neo4j, LibreTranslate) remain — 4 out of 65 tests
---

## 2026-03-13 - US-009
- What was implemented: Created app/utils/healthCheck.js with checkAllServices() function — parallel health checks (1500ms timeout each) for neo4j (bolt), mongo, fuseki (HTTP), keycloak (HTTP), recommender (HTTP); returns {status: 'ok'|'error', services:{...}} with latencyMs per service; 503 if neo4j or mongo down; updated v1Router.js to call checkAllServices (accepts optional serviceChecks injection); updated auth.routes.test.js to pass mock serviceChecks; added 5 unit tests in tests/unit/health.check.test.js
- Files changed: app/utils/healthCheck.js (new), app/routes/v1Router.js (updated health handler), app/tests/unit/health.check.test.js (new), app/tests/integration/auth.routes.test.js (pass mock serviceChecks)
- **Learnings for future iterations:**
  - createV1Router now accepts `serviceChecks` option for injecting mock DB checks in tests — avoids real connection attempts during unit/integration tests
  - healthCheck.js uses dependency injection pattern: neo4jCheck and mongoCheck are injectable, HTTP URLs are injectable via options
  - HTTP checks use AbortController with setTimeout for 1500ms timeout; non-2xx/3xx/4xx responses are ok (only 5xx counts as error)
  - Both format:check and some lint errors are pre-existing (40 files, BackupConfiguration, Neo4jDatabase) — not introduced here
---

## 2026-03-13 - US-007
- What was implemented: Created app/middleware/requireRole.js (checks req.user.realm_access.roles, returns 403 if role missing); created app/routes/v1Router.js with createV1Router factory — /health public, authenticate applied to all routes below, /admin requires admin|researcher, /surveys and /habits require participant|admin|researcher; mounted /api/v1 in app.js; created 12 integration tests in tests/integration/auth.routes.test.js covering 401/403/200 for each role level
- Files changed: app/middleware/requireRole.js (new), app/routes/v1Router.js (new), app/tests/integration/auth.routes.test.js (new), app/app.js (updated)
- **Learnings for future iterations:**
  - v1Router exports `createV1Router` as default (factory, not pre-built instance) — avoids eager JWKS prefetch with undefined URL at module load time
  - Integration tests use `global.fetch` patching that passes through non-JWKS URLs to the real fetch (save original, delegate non-JWKS calls) so the test server can handle real HTTP requests
  - `server.listen(0, '127.0.0.1', resolve)` binds to a random port; use `server.address().port` to get it
  - Pre-existing failures (Backup alert, Fuseki, Neo4j, LibreTranslate) remain — not introduced by this story
---

## 2026-03-13 - US-006
- What was implemented: Created app/middleware/auth.js with Keycloak JWT verification middleware using Node.js built-in crypto; created tests/unit/auth.middleware.test.js with 6 unit tests (valid JWT, expired JWT, missing header, tampered signature, non-Bearer header, malformed token)
- Files changed: app/middleware/auth.js (new), app/tests/unit/auth.middleware.test.js (new)
- **Learnings for future iterations:**
  - Node.js 25 has built-in fetch and crypto — no external JWT library needed; use createPublicKey({ key: jwk, format: 'jwk' }) to load RSA public key from JWK
  - createVerify('RSA-SHA256') + verify.verify(publicKey, sigBuffer) for RSA signature verification
  - authMiddleware.ready is an exposed Promise for tests to await the startup JWKS prefetch before making requests
  - Pre-existing test failures (BackupConfiguration alert test, Fuseki/Neo4j/LibreTranslate integration) remain — not introduced by this story
  - Use `before()` hook in node:test to await middleware.ready before running tests
---

## 2026-03-13 - US-005
- What was implemented: Replaced keycloak/hhh-realm.json placeholder with full Keycloak 24 realm export; updated docker-compose.yml and docker-compose.prod.yml to mount realm file and pass --import-realm flag
- Files changed: keycloak/hhh-realm.json, docker-compose.yml, docker-compose.prod.yml
- **Learnings for future iterations:**
  - Keycloak --import-realm reads from /opt/keycloak/data/import/*.json — mount realm file there as :ro
  - hhh-flutter is a public PKCE client (no secret); hhh-backend is confidential with serviceAccountsEnabled
  - Realm roles (participant, admin, researcher) are defined under roles.realm in the JSON
  - Both compose files parse cleanly after the volume mount addition
---

## 2026-03-13 - US-004
- What was implemented: Added Keycloak 24 service to docker-compose.yml (dev) and docker-compose.prod.yml (prod); added KEYCLOAK_ADMIN/KEYCLOAK_ADMIN_PASSWORD to stack.env; created keycloak/hhh-realm.json placeholder
- Files changed: docker-compose.yml, docker-compose.prod.yml, stack.env, keycloak/hhh-realm.json (new)
- **Learnings for future iterations:**
  - Traefik dashboard port default changed from 8080 to 8888 in dev docker-compose.yml to avoid conflict with Keycloak (both use port 8080 inside container)
  - Keycloak 24 in dev uses `start-dev` with `KC_DB=dev-file` (embedded H2); prod also uses `start-dev` (simpler, no external DB needed) with KC_PROXY=edge and KC_HTTP_RELATIVE_PATH=/auth
  - Both compose files parse cleanly with `docker-compose config`
  - stack.env is used as env_file for keycloak service; credentials are read from there
  - keycloak/ directory placeholder created at keycloak/hhh-realm.json for future realm config (US-005)
---

## Codebase Patterns
- All infrastructure branches (feature/traeffic, feature/h3-proxy, feature/server, docker-env, max-base-path, max-deployment-optionen) were already merged into master before this project started — merges into rapid_dev will be no-ops
- docker-compose config exits 0 cleanly on master/rapid_dev
- Real conflict markers use 7 angle brackets: `<<<<<<< ` and `>>>>>>> ` — the `=======` lines in DOCUMENTATION.md are section separators, not conflict markers
- feature/hjt-* branches have unrelated git histories — use --allow-unrelated-histories; they conflict on .gitignore and README.md (keep HHH version, append unique content)
- rapid_dev branch structure: API-service/ (Python FastAPI recommender), habit-recommendation-system-ui/ (Vue UI), mobile/ (Flutter app), app/ (Node.js backend)

---

## 2026-03-13 - US-001
- What was implemented: Created `ralph/hhh-platform-unified` working branch from master; created `rapid_dev` branch from master; merged all required infrastructure branches (feature/traeffic, feature/h3-proxy, feature/server, docker-env, max-base-path, max-deployment-optionen) — all were already in master so merges were no-ops
- Files changed: prd.json (US-001 passes=true), progress.txt (created)
- **Learnings for future iterations:**
  - All infrastructure branches listed in US-001 were already merged into master — rapid_dev is effectively identical to master at this point
  - `docker-compose config` (v1 CLI) returns exit 0; both docker-compose.yml and docker-compose.prod.yml exist at repo root
  - No actual merge conflict markers exist in the codebase; the `=======` lines in DOCUMENTATION.md are decorative separators
  - git stash was needed because analytics/survey_analytics.ipynb had unstaged changes on the development branch
---

## 2026-03-13 - US-002
- What was implemented: Merged all required feature branches into rapid_dev
  - feature/neo4j, feature/backup, feature/colorLaMarcel, contact-reward-language: already merged into master (no-op)
  - feature/adminpanel: fast-forward merge (adds admin panel, experiment config, analytics notebook)
  - ralph/flutter-mobile-app: merge commit (adds mobile/ Flutter app with all screens and widgets)
  - feature/hjt-diplomarbeit-habit-recommendation-system: merged with --allow-unrelated-histories (adds API-service/, habit-recommendation-system-ui/)
  - feature/hjt-context-classifier: merged with --allow-unrelated-histories (adds HHH-service/ context classifier)
- Files changed: rapid_dev branch (multiple merge commits), prd.json (US-002 passes=true), progress.txt
- **Learnings for future iterations:**
  - hjt branches (feature/hjt-*) have unrelated git histories — always use --allow-unrelated-histories when merging them
  - Both hjt branches conflict on .gitignore and README.md: resolve by keeping HHH version and appending unique content
  - rapid_dev now has API-service/, habit-recommendation-system-ui/, and mobile/ at repo root
  - A docker-compose.yaml was added from hjt-diplomarbeit (different from docker-compose.yml) — may cause duplicate config warnings
  - docker-compose up --no-start exits 0 even without Docker daemon running (warning only, not error)
---

## 2026-03-13 - US-003
- What was implemented: Removed all EJS/HTML view templates from app/views/ (14 files deleted); replaced all res.render() calls with res.json() in all controllers (aboutController, accessibilityController, contactController, defaultController, demoController, disclaimerController, donateController, imprintController, privacyController, rewardController, surveyController, thanksController); removed EJS view engine setup from app.js; added GET /api endpoint returning JSON health/info response
- Files changed: app/app.js, app/controllers/*.js (all 12), app/views/*.ejs (all 14 deleted)
- **Learnings for future iterations:**
  - The app had EJS view engine configured twice in app.js — both were removed
  - Controllers that used renderLocalizedView (from defaultController.js) still work since defaultController now returns JSON
  - Integration tests (Fuseki, Neo4j, LibreTranslate, Backup) were already failing before this story — pre-existing failures
  - ObjectId was imported in surveyController but never used — removed to fix lint
  - Pre-existing lint errors exist in app/public/js/ and app/utils/Neo4jDatabase.js — not introduced by this story
---

## 2026-03-13 - US-008
- What was implemented: Created app/routes/index.js (re-exports createV1Router as consolidation point); updated v1Router.js to mount actual surveyRouter under /surveys (GET /api/v1/surveys/:id now calls renderSurvey); added GET / handler to surveyRouter for base /surveys route; updated app.js to import from routes/index.js and removed old /:lng/survey route
- Files changed: app/routes/index.js (new), app/routes/v1Router.js (mount surveyRouter), app/routes/surveyRouter.js (add GET /), app/app.js (import from index.js, remove /:lng/survey route and import)
- **Learnings for future iterations:**
  - surveyRouter only had GET /:id — needed GET / handler for base /surveys path tests to return 200
  - app/routes/index.js is a thin re-export; routes/v1Router.js is still where all logic lives
  - 4 pre-existing failures remain (Backup alert, Fuseki, Neo4j, LibreTranslate) — not introduced here
---

## 2026-03-15 - US-048
- What was implemented: Created 4 per-service deployment scripts in `scripts/`: `deploy-backend.sh` (docker-compose build app + up -d app + polls GET /api/v1/health until status:ok or 60s timeout); `deploy-flutter-web.sh` (cd mobile && flutter build web; cp -r build/web/ app/public/flutter/); `deploy-recommender.sh` (docker-compose build recommender + up -d recommender); `deploy-keycloak.sh` (obtain Keycloak admin token via REST API, create or update realm 'hhh' from keycloak/hhh-realm.json, docker-compose restart keycloak). All scripts: `set -euo pipefail`, `--dry-run` flag (prints commands without executing), exit non-zero on failure, chmod +x (mode 755).
- Files changed: scripts/deploy-backend.sh (new), scripts/deploy-flutter-web.sh (new), scripts/deploy-recommender.sh (new), scripts/deploy-keycloak.sh (new), prd.json (US-048 passes=true)
- **Learnings for future iterations:**
  - `run_in_dir()` helper pattern: for dry-run of commands requiring a `cd`, print `(cd dir && cmd)` instead of using `run bash -c "..."` which loses command legibility
  - Keycloak admin API: GET /admin/realms/hhh returns 200 if realm exists — use HTTP status to decide create (POST /admin/realms) vs update (PUT /admin/realms/hhh)
  - KEYCLOAK_ADMIN_PASSWORD must be explicitly checked for empty string before hitting the API — `curl -sf` would fail silently with empty password producing a confusing error
  - `docker-compose` service names (app, recommender, keycloak) differ from container names (h3-app, h3-recommender, h3-keycloak) — always use service names with docker-compose CLI
  - `set -euo pipefail` with `grep -q` in a pipeline: if grep finds no match it exits non-zero, which triggers `set -e`. Use `|| true` after grep in non-critical checks to avoid premature script failure
---

## 2026-03-17 - US-072
- What was implemented: Created `mobile/lib/config/app_config.dart` with `AppConfig` abstract final class exposing three `String.fromEnvironment` constants: `apiBaseUrl` (default `http://localhost:3000/api/v1`), `keycloakUrl` (default `http://localhost:8080`), `wsBaseUrl` (default `ws://localhost:3000/ws`). Replaced hardcoded `https://api.hhh.tu-dresden.de/api/v1` and `https://keycloak.hhh.tu-dresden.de` strings in all six service files with references to `AppConfig` constants. Changed `_wsUrl` in `recommendation_ws_service.dart` from `static const` to `static final` since string interpolation of a const isn't itself const. Updated `docs/guides/developer-onboarding.md`: added `WS_BASE_URL` flag to both Chrome and Android emulator run commands, and added an `--dart-define` reference table above the Flutter section.
- Files changed: mobile/lib/config/app_config.dart (new), mobile/lib/services/admin_service.dart, auth_service.dart, habit_service.dart, recommendation_service.dart, recommendation_ws_service.dart, survey_service.dart (all updated), docs/guides/developer-onboarding.md, prd.json (US-072 passes=true)
- **Learnings for future iterations:**
  - String interpolation in Dart (`'${AppConfig.wsBaseUrl}/path'`) produces a non-const value even when the interpolated variable is const — use `static final` (not `static const`) for interpolated URL fields
  - `String.fromEnvironment` with a `defaultValue` is the correct Dart pattern for compile-time env vars; values are injected via `--dart-define=KEY=value` at `flutter run` / `flutter build` time
  - `flutter analyze lib/` must be run from inside the `mobile/` directory
---

## 2026-03-17 - US-075
- What was implemented: Removed unused npm polyfill packages `fs` (^0.0.1-security) and `path` (^0.12.7) from `dependencies` in app/package.json. All `import fs from 'fs'` and `import path from 'path'` usages in source files are Node.js built-ins and continue to work without the npm packages.
- Files changed: app/package.json (remove fs and path deps), app/package-lock.json (regenerated), prd.json (US-075 passes=true)
- **Learnings for future iterations:**
  - `fs` (npm) and `path` (npm) are polyfill/stub packages — Node.js provides both as built-ins; the npm packages add no value for server-side code
  - grep results for `import ... from 'fs'` / `import ... from 'path'` are all Node.js built-in usage (not npm package usage) — safe to remove the npm deps
---

## 2026-03-17 - US-076
- What was implemented: Removed all 8 TODO comments from app/utils/SparqlDatabase.js. Simple "Refactor" TODOs with no actionable detail were deleted outright. TODOs with meaningful context were replaced with // NOTE: comments: (1) label type mapping → NOTE about BCIO context categories; (2) NORMALIZE_LANG constant → NOTE about hardcoded 'en' and potential env var future; (3) translate(), addContext(), _appendContext(), addBehavior(), _appendBehavior() — bare "Refactor" TODOs removed. Ran `npx prettier --write` to fix blank-line formatting introduced by the edits.
- Files changed: app/utils/SparqlDatabase.js (remove 8 TODO comments, add 2 NOTE comments), prd.json (US-076 passes=true)
- **Learnings for future iterations:**
  - After removing comment lines from a JS file, run `npx prettier --write` from app/ to catch any whitespace/blank-line formatting issues that would fail the `format:check` step
  - TODO comments in this file were all stale (no bugs, just suggestions) — safe to remove or convert to NOTE
---
## 2026-03-18 - US-091
- What was implemented: Generated `mobile/assets/icon/app_icon.png` (1024×1024 PNG) via a Python/Pillow script: charcoal (#454446) circle background, a graph-network of 9 nodes (5 heart-shaped + 4 circle-with-inner-highlight) in HabShare green (#45b700), connected by white anti-aliased edges with glow halos, masked to a clean circle. Added `flutter_launcher_icons: ^0.14.0` to dev_dependencies. Added `flutter_launcher_icons` config block + `flutter.assets` entry in pubspec.yaml. Ran `flutter pub run flutter_launcher_icons` to produce Android mipmap-*, iOS AppIcon.appiconset (all sizes), and web favicons/icons.
- Files changed: mobile/assets/icon/app_icon.png (new), mobile/pubspec.yaml (dev_dependencies + config + assets), mobile/pubspec.lock (regenerated), mobile/android/app/src/main/res/mipmap-*/ic_launcher.png (overwritten), mobile/ios/Runner/Assets.xcassets/AppIcon.appiconset/* (overwritten + new sizes), mobile/ios/Runner.xcodeproj/project.pbxproj, mobile/web/favicon.png, mobile/web/icons/Icon-*.png, mobile/web/manifest.json, prd.json (US-091 passes=true)
- **Learnings for future iterations:**
  - Python/Pillow is available in the dev environment — use it for icon/asset generation scripts
  - `flutter_launcher_icons` config goes at the TOP level of pubspec.yaml (not inside `flutter:`); the `web:` key takes `generate: true` and `image_path:` as sub-keys
  - Run `flutter pub get` before `flutter pub run flutter_launcher_icons` when the package is newly added
  - The generator warns about alpha channel on iOS icons but still succeeds; adding `remove_alpha_ios: true` to the config suppresses it if needed for App Store submission
  - Generated icon sizes include some legacy iOS sizes (50x50, 57x57, 72x72) as untracked new files — always `git add` the entire appiconset directory
  - The `flutter.assets` section is required even for the icon asset if it is referenced in pubspec; otherwise the app build will fail
---

## 2026-03-18 - US-095
- What was implemented: Created `mobile/assets/icon/generate_icon.py` (the missing source script from US-091) — deterministic Pillow script producing a 1024×1024 PNG with charcoal (#454446) circular background, 9-node graph (5 hearts + 4 circles-with-inner-highlight) in HabShare green (#45b700), white anti-aliased edges with glow halos, masked to a circle. Re-ran `flutter pub run flutter_launcher_icons` to regenerate all Android mipmap, iOS AppIcon.appiconset, and web icon assets from the new script's output. Confirmed US-092 (Podfile platform line uncommented), US-093 (SDKROOT in project.pbxproj), US-094 (Info.plist CFBundleName and AndroidManifest android:label both 'Health Habit Hub'). flutter analyze passes.
- Files changed: mobile/assets/icon/generate_icon.py (new), mobile/assets/icon/app_icon.png (regenerated), mobile/android/app/src/main/res/mipmap-*/ic_launcher.png (regenerated), mobile/ios/Runner/Assets.xcassets/AppIcon.appiconset/* (regenerated), mobile/web/favicon.png + icons/* (regenerated), prd.json (US-095 passes=true)
- **Learnings for future iterations:**
  - Always commit the generation script alongside the generated asset — the acceptance criteria will ask for both
  - `generate_icon.py` must be deterministic (no randomness) so "re-running it regenerates the same PNG" is strictly satisfied
  - Heart shape: parametric `x = 16 sin³(t)`, `y = -(13 cos(t) − 5 cos(2t) − 2 cos(3t) − cos(4t))` scaled by `size/17`
  - `flutter pub run flutter_launcher_icons` regenerates all platform icons from `image_path` in pubspec.yaml; the WARNING about alpha channel on iOS does not block success
---

## 2026-03-20 - US-096
- What was implemented: Created API-service/requirements.txt (fastapi, uvicorn, openai, python-dotenv). Created API-service/llm_client.py — async chat_complete(messages, model, temperature) helper that reads OPENAI_API_KEY, LLM_MODEL (default gpt-4o-mini), LLM_TEMPERATURE (default 0.2) from env; logs a warning if key is placeholder but does not crash; uses openai.AsyncOpenAI. Created API-service/main.py — minimal FastAPI app with /health endpoint. Created API-service/pyrightconfig.json to suppress false-positive missing import errors (packages not installed locally, installed in Docker). Added OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_API_KEY, LLM_MODEL, LLM_TEMPERATURE to stack.env with explanatory comments. No SCADS references existed in API-service to remove.
- Files changed: API-service/requirements.txt (new), API-service/llm_client.py (new), API-service/main.py (new), API-service/pyrightconfig.json (new), stack.env (added OpenAI section), prd.json (US-096 passes=true)
- **Learnings for future iterations:**
  - API-service/ starts with only a Dockerfile — all Python source files (main.py, requirements.txt) must be created from scratch
  - `openai.AsyncOpenAI` accepts a placeholder API key without crashing at import time; errors only occur when an actual API call is made
  - pyrightconfig.json with `"reportMissingImports": "none"` is the correct way to suppress false-positive import errors for packages installed only inside Docker
  - Python 3.11 union syntax (`X | Y`) is supported at runtime but pyright may flag it if pythonVersion is not set — always specify pythonVersion in pyrightconfig.json
  - The docker-compose.yml `recommender` service builds from `./API-service` and reads `stack.env` — new env vars added to stack.env are automatically available to the service
---

## 2026-03-20 - US-097
- What was implemented: Added `POST /api/v1/llm/classify-habit` endpoint to API-service. Created `API-service/routers/classify_habit.py` with Pydantic request/response models, SHA-256-keyed Redis caching (TTL from `REDIS_TTL_SECONDS` env, default 86400), graceful Redis fallback (logs warning, continues without cache), and LLM call via existing `chat_complete` helper. Created `API-service/prompts/classify_habit.txt` with structured prompt that returns JSON `{"is_habit": bool, "confidence": float}`. Updated `main.py` to include the router under `/api/v1`. Added `redis[asyncio]`, `httpx`, `pytest`, `pytest-asyncio` to `requirements.txt`. Created 3 unit tests covering: habit → true, non-habit → false, cache hit skips LLM call.
- Files changed: API-service/main.py (import + include_router), API-service/requirements.txt (add redis/httpx/pytest), API-service/prompts/classify_habit.txt (new), API-service/routers/__init__.py (new), API-service/routers/classify_habit.py (new), API-service/tests/__init__.py (new), API-service/tests/test_classify_habit.py (new), prd.json (US-097 passes=true)
- **Learnings for future iterations:**
  - Python 3.9 (system Python on macOS) does NOT support `X | None` union type annotation syntax at runtime — always use `Optional[X]` from `typing`, or add `from __future__ import annotations` at the top of the file
  - `redis.asyncio.Redis.ping()` is not typed as a coroutine in pyright stubs — suppress with `# type: ignore[misc]` rather than changing the code
  - `aioredis.from_url()` returns a connected `Redis` client; place the `ping()` call inside `try/except` to detect unavailability; if ping raises, return `None` so all callers use LLM directly
  - FastAPI routers go in `routers/` sub-package; need an `__init__.py`; register with `app.include_router(router, prefix="/api/v1")` in main.py
  - Tests use `pytest-asyncio` with `@pytest.mark.asyncio`; use `ASGITransport` from `httpx` for in-process FastAPI testing (no server needed)
---

## 2026-03-20 - US-101
- What was implemented: Created `scripts/migrate-habits-bcio.js` — a standalone Node.js script (ESM, uses native fetch) that back-fills BCIO enrichment for existing Neo4j `Habit` nodes. Queries `MATCH (h:Habit) WHERE h.migrated_to_bcio IS NULL OR h.migrated_to_bcio = false`; for each habit calls M1.2 (`POST /api/v1/llm/classify-context`) then M1.3 (`POST /api/v1/llm/map-bcio`); MERGEs `Context` nodes + `HAS_CONTEXT` relationships and `BCIOConcept` nodes + `MAPS_TO` relationships exactly as US-100 does; sets `migrated_to_bcio: true` and `migrated_at: <ISO>` on the `Habit` node. Supports `--dry-run` flag. Individual failures are logged with UUID + error message and do not abort the run; exits code 1 if any failed. Configured via `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `API_SERVICE_URL` env vars. Created `docs/migration.md` documenting both migration scripts (migrate-habits-bcio.js and migrate-group-labels.cypher) with usage, env vars, expected output, and idempotency notes. All 196 tests still pass.
- Files changed: scripts/migrate-habits-bcio.js (new), docs/migration.md (new), prd.json (US-101 passes=true)
- **Learnings for future iterations:**
  - Migration scripts in this repo live in `scripts/` and use ESM (`import` syntax) — no `package.json` in scripts/; run with `node scripts/<file>` from repo root (node finds neo4j-driver in app/node_modules via node_modules resolution)
  - The `--dry-run` pattern: `const DRY_RUN = process.argv.includes('--dry-run')` at top; wrap all write operations in `if (!DRY_RUN) { ... }` blocks and log what would be written
  - Prettier is enforced repo-wide — always run `npx prettier --write <file>` from repo root after writing new scripts; the prettier version will be auto-installed if not cached
  - Neo4j session: open once for all habits (not per-habit), close in finally block; reuse the same session for all queries to avoid connection overhead
---

## 2026-03-20 - US-102
- What was implemented: Created `mongo/seed/questionnaires.json` with full SLIQ (4 questions: diet, physical activity, smoking, alcohol) and RAND-36 (36 questions covering physical functioning, role limitations, pain, emotional well-being, energy, social functioning, general health). Created `mongo/seed/seed-questionnaires.js` — upserts by slug using `$set`+`$setOnInsert` so re-runs are safe. Created `app/routes/questionnairesRouter.js` with `GET /` (returns active questionnaires with questionCount summary) and `GET /:slug` (returns full definition; 404 for inactive or missing). Registered in `v1Router.js` under `/questionnaires` with participant/admin/researcher role guard. 8 integration tests added covering auth enforcement, active-only filtering, full definition structure, 36-question count, 404 for unknown/inactive slugs, and role rejection. All 204 tests pass (up from 196).
- Files changed: mongo/seed/questionnaires.json (new), mongo/seed/seed-questionnaires.js (new), app/routes/questionnairesRouter.js (new), app/routes/v1Router.js (add import + /questionnaires mount), app/tests/integration/questionnaires.routes.test.js (new), prd.json (US-102 passes=true)
- **Learnings for future iterations:**
  - The questionnairesRouter follows the same factory injection pattern as surveyRouter: `{ db }` injected for tests, falls back to `import('../models/survey.js').connect()` at runtime
  - `GET /questionnaires` returns only `active: true` documents — inactive questionnaires must return 404 from `GET /:slug` too (pass `active: true` in the `findOne` query)
  - Mock DB `collection()` must handle unknown collection names gracefully (return empty find/findOne) to avoid errors from other routers sharing the same mock db
  - Seed script uses `$setOnInsert: { createdAt }` so `createdAt` is only written on first insert, not overwritten on re-runs
---

## 2026-03-20 - US-104
- What was implemented: Extended `app/routes/questionnaireResponsesRouter.js` — added `GET /me` (all responses for authenticated user, ordered by `submitted_at` DESC) and `GET /me/:slug` (most recent response for a specific questionnaire slug, 404 if none). Added eager non-blocking `createIndex({ userId: 1, questionnaireSlug: 1, submitted_at: -1 })` call on router init (errors swallowed so mock DBs don't break). userId always from `req.user.sub` (JWT sub claim); no client-supplied userId accepted. Created `app/tests/integration/questionnaire-responses.routes.test.js` with 9 tests: auth enforcement on all 3 endpoints, validation (missing slug / answers), submit SLIQ → fetch by slug → verify answers match, ordering check, user isolation (user B cannot see user A's responses).
- Files changed: app/routes/questionnaireResponsesRouter.js (add GET /me, GET /me/:slug, createIndex), app/tests/integration/questionnaire-responses.routes.test.js (new — 9 tests), prd.json (US-104 passes=true)
- **Learnings for future iterations:**
  - `createIndex` called eagerly at router init (non-blocking `.then().catch()`) is the pattern here — no dedicated migration/seed step; errors swallowed so in-memory mock DBs work without a `createIndex` stub (but the mock DB in the test does implement a no-op `createIndex` to be safe)
  - Mock DB `find()` must return a fluent builder (`sort()` → `limit()` → `toArray()`), not an async directly — required to test chained MongoDB queries
  - `sort({ submitted_at: -1 })` in mock requires special handling since `submitted_at` is a `Date` object — compare `.getTime()` not raw value
  - The `GET /me` and `GET /me/:slug` routes must be registered before any wildcard routes (they are, since Express matches in order); note `/me` must come before `/:slug` if ever a `:slug` route is added at root level
---

## 2026-03-20 - US-106
- What was implemented: Added questionnaire CRUD admin routes to `app/routes/adminRouter.js`: `GET /api/v1/admin/questionnaires` (all questionnaires with slug, title, active, questionCount, updatedAt); `POST /api/v1/admin/questionnaires` (create; 409 if slug exists); `PUT /api/v1/admin/questionnaires/:slug` (update title/description/version/questions); `PATCH /api/v1/admin/questionnaires/:slug/active` (toggle active flag); `DELETE /api/v1/admin/questionnaires/:slug` (hard-delete if zero responses in form_responses; deactivate if responses exist, returning deactivated:true + responseCount). Updated `admin/src/lib/auth.ts` to store Keycloak `access_token` in the JWT and expose as `session.accessToken`. Replaced `admin/src/app/(admin)/questionnaires/page.tsx` placeholder with full `"use client"` implementation: questionnaire table (slug, title, active badge, question count, version, last updated, actions); `QuestionnaireModal` for create/edit — auto-slugification from title, slug locked in edit mode; `QuestionCard` component with HTML5 native drag-and-drop for reorder; per-type option builder for single_choice and multi_choice; required toggle; `ConfirmDeleteDialog` with deactivation warning. Full CSS module `page.module.css` with design-system variables. `npm run build` passes (typecheck + lint).
- Files changed: app/routes/adminRouter.js (questionnaire CRUD routes), admin/src/lib/auth.ts (accessToken in session), admin/src/app/(admin)/questionnaires/page.tsx (full CRUD UI), admin/src/app/(admin)/questionnaires/page.module.css (styles), prd.json (US-106 passes=true)
- **Learnings for future iterations:**
  - NextAuth v4 `jwt` callback receives `account.access_token` on first sign-in (when `account` is non-null); store it as `token.accessToken` and forward via `session` callback so client components can access it via `useSession`
  - HTML5 native drag-and-drop: `draggable` + `onDragStart`/`onDragOver`/`onDrop` on each card; track `dragIndex` in a `useRef` (not state) to avoid re-renders; track `dragOverIndex` in state for visual feedback
  - For admin routes that need access to the Keycloak token, use `NEXT_PUBLIC_API_URL` env var with default `http://localhost:3000/api/v1`; the session `accessToken` is passed as `Authorization: Bearer` header
  - `DELETE /admin/questionnaires/:slug` checks `form_responses` collection for `questionnaireSlug` field before hard-delete; always deactivate instead of delete when responses exist
  - Prettier must be run on adminRouter.js after editing — format check is part of the test suite; run `npx prettier --write routes/adminRouter.js` from the `app/` directory
---

## 2026-03-20 - US-107
- What was implemented: Created `API-service/prompts/extract_habits.txt` — LLM prompt that lists all user habits (uuid, sentence, context) and asks the model to select relevant ones for a goal, returning JSON `{selected_habit_uuids, habit_summary}`. Created `API-service/routers/extract_habits.py` with `POST /api/v1/llm/extract-habits` — request: `{user_id, goal}`; response: `{selected_habits: [{uuid, sentence, context}], habit_summary}`. Neo4j query fetches all `Habit` nodes for `userID` with `OPTIONAL MATCH (h)-[:HAS_CONTEXT]->(c:Context)` and groups context phrases by dimension. Redis caching keyed by SHA-256 of `user_id||goal`. Empty-habits short-circuit (no LLM call). Added `neo4j>=5.19.0` to `requirements.txt`. Registered router in `main.py`. Created 4 unit tests (happy path, empty habits, bad LLM JSON, cache hit). All 15 tests pass; pyright 0 errors.
- Files changed: API-service/prompts/extract_habits.txt (new), API-service/routers/extract_habits.py (new), API-service/tests/test_extract_habits.py (new), API-service/requirements.txt (add neo4j), API-service/main.py (import + include router), prd.json (US-107 passes=true)
- **Learnings for future iterations:**
  - Use `neo4j>=5.19.0` Python driver (`AsyncGraphDatabase.driver`) for async Neo4j access from FastAPI; `await result.fetch(N)` retrieves records as a list
  - Habit nodes stored with `userID` property (camelCase) — query `MATCH (h:Habit {userID: $user_id})`; context nodes via `OPTIONAL MATCH (h)-[:HAS_CONTEXT]->(c:Context)`
  - `_fetch_habits_for_user` is a standalone async function (not a method) so it can be mocked directly with `patch("routers.extract_habits._fetch_habits_for_user", new=AsyncMock(...))`
  - Cache the full `result.model_dump()` JSON (not just the LLM output) so cache hits reconstruct the complete response without re-querying Neo4j
  - Python 3.9 supports `tuple[List[str], str]` return type hints only in quotes or with `from __future__ import annotations` — use the import
---

## 2026-03-20 - US-108
- What was implemented: Created `API-service/routers/extract_profile.py` — POST /api/v1/llm/extract-profile endpoint (M3.2 User Profile Extractor). Request: {user_id, goal}. Fetches user's most recent SLIQ and RAND-36 questionnaire responses from the Node.js backend via `GET /api/v1/questionnaire-responses/me/:slug` using `httpx.AsyncClient` with a service auth token (SERVICE_AUTH_TOKEN env var). If questionnaire data is unavailable, gracefully falls back to empty JSON objects. LLM prompt (prompts/extract_profile.txt) instructs the model to produce profile_summary (≤80 words), profile_detailed (80–200 words), and rag_query (single retrieval sentence). Redis caching keyed on SHA-256(user_id||goal) with prefix `extract_profile:`. Fallback on LLM parse error returns goal as rag_query. Registered router in main.py. Created `API-service/tests/test_extract_profile.py` with 4 tests: normal flow, missing questionnaire data, invalid LLM JSON fallback, Redis cache hit. All 19 API-service tests pass.
- Files changed: API-service/routers/extract_profile.py (new), API-service/prompts/extract_profile.txt (new), API-service/tests/test_extract_profile.py (new), API-service/main.py (add router import + include), prd.json (US-108 passes=true)
- **Learnings for future iterations:**
  - For inter-service HTTP calls from API-service (Python) to Node.js backend, use `httpx.AsyncClient` (already in requirements.txt). Pass a service auth token via `Authorization: Bearer <SERVICE_AUTH_TOKEN>` and the target user ID via `X-Service-User-Id` header
  - SERVICE_AUTH_TOKEN env var must be set in the deployment environment; when not set, questionnaire fetch is skipped gracefully (logs a warning)
  - BACKEND_URL defaults to `http://app:3000` (Docker service name for Node.js backend)
  - The `_fetch_questionnaire_response` function is injectable in tests by patching `routers.extract_profile._fetch_questionnaire_response` — no need to mock httpx directly
---

## 2026-03-20 - US-109
- What was implemented: Created `API-service/routers/retrieve.py` — POST /api/v1/llm/retrieve (M3.3 RAG Retrieval) plus KB CRUD (GET/POST/DELETE /api/v1/kb, POST /api/v1/kb/reindex). In-memory vector index keyed by filename with SHA-256 file-hash change detection. PDF text extracted via `pypdf`, chunked (500 chars, 100 char overlap), embedded via OpenAI text-embedding-3-small (same `_embed_texts` pattern as map_bcio.py). One-time LLM summaries generated per PDF and cached in `API-service/kb/_meta/<filename>.summary.json`. Cosine similarity search returns top-k sources. `API-service/kb/` directory created with `.gitkeep` files. `API-service/prompts/summarize_pdf.txt` added. `pypdf>=4.0.0` and `python-multipart>=0.0.9` added to requirements.txt (multipart required by FastAPI File/Form uploads). 13 new tests in `tests/test_retrieve.py`. All 32 tests pass. Pyright: 0 errors.
- Files changed: API-service/routers/retrieve.py (new), API-service/tests/test_retrieve.py (new), API-service/prompts/summarize_pdf.txt (new), API-service/kb/.gitkeep (new), API-service/kb/_meta/.gitkeep (new), API-service/requirements.txt (add pypdf + python-multipart), API-service/main.py (add retrieve_router), prd.json (US-109 passes=true)
- **Learnings for future iterations:**
  - FastAPI File/Form upload routes require `python-multipart` to be installed — add it to requirements.txt whenever using `UploadFile` or `Form()`
  - Module-level `asyncio.Lock()` works fine in Python 3.9+ without a running event loop at import time
  - For patchable path variables in tests, declare `_KB_DIR: Path = ...` as a typed module-level variable and use `patch.object(retrieve_module, "_KB_DIR", tmp_path)` — cleaner than patching `Path` itself
  - The `import routers.retrieve as _self` pattern inside functions (to access `_self._KB_DIR`) allows test-time patching of module variables; avoids stale closure captures
  - Route order: `POST /kb/reindex` must be registered before `DELETE /kb/{filename}` to ensure FastAPI's routing resolves the literal "reindex" segment correctly for POST requests
  - pypdf `PdfReader.pages[i].extract_text()` can return None for image-only PDFs — always apply `or ""` guard
---

## 2026-03-20 - US-110
- What was implemented: Created `API-service/routers/recommend.py` — POST /api/v1/llm/recommend endpoint that orchestrates M3.1 (extract_habits) + M3.2 (extract_profile) in parallel via asyncio.gather, then M3.3 (retrieve) using the rag_query from M3.2, then fetches prior feedback from MongoDB `recommendation_feedback` collection, builds a prompt, and calls the LLM to produce personalised recommendations. Response: {recommendation_id (uuid4), goal, recommendations[{title, body, rationale, sources[{filename, excerpt}]}], generated_at}. Recommendation stored in MongoDB `recommendations` collection. Redis caching keyed by (user_id, goal). Created `API-service/prompts/recommend.txt` prompt template with all 7 template variables: {goal, profile_summary, profile_detailed, habit_summary, habits_json, sources_json, prior_feedback}. Added `motor>=3.3.0` to requirements.txt. Registered router in main.py. 37 tests pass.
- Files changed: API-service/routers/recommend.py (new), API-service/prompts/recommend.txt (new), API-service/tests/test_recommend.py (new), API-service/requirements.txt (add motor), API-service/main.py (add recommend router), prd.json (US-110 passes=true)
- **Learnings for future iterations:**
  - Orchestrator routers should import sub-module endpoint functions directly (`from routers.extract_habits import extract_habits as _extract_habits`) and call them as async functions — this avoids HTTP overhead and keeps the pipeline in-process. Test patches target `routers.recommend._extract_habits` (the imported name), not the original module.
  - `asyncio.gather(m31_call, m32_call)` runs M3.1 and M3.2 in parallel since they are independent; M3.3 must wait for M3.2's rag_query output before it can run.
  - `motor.motor_asyncio.AsyncIOMotorClient` is the async MongoDB driver; construct the URL from MONGO_HOST/MONGO_PORT/MONGO_USER/MONGO_PASSWORD/MONGO_AUTH_SOURCE env vars (consistent with existing Node.js backend pattern). Allow MONGO_URL override for tests.
  - The `recommendation_feedback` collection is written by US-111 (Feedback Collector) — reading from it in US-110 is safe even if the collection doesn't exist yet (motor returns an empty cursor).
  - `_store_recommendation` and `_fetch_prior_feedback` are extracted as helper functions so tests can patch them independently without touching the MongoDB client.
---

## 2026-03-20 - US-111
- What was implemented: Created `app/routes/recommendationsRouter.js` with two endpoints: (1) `POST /api/v1/recommendations/:recommendation_id/feedback` — validates comment, looks up the recommendation in MongoDB to get goal, inserts `{recommendation_id, userId, goal, comment, created_at}` into `recommendation_feedback` collection, then deletes the Redis cache key `recommend:{SHA256(user_id||goal)}` (graceful — Redis failure is non-fatal); (2) `GET /api/v1/recommendations/me` — queries `recommendations` collection by userId, joins with `recommendation_feedback` by `recommendation_id` (using `$in`), and returns each recommendation with `feedback: [{comment, created_at}]` attached. Added `redis@4` npm package for Redis client. Registered router in `v1Router.js` under `/recommendations` with participant/admin/researcher role guard; `v1Router` accepts `redisClient` and `redisUrl` injected params for testing. Integration test file `recommendations.routes.test.js` covers: auth enforcement, validation, 404 for unknown recommendation, feedback storage in MongoDB, Redis cache invalidation, empty GET response, and GET with feedback attached. All 221 tests pass.
- Files changed: app/routes/recommendationsRouter.js (new), app/routes/v1Router.js (add redisClient/redisUrl params, mount recommendationsRouter), app/tests/integration/recommendations.routes.test.js (new), app/package.json (add redis@4), app/package-lock.json (regenerated), prd.json (US-111 passes=true)
- **Learnings for future iterations:**
  - Redis cache key matches Python: `recommend:{SHA256(user_id||goal)}` — use `createHash('sha256').update(`${userId}||${goal}`).digest('hex')` with `node:crypto`
  - Redis connection is lazy/optional: inject `redisClient` (or `null` to disable) for tests; production creates client from `REDIS_URL` env or `redis://localhost:6379`
  - The `redis@4` npm package supports ESM and async/await natively; import via `import('redis')` (dynamic) to allow injection pattern in tests
  - POST /recommendations/:id/feedback must look up the recommendation document first to get the `goal` field (needed to compute the Redis key); return 404 if not found
  - In-memory MongoDB mock pattern for tests: `find()` must handle both simple field equality and `{$in: [...]}` queries; `sort()` and `toArray()` chain off `find()`
---

## 2026-03-20 - US-112
- What was implemented: Created `mobile/lib/features/recommendation/` module with 5 files. `recommendation_models.dart` — `RecommendationSourceRef`, `RecommendationItem`, `RecommendationResponse` models matching the Python API-service response schema (recommendation_id, goal, recommendations[]{title, body, rationale, sources[]{filename, excerpt}}). `recommendation_feature_service.dart` — Dio-based service calling `POST /api/v1/recommend/generate` (new proxy) and `POST /api/v1/recommendations/:id/feedback`; Riverpod provider. `goal_input_screen.dart` — form with multi-line TextField + FilledButton, validates non-empty, navigates to LoadingScreen on submit. `loading_screen.dart` — `RecommendationLoadingScreen`: fires API call immediately on initState; cycles through 4 phases (each min 2s) with AnimatedBuilder animations: phase 0 = pulsing book icon, phase 1 = custom `_GraphPainter` (force-directed 7-node graph rotating via `animValue`), phase 2 = custom `_DocumentPainter` (scanning line traversing document), phase 3 = pulsing sparkle icon; navigates to ResultsScreen after all phases + API done. `results_screen.dart` — `RecommendationResultsScreen`: 3 states (error/empty/results); empty state shows "Donate some habits first to get personalised recommendations" + CTA; results show `_RecommendationCard` list; each card: title, body, rationale, ExpansionTile sources, inline comment TextField + send button (POST feedback); "Try a different goal" bottom bar navigates back. Added `POST /api/v1/recommend/generate` proxy in `app/routes/recommendRouter.js` (proxies to Python `POST /api/v1/llm/recommend`). Updated `mobile/lib/main.dart`: added import for `goal_input_screen.dart`, changed `/recommend` route builder from `RecommendScreen` to `GoalInputScreen`. All 221 Node.js tests pass. `flutter analyze lib/` passes with no issues.
- Files changed: mobile/lib/features/recommendation/ (5 new files), mobile/lib/main.dart (import + route), app/routes/recommendRouter.js (add /generate proxy), prd.json (US-112 passes=true)
- **Learnings for future iterations:**
  - Python `recommend.py` exposes `POST /api/v1/llm/recommend` on the recommender service (http://recommender:8000); add proxy in Node.js `recommendRouter.js` using existing `proxyToRecommender` helper — target URL includes full `/api/v1/` prefix unlike `/classify` and `/recommend/:userId` which are at root
  - Dart `unnecessary_underscores` lint fires on `(_, __)` double-underscore params — use `(context, _)` or `(_, child)` instead for AnimatedBuilder builders
  - Loading screen pattern: fire the async API call in `initState` (store result in state); run phase timer loop separately; after all phases, poll `_apiDone` flag with `Future.delayed(100ms)` loop before navigating
  - `_RecommendationCardState` reuses `recommendationFeatureServiceProvider` for feedback; uses the batch `recommendation_id` (not per-item) matching what's stored in MongoDB
  - `flutter analyze` is the quality gate for Flutter — run from inside `mobile/` directory
---
## 2026-03-20 - US-113
- What was implemented: Created `app/routes/kbRouter.js` — Express router proxying KB CRUD to API-service: GET / (list PDFs), POST /reindex (force re-index), POST / (multipart upload via raw body pipe), DELETE /:filename (delete). Registered in `v1Router.js` under `/kb` with admin/researcher role guard, passing `apiServiceUrl`. Updated `admin/src/components/sidebar.tsx` — added "Knowledge Base" (📚 icon) nav item between Questionnaires and Settings. Created `admin/src/app/(admin)/knowledge-base/page.tsx` — full Knowledge Base management UI: lists all PDFs grouped by category; each row shows filename, file size, summary badge (Ready/Pending), upload date, and Delete button (with confirmation dialog); header has Re-index button and Upload PDF button; upload modal accepts PDF file + category field. Created `admin/src/app/(admin)/knowledge-base/page.module.css`. `npm run build` (Next.js typecheck) passes. All 221 Node.js tests pass.
- Files changed: app/routes/kbRouter.js (new), app/routes/v1Router.js (import + mount /kb), admin/src/components/sidebar.tsx (add Knowledge Base nav item), admin/src/app/(admin)/knowledge-base/page.tsx (new), admin/src/app/(admin)/knowledge-base/page.module.css (new), prd.json (US-113 passes=true)
- **Learnings for future iterations:**
  - To pipe multipart/form-data from Express to the API-service without multer, iterate `for await (const chunk of req)` and `Buffer.concat(chunks)`, then pass as `body` to `fetch()` with the original `req.headers['content-type']` (which includes the boundary) — body-parser skips multipart content types so the stream is still available
  - Register POST /kb/reindex before POST /kb/:filename (if ever needed) to avoid route shadowing — in this router POST /reindex and POST / are different paths so order is fine
  - The API-service `/api/v1/kb` endpoints are already implemented in `routers/retrieve.py` — the Node.js router is just a thin proxy
  - Admin panel KB page uses the same CSS variable tokens (`--color-primary`, `--color-surface`, `--color-border`, `--color-text`, `--color-text-muted`, `--color-bg`) as questionnaires page — consistent design
---

## 2026-03-20 - US-115
- What was implemented: Created `API-service/prompts/refine_translation_de.txt` — German-specific prompt instructing LLM to refine a literal machine translation into natural, tone-preserving German. Created `API-service/routers/refine_translation_de.py` with POST `/api/v1/llm/refine-translation-de` (request: `{original, raw_translation}`, response: `{refined_translation}`); falls back to `raw_translation` when LLM returns empty string. Registered router in `API-service/main.py`. Added `translateToGerman(sentence, language, apiBase, translateUrl)` helper in `app/routes/habitsRouter.js`: (1) skips when `language` does NOT start with 'en'; (2) calls LibreTranslate source=en target=de for German draft; (3) calls `/api/v1/llm/refine-translation-de` with 10 s AbortController timeout; (4) warns and falls back to raw draft if LLM fails or times out. Updated Habit node CREATE cypher to include `translationDE: $translationDE` (null for non-English habits). Created `app/tests/integration/habits.de-translation.test.js` (3 tests: refined German stored, non-English habit gets null, LLM failure falls back to raw). Created `API-service/tests/test_refine_translation_de.py` (2 tests). Created `scripts/backfill-de-translations.js` migration script. All 227 Node.js tests pass; 2 Python tests pass; lint/format pass.
- Files changed: API-service/prompts/refine_translation_de.txt (new), API-service/routers/refine_translation_de.py (new), API-service/tests/test_refine_translation_de.py (new), API-service/main.py (register router), app/routes/habitsRouter.js (add translateToGerman helper, translationDE in CREATE), app/tests/integration/habits.de-translation.test.js (new), scripts/backfill-de-translations.js (new), prd.json (US-115 passes=true)
- **Learnings for future iterations:**
  - German translation uses a separate `/api/v1/llm/refine-translation-de` endpoint with a German-specific prompt; request omits `language` field (always EN→DE)
  - `translateToGerman` mirrors `translateAndRefine` but inverts the language guard: skips when language does NOT start with 'en'
  - LibreTranslate EN→DE: same POST pattern but `source: 'en', target: 'de'`
  - Backfill script pattern: query Neo4j for `(h:Habit) WHERE h.language STARTS WITH 'en' AND h.translationDE IS NULL`, then SET translationDE for each
  - Test isolation: use distinct mock hostnames (e.g. `mock-api-service-de`, `mock-libretranslate-de`) to avoid cross-test fetch mock collisions
---

## 2026-03-20 - US-117
- What was implemented: Added `flutter_localizations` (sdk) and `intl: ^0.20.2` to `pubspec.yaml` dependencies; added `generate: true` to the `flutter:` section. Created `mobile/l10n.yaml` with `arb-dir: lib/l10n`, `template-arb-file: app_en.arb`, `output-localization-file: app_localizations.dart`, `output-dir: lib/l10n`. Created `mobile/lib/l10n/app_en.arb` (keys: appTitle, donateHabit, exploreHabits, settings, profile) and `mobile/lib/l10n/app_de.arb` (same keys in German). Ran `flutter gen-l10n` — generated `app_localizations.dart`, `app_localizations_en.dart`, `app_localizations_de.dart` in `lib/l10n/`. Updated `main.dart`: added `flutter_localizations` and `l10n/app_localizations.dart` imports; added `localizationsDelegates` (AppLocalizations.delegate + 3 Global* delegates) and `supportedLocales` (en, de) to `MaterialApp.router`. `flutter analyze`: no issues.
- Files changed: mobile/pubspec.yaml (add deps + generate:true), mobile/pubspec.lock (updated), mobile/l10n.yaml (new), mobile/lib/l10n/app_en.arb (new), mobile/lib/l10n/app_de.arb (new), mobile/lib/l10n/app_localizations.dart (generated), mobile/lib/l10n/app_localizations_en.dart (generated), mobile/lib/l10n/app_localizations_de.dart (generated), mobile/lib/main.dart (imports + MaterialApp delegates), prd.json (US-117 passes=true)
- **Learnings for future iterations:**
  - `intl` version must match what `flutter_localizations` from the SDK pins — check with `flutter pub deps | grep intl`; as of Flutter 3.29+ it's `0.20.2`, not `0.19.0`
  - `synthetic-package` option in l10n.yaml is deprecated and must be removed (Flutter 3.27+); `output-dir: lib/l10n` is the correct way to place generated files in-tree
  - Generated files (`app_localizations*.dart`) live in `mobile/lib/l10n/` alongside the ARBs and are importable as `package:hhh/l10n/app_localizations.dart`
  - Import order in main.dart: `flutter_localizations` before `flutter_riverpod`, then local `l10n/app_localizations.dart` at end of import block
---

## 2026-03-20 - US-118
- What was implemented: Replaced all hardcoded English UI strings in donate_screen.dart, explore_screen.dart, admin_settings_screen.dart, and profile_screen.dart with AppLocalizations references. Added 45 new keys to app_en.arb (with @metadata) covering: snackbar messages, AppBar titles, button labels, tab labels, section headings, offline banners, radio subtitles, appearance/theme segment labels, and interpolated strings (completedOn, iDoThisCount, helpfulCount with String placeholders). Updated app_de.arb with German translations for all new keys (satisfying US-119 prerequisites). Corrected import path to use relative `'../l10n/app_localizations.dart'` (not the flutter_gen package path). flutter gen-l10n and flutter analyze both pass with no issues.
- Files changed: mobile/lib/l10n/app_en.arb (45 new keys), mobile/lib/l10n/app_de.arb (45 new German keys), mobile/lib/l10n/app_localizations.dart (regenerated), mobile/lib/l10n/app_localizations_en.dart (regenerated), mobile/lib/l10n/app_localizations_de.dart (regenerated), mobile/lib/screens/donate_screen.dart (l10n import + 6 strings), mobile/lib/screens/explore_screen.dart (l10n import + 12 strings), mobile/lib/screens/admin/admin_settings_screen.dart (l10n import + 12 strings), mobile/lib/screens/profile_screen.dart (l10n import + 15 strings), prd.json (US-118 passes=true)
- **Learnings for future iterations:**
  - Import path for generated l10n is RELATIVE not package: use `'../l10n/app_localizations.dart'` from screens/, `'../../l10n/app_localizations.dart'` from screens/admin/ — NOT `package:flutter_gen/gen_l10n/...`
  - Capture `AppLocalizations.of(context)!` at the top of build() or before async gaps in callbacks; it's safe to use the captured reference after await since context is still valid when mounted check passes
  - ARB interpolated string placeholders use `"type": "String"` when the value comes from string interpolation (`'${val}'`); this avoids int/num type mismatch when map values are dynamic
  - `const TabBar(tabs: [...])` must become non-const when tab text uses l10n — same for `SegmentedButton` segments and `Row` children containing l10n strings
  - German translations (app_de.arb) should be added in the same PR as English extraction to keep all ARBs in sync; US-119 acceptance criteria were already satisfied here
---

## 2026-03-20 - US-119
- What was implemented: Verified that `app_de.arb` already contains a German value for every key in `app_en.arb` (48/48 keys). Spot-checks: "Donate a Habit" → "Gewohnheit spenden", "Settings" → "Einstellungen", "Explore Habits" → "Gewohnheiten entdecken". Ran `flutter gen-l10n` (exit 0, no warnings) and `flutter analyze` (no issues). The generated `app_localizations_de.dart` was already present. No code changes were needed — translations were complete from US-118.
- Files changed: prd.json (US-119 passes=true)
- **Learnings for future iterations:**
  - When US-118 scaffolded the ARB files, it already populated all DE translations — US-119 is purely a verification story; just run `flutter gen-l10n` and `flutter analyze` to confirm completeness
  - `flutter gen-l10n` reads `l10n.yaml` at the project root (not in lib/) and exits 0 with no output when all translations are present
---

## 2026-03-20 - US-120
- What was implemented: Created `app/routes/usersRouter.js` with GET /me (returns user record including preferredLanguage, defaults to 'en' if no record exists) and PUT /me (upserts preferredLanguage; returns 400 if value is not 'en' or 'de'). Users stored in MongoDB `users` collection keyed by `userId` (from req.user.sub). Registered `/users` route in `v1Router.js` behind participant/admin/researcher role guard. Created `app/tests/integration/users.routes.test.js` with 9 tests: auth enforcement (401/403), GET default, PUT to de/en, GET after PUT, 400 for unsupported language, 400 for empty string. All 240 backend tests pass; lint/prettier pass.
- Files changed: app/routes/usersRouter.js (new), app/routes/v1Router.js (import + mount), app/tests/integration/users.routes.test.js (new), prd.json (US-120 passes=true)
- **Learnings for future iterations:**
  - Users collection is `users` (separate from `profiles`) — userId is the Keycloak sub claim
  - GET /me returns a synthetic default `{ userId, preferredLanguage: 'en' }` without persisting to DB when no record exists — this is intentional to avoid creating phantom records
  - PUT /me uses `{ $set: { userId, ...update } }` with `upsert: true` — this creates the record on first PUT
  - Prettier formatting in the app/ directory: run `npx prettier --write` in the app/ dir specifically (not in the project root)
---

## 2026-03-20 - US-121
- What was implemented: Created `mobile/lib/providers/locale_provider.dart` — StateNotifierProvider<LocaleNotifier, Locale> holding current locale; `setLocale(locale)` calls PUT /api/v1/users/me with `preferredLanguage` and updates state on success, returning bool. Created `mobile/lib/screens/user_settings_screen.dart` — Settings screen with Language dropdown (English / Deutsch) using InputDecorator + DropdownButtonHideUnderline + DropdownButton to avoid deprecated DropdownButtonFormField.value; shows success/failure snackbar. Updated `main.dart`: import user_settings_screen + locale_provider, added `locale: ref.watch(localeProvider)` to MaterialApp.router, added `/settings` StatefulShellBranch (branch 4, before admin). Updated `shell_screen.dart`: added Settings tab (icon: Icons.settings, path: /settings), updated _adminBranchIndex from 4 to 5. Updated `profile_screen.dart`: added gear icon in AppBar actions that pushes `/settings`. Flutter analyze: no issues.
- Files changed: mobile/lib/providers/locale_provider.dart (new), mobile/lib/screens/user_settings_screen.dart (new), mobile/lib/main.dart (import, locale, /settings branch), mobile/lib/screens/shell_screen.dart (Settings tab + adminBranchIndex=5), mobile/lib/screens/profile_screen.dart (AppBar settings icon), prd.json (US-121 passes=true)
- **Learnings for future iterations:**
  - `DropdownButtonFormField` `value` property is deprecated in Flutter 3.33+ — use `InputDecorator` + `DropdownButtonHideUnderline` + `DropdownButton` with `isDense: true` instead
  - `curly_braces_in_flow_control_structures` lint requires braces around single-statement if blocks even for early returns
  - Adding a new tab to StatefulShellRoute requires: (1) adding _TabConfig entry to shell_screen.dart, (2) updating _adminBranchIndex, (3) adding corresponding StatefulShellBranch in main.dart
  - `locale: locale` on MaterialApp.router + StateNotifier pattern causes instant locale change without restart
---

## 2026-03-20 - US-122
- What was implemented: Updated `locale_provider.dart` to implement startup locale loading with no flash: (1) Constructor calls `_initFromStorage()` which reads from `flutter_secure_storage` (`preferred_language` key) and sets state immediately (sync-ish, avoids flash on repeat visits); (2) Then fetches from GET /api/v1/users/me in background and updates state + storage if different; (3) `setLocale()` now also writes to secure storage so the next startup is instant. Added `loadPreference()` public method for post-auth refresh. Auth headers check for null token (unauthenticated case returns early). Flutter analyze: no issues.
- Files changed: mobile/lib/providers/locale_provider.dart (startup loading + secure storage caching), prd.json (US-122 passes=true)
- **Learnings for future iterations:**
  - "No flash" startup pattern: read secure storage in constructor synchronously (well, it's async but runs immediately), set state before first build, then refresh from API in background
  - `flutter_secure_storage` is already a dependency (used by theme_provider) — reuse same `FlutterSecureStorage()` singleton pattern with key constants
  - Auth headers returning empty `{}` (when no token) should be checked before API calls — return early to avoid spurious 401 errors in background init
  - The `loadPreference()` public method is for calling after auth completes (e.g., after login); background fetch in constructor handles the "already logged in on startup" case
---

## 2026-03-20 - US-123
- What was implemented: Updated `HabitNode` model to add `language` (String, ISO 639-1, e.g. 'en') and `hasTranslation` (bool, whether name is translated vs original) fields. Added `HabitNode.fromDonatedJson(json, requestedLang)` factory that maps `GET /api/v1/habits` response format (uuid, original, language, displayText) to HabitNode — name is displayText if available, else original; hasTranslation is true if language matches or displayText != original. Added `fetchDonatedHabits(String lang)` to `HabitService` that calls `GET /api/v1/habits?lang=X`. Updated `ExploreScreen` to: import localeProvider, track `_fetchedLang`, call `fetchDonatedHabits(lang)` instead of `fetchPublicHabits()`, detect locale changes in `build()` via addPostFrameCallback and refetch. Updated bottom sheet to show an orange translate icon + "Original (de)" label when `!node.hasTranslation`. Flutter analyze: no issues.
- Files changed: mobile/lib/models/habit_node.dart (language + hasTranslation fields + fromDonatedJson factory), mobile/lib/services/habit_service.dart (fetchDonatedHabits), mobile/lib/screens/explore_screen.dart (locale watch + refetch + language indicator), prd.json (US-123 passes=true)
- **Learnings for future iterations:**
  - POST-frame callback pattern for state-driven refetch: `WidgetsBinding.instance.addPostFrameCallback((_) => _fetchHabits())` in `build()` when locale differs from `_fetchedLang`
  - `HabitNode.fromDonatedJson` vs `fromJson`: two separate factories handle old (public) and new (donated) API response shapes — keeps backward compat
  - When `fetchDonatedHabits` returns habits without categories, the category filter bar becomes empty (which is handled by `_CategoryFilterBar` returning `SizedBox.shrink()` when categories list is empty)
---

## 2026-03-20 - US-124
- What was implemented: Added `GET /api/v1/surveys/:id/render?lang=en|de` route to `surveyRouter.js` — returns `{id, title, lang, jsonSchema}` where lang defaults to 'en' if absent or unsupported. Updated `DonateScreen` to import localeProvider, read `ref.read(localeProvider).languageCode` in `_initSurvey()`, and append `?lang=X` to the render URI using `Uri.replace(queryParameters: {'lang': lang})`. Added 4 integration tests: 401 without token, default lang=en, lang=de when requested, 404 for unknown survey. All 244 backend tests pass; flutter analyze passes.
- Files changed: app/routes/surveyRouter.js (add /:id/render route), app/tests/integration/surveys.routes.test.js (4 new render tests), mobile/lib/screens/donate_screen.dart (import locale_provider + pass ?lang), prd.json (US-124 passes=true)
- **Learnings for future iterations:**
  - The `/:id/render` route must be registered BEFORE `/:id` in Express, otherwise `render` would be matched as the id parameter — ordering matters
  - `Uri.parse(url).replace(queryParameters: {...})` is the Dart way to append query params to an existing URI
  - Survey render endpoint returns `jsonSchema` verbatim — locale/i18n of the actual questions is SurveyJS client-side (e.g. using SurveyJS locale setting); the `lang` field in the response tells the client which locale to activate
---

## 2026-03-20 - US-125
- What was implemented: Changed the backend-integration job's Neo4j service `--health-cmd` in `.github/workflows/ci.yml` from `cypher-shell -u neo4j -p password 'RETURN 1'` to `wget -q -O /dev/null http://localhost:7474 || exit 1`. The `cypher-shell` command fails in CI because the tool is not available in the Neo4j 5 community Docker image path, but the HTTP port 7474 is available once Neo4j is ready. No other jobs used cypher-shell as a health-check (the ontology-test job already used wget).
- Files changed: .github/workflows/ci.yml (backend-integration Neo4j --health-cmd), prd.json (US-125 passes=true)
- **Learnings for future iterations:**
  - Neo4j 5-community Docker image does NOT have `cypher-shell` in the PATH available for health-check commands — use `wget -q -O /dev/null http://localhost:7474 || exit 1` instead
  - The ontology-test job already had the correct wget health-check; only backend-integration was using the broken cypher-shell one
---

## 2026-03-20 - US-126
- What was implemented: Ran `npm uninstall deeplx` in `app/` — removed the `deeplx` package (and its 24 transitive dependencies) from `package.json` and regenerated `package-lock.json`. Confirmed no source files reference `deeplx` (only `package.json` had it). All 244 tests pass.
- Files changed: app/package.json (deeplx removed), app/package-lock.json (regenerated), prd.json (US-126 passes=true)
- **Learnings for future iterations:**
  - `npm uninstall <pkg>` both removes from package.json AND removes from node_modules AND updates package-lock.json in one command
  - deeplx was a legacy translation package; LibreTranslate HTTP is the translation mechanism now
---

## 2026-03-20 - US-127, US-128, US-129
- What was implemented: Marked three audit-finding tracker stories as passing — all were already resolved by previously completed stories: US-127 (English habit DE translation) resolved by US-115 (passes=true), US-128 (habits API lang param) resolved by US-116 (passes=true), US-129 (DonateScreen locale) resolved by US-124 (passes=true). No code changes needed.
- Files changed: prd.json (US-127, US-128, US-129 passes=true)
- **Learnings for future iterations:**
  - Audit-finding tracker stories (US-127/128/129) are just visibility markers — check if the referenced resolution story already passes=true before implementing anything
---
---
- US: US-130
- What was implemented: Extracted all hardcoded English strings from admin screens (admin_devices_screen.dart, admin_habits_screen.dart, admin_participant_detail_screen.dart, admin_participants_screen.dart, admin_surveys_screen.dart). Added 67+ new ARB keys to app_en.arb and matching German translations in app_de.arb. Replaced all hardcoded strings with AppLocalizations.of(context)! calls. Ran flutter gen-l10n and flutter analyze — no issues.
- Files changed: mobile/lib/l10n/app_en.arb, mobile/lib/l10n/app_de.arb, mobile/lib/screens/admin/admin_devices_screen.dart, mobile/lib/screens/admin/admin_habits_screen.dart, mobile/lib/screens/admin/admin_participant_detail_screen.dart, mobile/lib/screens/admin/admin_participants_screen.dart, mobile/lib/screens/admin/admin_surveys_screen.dart, mobile/lib/l10n/app_localizations_en.dart, mobile/lib/l10n/app_localizations_de.dart, prd.json
- **Learnings for future iterations:**
  - Always run flutter gen-l10n after modifying ARB files to regenerate the Dart localization classes before running flutter analyze

## 2026-03-21 - US-135
- What was implemented: Senior infrastructure engineer review of `docker-compose.yml`, `docker-compose.prod.yml`, `.env.example`, `stack.env`, all 5 Dockerfiles (app, admin, API-service, fuseki, backup-service), `backup-service/backup.sh`, and `backup-service/restore.sh`. Report written to `tasks/review-infrastructure.md` covering 7 sections with 5 Critical, 9 Major, and 7 Minor findings. Every finding tied to specific file and line range.
- Files changed: tasks/review-infrastructure.md (new), prd.json (US-135 passes=true)
- **Learnings for future iterations:**
  - Critical infrastructure findings: (1) Keycloak uses `KC_DB=dev-file` in `docker-compose.prod.yml:246` — not production-ready; (2) `.env` with real credentials is in Git history despite US-071; all those secrets need rotation; (3) Traefik `--api.insecure=true` + `--api.debug=true` in dev config expose dashboard without auth; (4) docker-compose.yml defaults include `MONGO_PASSWORD=admin`, `KEYCLOAK_ADMIN_PASSWORD=admin`, `NEXTAUTH_SECRET=change-me-in-production`; (5) `stack.env` (production env template) is committed to Git
  - `backup-service/restore.sh:48` looks for `$RESTORE_DIR/mongodb/` but `backup.sh:79` creates `$RESTORE_DIR/mongo/` — MongoDB restore is silently skipped on every run
  - Fuseki, the Python recommender, and the Next.js admin all have NO healthcheck stanzas — `depends_on: condition: service_healthy` cannot gate on them
  - Neo4j procedure allowlist is a wildcard `n10s.*` — `n10s.rdf.import.fetch` can load external URLs from any authenticated Neo4j user
  - LibreTranslate `LT_REQ_LIMIT=0` in production means unlimited unauthenticated requests
  - `mongo` image in dev compose is unpinned (`:latest`); prod correctly pins to `mongo:7.0`
  - Positives: all images run as non-root users; backup covers all 4 databases with alerting; dev/prod separation via override file is clean; Traefik TLS + ACME correctly configured in prod
---

## 2026-03-21 - US-138
- What was implemented: Applied Critical and Major backend improvements from review-backend.md. C-1: moved `router.use(limiter)` to after `router.use(authenticate)` in v1Router.js so `req.user.sub` is available for per-user rate limiting. C-2+C-6: added `iss`/`aud` JWT validation (env-gated via `KEYCLOAK_JWT_ISSUER`/`KEYCLOAK_JWT_AUDIENCE`) and 24-hour JWKS TTL cache with on-miss force-refresh in auth.js. C-3: replaced per-query Neo4j driver creation in habitsRouter.js with a single `_neo4jDriver` instance created at factory time. C-4: added `X-Internal-Secret` header guard to internalRouter.js (fails closed if `INTERNAL_API_SECRET` not set); WS test updated to pass and use the secret. C-5: added IDOR guard on `/:userId` and `/:userId/history` routes in recommendRouter.js — participants can only access their own data; recommend tests updated. M-7 (partial): consolidated duplicate `translateToGerman`/`translateAndRefine` into a single parameterized `translate()` helper with LibreTranslate timeout (10s AbortController); full service-layer extraction deferred. M-8: replaced in-memory `.toArray().slice()` pagination with `.skip().limit()` + `countDocuments()` in adminRouter.js. M-9: added `sentence` length <= 1000 and `language` ISO-639-1 allowlist validation in `/habits/donate`. M-10: added `console.error` to all bare `catch {}` blocks across 8 router files (45+ blocks) via perl batch replacement. M-12: removed unused frontend deps (`d3`, `jquery`, `bootstrap`, `@popperjs/core`, `node-sessionstorage`, `js-yaml`, `survey-core`, `survey-js-ui`) from package.json. M-13: replaced sequential per-phrase/per-mapping Neo4j writes with UNWIND batch queries (2 queries instead of N). M-23: LibreTranslate timeout now included in consolidated translate() helper. Minor 24: stripped `_id` from questionnaire-responses `/me` endpoint. All deferred findings documented with written reasons in review-backend.md.
- Files changed: app/middleware/auth.js, app/routes/v1Router.js, app/routes/habitsRouter.js, app/routes/internalRouter.js, app/routes/recommendRouter.js, app/routes/adminRouter.js, app/routes/surveyRouter.js, app/routes/questionnairesRouter.js, app/routes/kbRouter.js, app/routes/onboardRouter.js, app/routes/recommendationsRouter.js, app/routes/questionnaireResponsesRouter.js, app/package.json, app/tests/integration/ws.recommendations.test.js, app/tests/integration/recommend.routes.test.js, app/tests/integration/admin.progress.test.js, app/tests/integration/habits.donate.test.js, tasks/review-backend.md, prd.json (US-138 passes=true)
- **Learnings for future iterations:**
  - Rate limiter must run AFTER auth middleware if keyGenerator uses `req.user.sub` — otherwise `req.user` is null and all requests fall back to IP-based limits
  - JWKS TTL cache: use `Date.now() - lastFetchedAt > TTL_MS` pattern; force-refresh on key-not-found before returning 401 to handle Keycloak key rotation gracefully
  - Neo4j driver: create once at factory scope (`const _neo4jDriver = neo4jRun ? null : neo4j.driver(...)`); check `neo4jRun` to avoid creating driver in test environment
  - Internal shared-secret pattern: `req.headers['x-internal-secret']` vs env `INTERNAL_API_SECRET`; fail closed (403) if secret not configured
  - UNWIND batch pattern for Neo4j: `UNWIND $rows AS row MERGE (c:Context {...})` — test mocks must handle `params.rows` array, not individual `params.text`/`params.dimension` fields
  - When using `Promise.all([collection.countDocuments(filter), collection.find(filter).skip(n).limit(m).toArray()])`, mock must implement both `countDocuments()` and a chainable cursor with `.skip()`, `.limit()`, `.toArray()`
  - perl bulk catch replacement: `perl -i -pe 's/^(\s*)\} catch \{$/$1} catch (err) {\n$1  console.error("[route] Error:", err);/'` preserves indentation and works on all nesting levels
---

## 2026-03-21 - US-139
- What was implemented: Consistency pass across the full codebase. (1) Error response shape: fixed `surveyController.js` `submitSurvey` — was using `{ status: 'error', message: err.message }`, changed to `{ error: 'Server error' }` with `console.error('[route] Error:', err)` — now consistent with all v1 routes. (2) Flutter pubspec.yaml: pinned security-sensitive packages to exact resolved versions: `flutter_appauth: 8.0.3` (was `^8.0.1`) and `flutter_secure_storage: 9.2.4` (was `^9.2.3`). (3) Verified locale codes: `'en'`/`'de'` ISO 639-1 codes used identically across Flutter, backend, LibreTranslate calls — already consistent. (4) Verified error shape: all v1 routes use `{ error: '...' }` consistently after US-138. (5) Verified logging: all v1 catch blocks use `console.error('[route] Error:', err)` consistently after US-138. (6) Updated `tasks/review-system-coherence.md` with Resolution notes for all 6 Critical, 10 Major, and 10 Minor findings — added Section 9 with field mapping table, locale consistency confirmation, error shape audit, logging audit, and Flutter dep pinning summary. Quality: 247/247 backend tests pass, flutter analyze zero issues, ESLint clean.
- Files changed: app/controllers/surveyController.js (error shape + console.error), mobile/pubspec.yaml (pin flutter_appauth + flutter_secure_storage), tasks/review-system-coherence.md (Resolution notes + Section 9), prd.json (US-139 passes=true)
- **Learnings for future iterations:**
  - `surveyController.js` is in `app/controllers/` (not `app/routes/`) — the US-138 batch perl replacement only targeted `app/routes/*.js`. Always check controllers separately when doing a catch-block audit.
  - Pinning Flutter deps: read resolved version from `pubspec.lock` (not the constraint in pubspec.yaml) before pinning — the lock file has the actual installed version (e.g., `^8.0.1` resolved to `8.0.3`).
  - `flutter pub get` runs automatically when pubspec.yaml changes and modifies `pubspec.lock` — the 2 dependency changes (test_api, matcher) shown in `flutter analyze` output are transitive and expected.
  - The consistency review in Section 9 of review-system-coherence.md is the canonical field-mapping reference for Flutter↔Backend↔Neo4j — update it whenever API contracts change.
---

## 2026-03-21 - US-140
- What was implemented: Updated Flutter and mobile documentation. (1) docs/guides/developer-onboarding.md: added `flutter gen-l10n` step after `flutter pub get` in section 5, added `flutter gen-l10n` to Flutter test section, added "Donation flow (WebView)" subsection explaining the JS bridge flow, added "Missing localisation methods after editing ARB files" pitfall entry, corrected Flutter test expected output to remove stale exact count. (2) docs/MANUAL-en.md: added "Settings / Language" section describing the language dropdown in the Settings screen (navigation, steps, persistence, survey language effect). (3) docs/MANUAL-de.md: added German equivalent "Einstellungen / Sprache" section. (4) docs/guides/flutter-architecture.md: new file covering overview, folder structure, routing, state management, localisation pipeline, auth token management, AppConfig env vars, services layer, WebView donation flow, and testing.
- Files changed: docs/guides/developer-onboarding.md, docs/MANUAL-en.md, docs/MANUAL-de.md, docs/guides/flutter-architecture.md (new), prd.json (US-140 passes=true)
- **Learnings for future iterations:**
  - MANUAL-en/de.md describe the OLD web-browser flow (EJS experiment groups 1-4); the native Flutter app replaces that UI — do NOT update the experiment-group screenshots section as it describes backend study logic, not the Flutter screens
  - The donation screen is WebView-based (server-rendered SurveyJS), not a native Flutter form — document it as such to avoid confusion for future developers
  - flutter gen-l10n must always precede flutter analyze and flutter test — this is now documented in two places in the onboarding guide
---
## 2026-03-21 - US-142
- What was implemented: Updated all infrastructure and deployment documentation. (1) `DEPLOYMENT.md`: updated service list to include h3-keycloak, h3-recommender, h3-admin; added Keycloak and OpenAI required overrides and LibreTranslate optional overrides to Portainer step; added Step 5 pre-deploy volume permission note for UID 1032 (LibreTranslate chown); added Step 4 post-deploy migration section with `backfill-de-translations.js` and `migrate-habits-bcio.js` usage; updated network diagram and URLs table with all current services. (2) `docs/guides/admin-guide.md`: added Section 9 — Language Settings for Participants documenting the new Settings tab language dropdown and how preferredLanguage is stored/used. (3) `docs/guides/admin-guide-de.md`: added Section 9 German translation + updated ToC to include Sections 8 and 9 (was missing Section 8 entry). (4) `docs/runbook.md`: added "Neo4j failed to start — data directory permissions" (UID 7474 chown fix) and "LibreTranslate down or returning empty translations" (UID 1032 fix + OpenAI API key diagnosis path) troubleshooting entries; updated ToC with direct links to all 11 troubleshooting entries. (5) `.env.example`: complete rewrite — all env vars now present with one-line comments covering Traefik, Keycloak (JWKS URL, JWT issuer/audience, client IDs/secrets), Fuseki, LibreTranslate (both full-URL and component vars), MongoDB, Neo4j, API-service, Redis, OpenAI (key + model + temperature), reCAPTCHA, Mail, internal API secret, and Backup. (6) `CHANGELOG.md`: added [1.1.0] entry covering all changes in this PRD branch with sections for donation pipeline, translation, questionnaires, user preferences, recommendations, onboarding, admin panel, KB, security improvements, test counts, docs; updated comparison URLs.
- Files changed: DEPLOYMENT.md, docs/guides/admin-guide.md, docs/guides/admin-guide-de.md, docs/runbook.md, .env.example, CHANGELOG.md, prd.json (US-142 passes=true)
- **Learnings for future iterations:**
  - The German admin guide ToC was missing its own Section 8 entry — when adding a new section, always check that the ToC in the same file is also updated
  - `.env.example` was missing ~15 env vars added after v1.0.0: KEYCLOAK_JWKS_URL, KEYCLOAK_JWT_ISSUER, KEYCLOAK_JWT_AUDIENCE, KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID, KEYCLOAK_ADMIN_CLIENT_ID, KEYCLOAK_ADMIN_CLIENT_SECRET, API_SERVICE_URL, INTERNAL_API_SECRET, REDIS_URL, OPENAI_API_KEY, LLM_MODEL, LLM_TEMPERATURE — always update .env.example when adding process.env references
  - The LibreTranslate UID 1032 constraint is documented in `docker-compose.prod.yml:169-183` but was not surfaced anywhere accessible to ops — the key fact is `sudo chown -R 1032:1032 /mnt/data/appdata/hhh/translate` must run before first deploy
  - CHANGELOG sections should map 1:1 to PRD phases; the 1.1.0 entry covers ~40 US stories — keeping it grouped by functional area (donation, translation, questionnaires, etc.) is more readable than listing individual stories
---

## 2026-03-21 - US-143
- What was implemented: Updated all architecture documentation. (1) `docs/architecture.md`: completely rewritten with an updated Mermaid component diagram showing all 11 services (including new API-service and Admin Panel), three new sequence diagrams (end-to-end donation pipeline, language/locale flow, auth flow), data storage rationale table, admin panel auth section, and updated per-service reference table; also updated Neo4j schema section to document Habit/Context/BCIOConcept nodes vs legacy hhh__ nodes. (2) `docs/migration.md`: added new "Schema Changes in ralph/hhh-platform-unified" section documenting translationEN/translationDE on Habit nodes (US-114), preferredLanguage in MongoDB users collection, and missing Neo4j uniqueness constraints/indexes for new schema labels. (3) `AUDIT.md`: cleared all stale AUDIT-001+ findings (superseded by US-131–US-136 reviews) and reserved the file for US-144 final audit output. (4) `DOCUMENTATION.md`: added "Documentation Index" section listing every file in docs/ with a one-line description, organised into Root-Level, docs/ Architecture & Design, docs/api/, docs/guides/, docs/ User Manuals, and docs/assets/ categories.
- Files changed: docs/architecture.md, docs/migration.md, AUDIT.md, DOCUMENTATION.md, prd.json (US-143 passes=true)
- **Learnings for future iterations:**
  - The existing architecture.md was accurate for the old EJS/server-rendered app but did not reflect the Admin Panel (Next.js), API-service (FastAPI), or the new donation pipeline sequence — needed full rewrite
  - DOCUMENTATION.md is very long (~3400+ lines) and still describes the old EJS-based architecture in many places; the Documentation Index section was added at the end of the file rather than rewriting the whole document
  - AUDIT.md stale findings predated US-131–US-142 review/improvement cycle; clearing them for US-144 prevents confusion between old and new audit findings
---
## 2026-03-21 - US-144
- What was implemented: Final system audit. (1) `AUDIT.md`: completely rewritten with date stamp (2026-03-21), Executive Summary, Component scorecards (Flutter, Backend, Neo4j, CI/CD, Infrastructure — each scored on Code Quality, Test Coverage, Security, Documentation, Consistency 1–5 with written justification — all dimensions 3/5 or above), 12 Open Findings (4 Critical, 8 Major) tagged with component/severity/owning story, Resolved Findings summary covering US-137–US-143, 10 Recommended Next Actions ordered by value, and System Strengths section identifying 8 architectural strengths. (2) `.github/workflows/ci.yml`: fixed Critical CI breakage — updated all `actions/checkout@v6` → `@v4`, `actions/setup-node@v6` → `@v4`, `actions/upload-artifact@v6` → `@v4` (non-existent versions that caused every CI job to fail at action-resolution step since branch creation).
- Files changed: AUDIT.md (complete rewrite), .github/workflows/ci.yml (fix action versions), prd.json (US-144 passes=true)
- **Learnings for future iterations:**
  - The @v6 action version fix was the single highest-leverage change in this story — one sed command unblocked all 10 CI jobs
  - AUDIT.md should always be cleared/reserved before the final audit story runs (done in US-143) so there is no confusion between cycle-specific and historical findings
  - Scoring components honestly (all ≥ 3/5) required genuine improvements across the cycle — the audit acceptance criterion "no score below 3/5" serves as a useful forcing function for completing improvement stories before the audit
  - The dual Neo4j schema (OF-01) is the highest-priority remaining finding: newly donated habits are invisible to all participants and the stats always show 0 for new pipeline data
---

## 2026-03-21 - US-146
- What was implemented: Updated all backend routes that queried the old `hhh__Habit` Neo4j schema to use the new `Habit` schema. (1) `habitsRouter.js` GET /public: replaced `MATCH (h:hhh__Habit) OPTIONAL MATCH (h)-[:hhh__hasBehavior]->()-[:hhh__partOf]->(es)` with `MATCH (h:Habit) RETURN h.uuid AS id, h.sentence AS name, null AS category, null AS bcioClass`. (2) `habitsRouter.js` GET /stats total query: `MATCH (h:hhh__Habit)` → `MATCH (h:Habit)`; byCategory query: replaced hhh__Group label pattern with `WITH h.language AS cat, count(h) AS cnt`. (3) `adminRouter.js` participant habit count: `MATCH (h:hhh__Habit {hhh__source: $userId})` → `MATCH (h:Habit {userID: $userId})`. (4) `habit.controller.test.js` mock: changed `cypher.includes('hhh__Habit')` guard to `cypher.includes('h.sentence AS name')` so /public tests pass. All 247 tests pass; lint passes; `grep -rn 'hhh__Habit' app/` returns no results.
- Files changed: app/routes/habitsRouter.js, app/routes/adminRouter.js, app/tests/unit/habit.controller.test.js, prd.json (US-146 passes=true)
- **Learnings for future iterations:**
  - New `Habit` nodes store participant ID as `userID` (not `hhh__source`) — admin habit-count query must use `{userID: $userId}`
  - `GET /public` category field is now always `null` since new `Habit` nodes have no study-group relationship; downstream consumers should treat category as optional
  - Stats `byCategory` now groups by `h.language` (ISO 639-1 codes like 'en', 'de') rather than `hhh__GroupN` labels
  - The `.replace('hhh__', '')` call in byCategory mapping is now a no-op for language-code values but harmless to keep
---

## 2026-03-21 - US-147
- What was implemented: Added two new statements to `neo4j/init/constraints.cypher`: (1) `CREATE CONSTRAINT habit_uuid IF NOT EXISTS FOR (h:Habit) REQUIRE h.uuid IS UNIQUE` — prevents duplicate Habit nodes from the donate pipeline; (2) `CREATE INDEX context_text_dim IF NOT EXISTS FOR (c:Context) ON (c.text, c.dimension)` — speeds up MERGE lookups by text+dimension during habit donation. Both statements use IF NOT EXISTS guards, making them fully idempotent. All 247 backend tests pass unchanged.
- Files changed: neo4j/init/constraints.cypher (added constraint + index for new Habit/Context schema), prd.json (US-147 passes=true)
- **Learnings for future iterations:**
  - The `tests/ontology/test-ontology.sh` suite only runs Cypher queries against a live Neo4j instance — it does NOT parse or validate constraints.cypher syntax. So changes to that file cannot break the ontology test suite directly.
  - New Habit/Context schema (donate pipeline) was completely missing from constraints.cypher — only old `hhh__` schema was covered. Always check both schemas when adding constraints.
  - `IF NOT EXISTS` guards are mandatory on all constraint/index statements in this file so the init script is safe to re-run on an existing database.
---

## 2026-03-21 - US-149
- What was implemented: Gated tag-push production deploys behind CI. Added `tags: ["v*"]` to `ci.yml`'s `on.push` so CI runs when a v* tag is pushed. In `deploy.yml`: removed the `branches: master` filter from `workflow_run` so the trigger fires for tag CI completions too; removed the `(github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v'))` path from the deploy job's `if` condition; added a "Verify CI passed" first step that explicitly exits 1 if `conclusion != 'success'`; updated the deploy job `if` to allow deployment only when `head_branch == 'master'` or `startsWith(head_branch, 'v')`. The `release` job (Flutter APK/web artifact upload) retains its `push: tags: v*` trigger since it does not deploy to production infrastructure. Updated `tasks/review-cicd.md` marking C5 as resolved (US-149).
- Files changed: .github/workflows/ci.yml (add tags trigger), .github/workflows/deploy.yml (remove push:tags from deploy job, add explicit CI check step, rework if condition), tasks/review-cicd.md (C5 resolved), prd.json (US-149 passes=true)
- **Learnings for future iterations:**
  - When CI only runs on branch pushes (not tag pushes), `workflow_run` with `branches: master` never fires for tag-triggered events — you MUST add `tags: ["v*"]` to ci.yml's `on.push` first
  - For tag-triggered CI runs, `github.event.workflow_run.head_branch` contains the tag name (e.g., "v1.2.3") — use `startsWith(..., 'v')` to detect release tags
  - The `release` job (build artifacts, upload to GitHub Release) is safe to keep on `push: tags: v*` — it's not a production deploy, so bypassing CI is acceptable risk; only the `deploy` job (SSH prod deploy) needs gating
  - Removing `branches: master` from the workflow_run filter means it fires for ALL CI completions (PRs, feature branches, tags) — the deploy job's `if` condition must be tight enough to filter correctly
---

## 2026-03-21 - US-150
- What was implemented: Added `_esc()` method to `DbClient` in `SparqlDatabase.js` (identical to `Neo4jDatabase.js:187-194`) and applied it to all 10 user-controlled string literal interpolation points: `userId` in `addDonor()`; `donation.value`, `donation.language`, `donation.source` in `addHabit()`; `context.data`, `donation.language`, `donation.source` in `_appendContext()`; `behavior.data`, `donation.language`, `donation.source` in `_appendBehavior()`. IRI fragments (`context.value`, `experimentalSetting.group`) are safe — `context.value` only passes the `type==='context'` filter if the label name is a recognized key (TimeReference, PhysicalSetting, People, InternalState, PriorBehavior, Reasoning), and `experimentalSetting.group` comes from static conditional logic yielding Group1–4 or undefined. Added Resolution note to the SPARQL injection finding in tasks/review-backend.md. All 247 tests pass; lint passes.
- Files changed: app/utils/SparqlDatabase.js (_esc() method + 10 escaped interpolations), tasks/review-backend.md (Resolution note), prd.json (US-150 passes=true)
- **Learnings for future iterations:**
  - SPARQL `INSERT DATA` does NOT support bind parameters — string escaping is the correct fix for literal values (escape `\`, `"`, `\n`, `\r`)
  - IRI fragment injection (`hhh:${value}`) is safe when the value is constrained by the label-type mapping in `Donation` constructor — only recognized label names get `type==='context'` and pass the filter
  - `Neo4jDatabase.js` already had the correct `_esc()` pattern; `SparqlDatabase.js` was missing it — always verify both SPARQL and Neo4j clients when fixing injection issues
  - `SparqlDatabase.test.js` integration tests use `ENABLE_INTEGRATION=1` flag and skip without a live Fuseki — unit assertions for escaping should use the `addHabit`/`_appendBehavior` etc. methods directly without needing Fuseki
---

## 2026-03-21 - US-151
- What was implemented: Added `VALID_GROUPS` constant (`new Set(['hhh__Group1','hhh__Group2','hhh__Group3','hhh__Group4'])`) to `adminRouter.js` above the factory function. Added second-layer validation inside the neo4jRun block: after `newLabel = labelMap[group]`, checks `VALID_GROUPS.has(newLabel)` and returns HTTP 400 `{ error: 'Invalid group' }` if not present — preventing any Cypher string with the label from being constructed. Added unit test in `admin.controller.test.js` that starts a fresh Express server with a tracked `neo4jRun` mock, sends a malicious group string (`G1\`; MATCH (n) DETACH DELETE n //`), asserts HTTP 400, and asserts neo4jRun was never called. Updated `AUDIT.md` to mark OF-06 as resolved. All 248 tests pass; lint/prettier pass.
- Files changed: app/routes/adminRouter.js (VALID_GROUPS constant + newLabel validation), app/tests/unit/admin.controller.test.js (malicious input test), AUDIT.md (OF-06 resolved), prd.json (US-151 passes=true)
- **Learnings for future iterations:**
  - The injection site is `adminRouter.js` line ~440: `SET d:\`${newLabel}\`` — backtick-quoted label in Cypher; the existing `group` → `['G1'..'G4']` check already prevents exploitation, but a second defense-in-depth guard on the actual label string is required by the story
  - `VALID_GROUPS` should be declared at module scope (before `createAdminRouter`) so it is not re-created on every request
  - Unit test for injection must use its own server instance with `neo4jRun` injected — the shared `before()` server uses no `neo4jRun`, so a second server is the cleanest approach
  - Prettier formats the test file on first `npm test` run — always run `npm test` (not just `node --test`) to catch format violations before committing
---

## 2026-03-21 - US-152
- What was implemented: Added `python-api-test` job (job 10) to `.github/workflows/ci.yml`. Job uses ubuntu-latest, `actions/setup-python@v5` with Python 3.11 and pip cache (`cache-dependency-path: API-service/requirements.txt`), installs `pip install -r requirements.txt`, and runs `pytest tests/ -v` with `OPENAI_API_KEY=placeholder` env var (prevents warning noise — no real key needed since all tests mock the LLM client). Job added to `ci-passed` gate's `needs` list. Updated `tasks/review-cicd.md` marking CV1 (referenced as OF-08 in PRD) as `✅ Resolved (US-152)` in both the finding body and the summary table.
- Files changed: .github/workflows/ci.yml (new python-api-test job, updated ci-passed needs), tasks/review-cicd.md (CV1 marked resolved), prd.json (US-152 passes=true)
- **Learnings for future iterations:**
  - All Python API-service tests use `unittest.mock` patches — no Redis service container needed; `_get_redis` is patched to return `None` or a mock in every test
  - `OPENAI_API_KEY` is read at module import time in `llm_client.py` — setting it to any non-empty placeholder suppresses the warning log without requiring a real key
  - `actions/setup-python@v5` `cache: pip` requires `cache-dependency-path` pointing to the requirements file, otherwise it caches nothing useful
  - `pytest tests/ -v` must be run from `API-service/` as working-directory so that `from main import app` resolves correctly (main.py is at API-service root, not project root)
---

## 2026-03-21 - US-154
- What was implemented: Added `http_mock_adapter: ^0.6.1` to `mobile/pubspec.yaml` dev_dependencies. Added 401→`UnauthorisedException` try-catch in `HabitService.fetchDonatedHabits` (imports `core/exceptions.dart`). Created `mobile/test/services/habit_service_test.dart` (3 tests: `?lang=en` sent as query param, `lang=de` forwarded, 401 throws `UnauthorisedException`), `mobile/test/services/stats_service_test.dart` (2 tests: full stats parse + empty-response graceful defaults), `mobile/test/services/recommendation_service_test.dart` (3 tests: happy path with ragCitation, empty list, 500 → DioException). All 8 tests pass; `flutter analyze` reports no issues. Updated `tasks/review-flutter.md` row C-2/T-1 from DEFERRED to RESOLVED.
- Files changed: mobile/pubspec.yaml (http_mock_adapter dev dep), mobile/pubspec.lock, mobile/lib/services/habit_service.dart (401 handling + exceptions import), mobile/test/services/habit_service_test.dart (new), mobile/test/services/stats_service_test.dart (new), mobile/test/services/recommendation_service_test.dart (new), tasks/review-flutter.md (C-2 resolved), prd.json (US-154 passes=true)
- **Learnings for future iterations:**
  - `http_mock_adapter` DioAdapter matches routes by exact string: register the FULL URL (e.g. `'http://localhost:3000/api/v1/habits'`) not just the path (`'/habits'`) because services call `_dio.get('$_baseUrl/...')` with absolute URLs — the adapter's `doesRouteMatch` does `actual == expected` string comparison, so relative paths never match absolute request paths
  - Create Dio without `baseUrl` in service tests; let the mock just use the same absolute URL the service constructs
  - `FullHttpRequestMatcher` also matches `headers` — since `_StubAuthService` returns `null` for an unauthenticated stub and the stats/no-auth tests have no Authorization header, the mock must not set `headers:` constraint (or it must match the actual headers including auth)
  - `server.throws(statusCode, DioException(...))` is the correct way to simulate error responses in http_mock_adapter; the `response.statusCode` on the inner `DioException` is what the service's `e.response?.statusCode` check reads
---
## 2026-03-21 - US-155
- What was implemented: Fixed MongoDB restore path mismatch in `backup-service/restore.sh`. Changed `$RESTORE_DIR/mongodb` to `$RESTORE_DIR/mongo` on lines 48 and 55 to match the `--out="$BACKUP_DIR/$DATE/mongo"` path used in `backup-service/backup.sh:79`. Added a sync comment above the mongorestore call in restore.sh and above the mongodump call in backup.sh noting the directory names must stay in sync.
- Files changed: backup-service/restore.sh (mongodb → mongo, added sync comment), backup-service/backup.sh (added sync comment), tasks/review-infrastructure.md (M5 marked ✅ Resolved US-155), prd.json (US-155 passes=true)
- **Learnings for future iterations:**
  - The backup-service/ directory contains its OWN restore.sh (the original), separate from scripts/restore.sh (a more modern version created in a later story) — both exist; the bug was in backup-service/restore.sh which is actually mounted into the Docker backup service container
  - The `RESTORE_DIR` variable in backup-service/restore.sh is a temp dir set to `/tmp/restore-$$`; the archive is extracted there and the subdirectory names directly correspond to what backup.sh creates inside `$BACKUP_DIR/$DATE/`
---

## 2026-03-21 - US-157
- What was implemented: Created `docker-compose.local.yml` at repo root for Mac local development. Includes: app, mongo, neo4j, keycloak, translate, recommender, fuseki. Excludes: proxy (Traefik), backup, admin, mongo-express. All services expose ports directly (app→3000, mongo→27017, neo4j→7474+7687, keycloak→8080, translate→5000, recommender→8001, fuseki→3030). app service uses `command: node --watch app.js` for built-in hot-reload. All services use `env_file: .env`. Named volumes use `-local-` suffix to avoid colliding with production volume names. Keycloak uses `KC_DB=dev-file` (simple, no separate postgres needed for local dev). `docker compose -f docker-compose.local.yml config` validates without errors.
- Files changed: docker-compose.local.yml (new), prd.json (US-157 passes=true)
- **Learnings for future iterations:**
  - Local volumes use `-local-` suffix (e.g., `neo4j-local-data`) to avoid name collisions with production volumes from docker-compose.yml
  - recommender internal port is 8000 (FastAPI default) but mapped to 8001 on the Mac host to avoid conflicts
  - keycloak in local mode uses `KC_DB=dev-file` and `start-dev` — no separate postgres dependency needed
  - `env_file: .env` on all services means a single `.env` file covers all service configuration for local dev
---

## 2026-03-21 - US-158
- What was implemented: Added nodemon dev workflow to the backend. Added `"dev": "nodemon app.js"` to scripts in `app/package.json`. Added `nodemon` (^3.1.14) to `devDependencies`. Created `app/nodemon.json` watching `controllers/`, `routes/`, `services/`, `middleware/`, `utils/` directories with 500ms delay. npm run lint passes.
- Files changed: app/package.json (dev script + nodemon devDependency), app/package-lock.json (nodemon installed), app/nodemon.json (new), prd.json (US-158 passes=true)
- **Learnings for future iterations:**
  - nodemon.json `watch` array uses directory names relative to `app/` (not glob patterns) — nodemon handles the recursive watch automatically
  - The `delay` field is in milliseconds in nodemon.json (500 = 0.5s)
  - `npm install --save-dev nodemon@^3.1.0` resolves to 3.1.14 (latest stable); package.json is updated to `^3.1.14` by npm
---

## 2026-03-21 - US-160
- What was implemented: Created `Makefile` at repo root with 8 targets: `help` (default, lists all targets with descriptions), `dev` (docker compose local up -d), `stop` (docker compose down), `seed` (cd app && npm run seed), `logs` (docker compose logs -f app), `logs-all` (docker compose logs -f), `ios` (cd mobile && flutter run -d iPhone), `reset` (stop + down -v + dev + seed). Each target has a `## comment` on the same line so `make help` can extract and display them with colour formatting.
- Files changed: Makefile (new), prd.json (US-160 passes=true)
- **Learnings for future iterations:**
  - `make help` grep pattern requires the `## comment` to be on the SAME LINE as the target (e.g. `dev: ## description`), not on a separate preceding line — separate-line comments are not matched by the standard `grep -E '^[a-zA-Z_-]+:.*?## .*$$'` pattern
  - `reset` calls `$(MAKE) dev` and `$(MAKE) seed` (not `make`) to support recursive make correctly and inherit CLI flags
  - `.DEFAULT_GOAL := help` sets `make` (no target) to print help — useful developer UX
---

## 2026-03-21 - US-161
- What was implemented: Created `docs/guides/local-dev.md` — step-by-step local development guide covering: (1) Prerequisites table (Xcode, Docker Desktop, Flutter SDK, Node 22, Git) with minimum versions and verify commands; (2) First-time setup (clone, copy .env, `make dev`, verify `curl localhost:3000/health`, `make seed`); (3) Running iOS Simulator via `make ios` or manual `flutter run -d iPhone`, noting that iOS Simulator shares Mac host network so `localhost` works without any IP config; (4) Test login credentials table (testuser/testpass123 for participant, Keycloak admin URL); (5) Hot reload instructions for both Flutter (`r`/`R`/`q`) and backend (nodemon auto-restart); (6) Stop/reset via `make stop` and `make reset`; (7) Troubleshooting section with 6 entries: port conflicts, Neo4j slow start, Keycloak realm not found, iOS Simulator network issues (ATS/localhost), LibreTranslate OOM on model download, Flutter build errors after pull. Added link to `docs/guides/local-dev.md` in the `## Local Development` section of README.md.
- Files changed: docs/guides/local-dev.md (new), README.md (link added), prd.json (US-161 passes=true)
- **Learnings for future iterations:**
  - iOS Simulator shares Mac host network — `localhost` in the simulator resolves to Mac loopback directly; no special IP needed. ATS exempts `localhost` for HTTP; do NOT use `127.0.0.1` as it is NOT ATS-exempt.
  - The `make ios` Makefile target runs `flutter run -d iPhone` from the `mobile/` directory; it selects the first available iPhone Simulator automatically.
  - LibreTranslate downloads ~1 GB of language models on first start — requires Docker Desktop memory ≥ 6 GB; can be commented out of docker-compose.local.yml if translation is not needed during development.
---

## 2026-03-21 - US-166
- What was implemented: Synthesised all four component clean code reviews (Flutter, Backend, Neo4j, CI/CD) into a single cross-component coherence report at `tasks/review-clean-coherence.md`. Identified 6 cross-cutting smell patterns (hardcoded config, duplication, silent errors, SRP violations, long methods, inline queries), 7 naming inconsistencies across layers, the single most impactful refactoring (centralise `getDb()` + `_authHeaders()`), and a 4-phase fix order by max improvement/min risk.
- Files changed: tasks/review-clean-coherence.md (new), prd.json (US-166 passes=true)
- **Learnings for future iterations:**
  - The single highest-urgency fix is Neo4j C3: missing `()` on 3 method references in `SparqlDatabase.js` causes silent data corruption (all non-Group-1 donors silently assigned to Group2)
  - `getDb()` dedup (Backend, 8 files) is the keystone refactoring — doing it first unblocks mocking and enables safe extraction of god handlers
  - `_authHeaders()` (Flutter, 6 files) and `getDb()` (Backend, 8 files) are parallel problems that can be fixed in the same PR window
  - Phase 1 quick wins (~80 min total) fix 3 prod-breaking bugs: CI checkout action version, docker-compose v1 CLI, SparqlDatabase group assignment
  - Cross-cutting patterns: all four components show code duplication and silent error swallowing; addressing them systematically eliminates ~1,000 LOC
---

## 2026-03-22 - US-167
- What was implemented: Applied all Critical and Major clean code fixes to the Flutter mobile app. C-1: ProfileScreen now uses `AppConfig.apiBaseUrl`. C-2: AdminParticipantsScreen decomposed into `_FilterBar`, `_ParticipantsTable`, `_PaginationBar`, `_ErrorView`, `_CreateParticipantDialog`. C-3: AdminHabitsScreen decomposed into `_FilterBar`, `_DonationListView`, `_DonationTile`, `_ErrorView`. M-1: ProfileScreen._init() split into separate helpers. M-2: DonateScreen._initSurvey() split into `_buildSurveyUri()` and `_buildWebController()`. M-3: QuestionnaireFormWidget decomposed into 8 sub-widget classes. M-4: ExploreScreen annotate closure extracted; fixed infinite rebuild loop bug. M-5: AdminSurveyEditorScreen._save() extracted `_validateJson()`. M-6: Shared `OfflineBanner` widget in `lib/widgets/offline_banner.dart`. M-7: Auth headers replaced with `AuthInterceptor` + `dioProvider` — no service has `_authHeaders()` any more. M-9: All silent `catch (_) {}` replaced with `catch (e, st) { debugPrint(...) }`. M-11: `HabitDonation.donatedAt` changed to `DateTime?`. M-8 (AdminService split) and M-10 (ARB strings) deferred with written reasons. flutter analyze: 0 issues; flutter test: 49/49 passed.
- Files changed: mobile/lib/core/auth_interceptor.dart (new), mobile/lib/core/dio_provider.dart (new), mobile/lib/widgets/offline_banner.dart (new), mobile/lib/screens/profile_screen.dart, donate_screen.dart, explore_screen.dart, recommend_screen.dart, admin/admin_habits_screen.dart, admin/admin_participants_screen.dart, mobile/lib/services/admin_service.dart, habit_service.dart, recommendation_service.dart, survey_service.dart, mobile/lib/features/questionnaire/questionnaire_service.dart, mobile/lib/models/admin_habit_donation.dart, all test files (updated for new constructors), tasks/review-flutter-clean.md (resolution notes added), prd.json (US-167 passes=true)
- **Learnings for future iterations:**
  - `_fetchedLang` must be set in BOTH success AND error branches of `_fetchHabits()` — otherwise `!_loading && currentLang != _fetchedLang` triggers infinite re-fetches on error state, causing `pumpAndSettle` to time out in widget tests
  - The `dioProvider` pattern (shared Dio + AuthInterceptor) is now the canonical auth approach for all Flutter services in this codebase — never add `_authHeaders()` to new services; instead accept `Dio dio` via constructor and get it from `dioProvider`
  - All admin screen widgets now follow the split pattern: state class holds only data/action methods; UI extracted to private `StatelessWidget` classes in the same file
  - `_OfflineBanner` was duplicated in donate_screen and profile_screen; `lib/widgets/offline_banner.dart` is the single source of truth
---

## 2026-03-22 - US-168
- What was implemented: Applied all Critical and Major clean code improvements to the Node.js backend. Created `app/utils/getDb.js` (`makeGetDb` factory) replacing 8 copy-pasted getDb() functions. Extracted `app/services/habitDonationService.js` — POST /donate god handler (143 lines) reduced to 20-line route. Extracted `app/services/keycloakAdminClient.js` with 55s token TTL cache, used by both adminRouter and onboardRouter. Extracted `app/middleware/roles.js` with ROLES constants and `isPrivileged(user)` helper. Refactored `createAuthMiddleware` to compose `createTokenVerifier` internally (single JWKS cache). Split `translate()` into `fetchLibreTranslation()` + `refineLLMTranslation()`. Removed all 8 debug `console.log` statements from app.js, removed /test-disclaimer route, removed dead /submit-form route and emoji debug logs from donateRouter. Fixed 4 silent catch blocks in usersRouter/profileRouter. Fixed Redis error suppression. Extracted `getSurveysForUser()` from surveyRouter and `buildTimeline()` from adminStatsService. Updated review doc with resolution notes. All 265 tests pass.
- Files changed: app/utils/getDb.js (new), app/services/habitDonationService.js (new), app/services/keycloakAdminClient.js (new), app/middleware/roles.js (new), app/middleware/auth.js, app/routes/habitsRouter.js, app/routes/donateRouter.js, app/routes/adminRouter.js, app/routes/onboardRouter.js, app/routes/surveyRouter.js, app/routes/recommendRouter.js, app/routes/recommendationsRouter.js, app/routes/usersRouter.js, app/routes/profileRouter.js, app/routes/questionnairesRouter.js, app/routes/questionnaireResponsesRouter.js, app/services/adminStatsService.js, app/app.js, tasks/review-backend-clean.md, prd.json (US-168 passes=true)
- **Learnings for future iterations:**
  - `makeGetDb(injectedDb)` factory pattern in `app/utils/getDb.js` — all route files call this once at top of factory; caches the resolved db so repeated calls don't reconnect. Import from `../utils/getDb.js`.
  - `habitDonationService.donateHabit()` accepts `{ uuid, sentence, language, userID, queryNeo4j, getDb, apiBase, translate, translateUrl }` — the `translate` function is passed in (closure from router) so the service has no direct dependency on the router context
  - `createKeycloakAdminClient()` in `services/keycloakAdminClient.js` caches admin token for 55 seconds — use this instead of raw `getAdminToken()` in any new code that calls Keycloak admin APIs
  - When injecting a mock keycloak in tests, check `typeof kc.getAdminToken === 'function'` before using it — tests may pass `{}` as a stub that signals "use fetch-based fallback"
  - `isPrivileged(user)` in `middleware/roles.js` is the canonical way to check admin/researcher role — avoids magic string duplication across files
  - BK-M2 (`Neo4jDatabase._buildDonationTurtle` 149-line function) was deferred — no test coverage for it, regression risk is high. Consider adding unit tests for Neo4jDatabase before refactoring.
---

## 2026-03-22 - US-169
- What was implemented: Applied all Critical and Major clean code improvements to the Neo4j/SPARQL data layer. (1) **C3** — Fixed silent data corruption bug in `SparqlDatabase.js` where three `ExperimentalSetting` group methods were referenced as truthy function values instead of being called with `()`, causing all non-Group1 donations to be silently assigned to Group2. Fixed by extracting shared `ExperimentalSetting` class to `app/models/donation.js` with correct `()` calls. (2) **C1** — Extracted duplicated 48-line `translate()` function to `app/utils/translate.js` as `translateText(text, from, to, endpoint, retries)`. Both database files now import from the shared module; `Donation.translate(targetLanguage, endpoint)` takes an explicit URL. (3) **C2/M2** — Declared `const HHH_NS` and module-level `const iri = (local) => ...` in `Neo4jDatabase.js`; extracted full Turtle prefix block as `const PREFIXES`. (4) **M1** — Split 149-line `_buildDonationTurtle` into six named private methods: `_habitTriples`, `_experimentalSettingTriples`, `_donorTriples`, `_contextTriples`, `_behaviorContent`, `_translationTriples`. (5) **M3** — Extracted all four domain model classes to `app/models/donation.js`. (6) **M4** — Extracted `escapeStringLiteral()` to `translate.js`; both `_esc()` methods delegate to it. (7) **M5** — Removed dead `new Donor(donation)` call. (8) **M6** — Created `app/db/habitQueries.js` and `app/db/adminQueries.js` with named query functions; all inline Cypher removed from route/service files. (9) **M7** — Moved `SUPPORTED_LANGUAGES` and `DIMENSIONS` to `app/utils/constants.js`. (10) **m1** — Removed deprecated `exists()` wrapper from `migrate-group-labels.cypher`. (11) **m2** — Removed `console.debug(insertQuery)` from `SparqlDatabase.js`. (12) **m3** — Added `console.error` logging in `_importTurtle` on non-OK status. (13) **m4** — Added dual-schema comment blocks to both database files. (14) **m5** — Resolved naturally by C1 (explicit endpoint parameter).
- Files changed: app/utils/translate.js (new), app/utils/constants.js (new), app/models/donation.js (new), app/db/habitQueries.js (new), app/db/adminQueries.js (new), app/utils/Neo4jDatabase.js (refactored), app/utils/SparqlDatabase.js (refactored), app/routes/habitsRouter.js (imports from constants/db), app/services/adminParticipantService.js (uses adminQueries), app/services/adminStatsService.js (uses adminQueries), app/services/habitDonationService.js (imports DIMENSIONS from constants), scripts/migrate-group-labels.cypher (m1 fix), tasks/review-neo4j-clean.md (Resolution notes), prd.json (US-169 passes=true)
- **Learnings for future iterations:**
  - `Donation.translate()` now takes `(targetLanguage, endpoint)` where endpoint is the full LibreTranslate URL string — callers must pass `config.getTranslateApiEndpoint()` or `this.config.getTranslateApiEndpoint()`
  - `app/models/donation.js` is the single source of truth for `ExperimentalSetting`, `Donor`, `Label`, `Donation` — import from there, not from the database utils
  - `app/utils/translate.js` exports `translateText(text, from, to, endpoint, retries)` and `escapeStringLiteral(str)` — use for all translation and SPARQL/Turtle string escaping needs
  - `app/utils/constants.js` holds `SUPPORTED_LANGUAGES` and `DIMENSIONS` — import from there instead of defining locally
  - `app/db/habitQueries.js` and `app/db/adminQueries.js` contain all Cypher strings — route/service files should call these named functions rather than embedding Cypher inline
  - `HHH_NS` constant and `iri()` helper are at module level in `Neo4jDatabase.js` — do not re-create inside methods
  - Prettier runs before lint in `npm test` — always run `npx prettier --write` on new files in app/ before committing
---

## 2026-03-22 - US-170
- What was implemented: Final consistency and dead code sweep across all layers. (1) **Scripts**: `restore.sh` shebang `#!/bin/bash` → `#!/usr/bin/env bash`; timestamp format normalized to UTC ISO in `restore.sh`; `docker-compose` → `docker compose` in 3 deploy scripts; `deploy-keycloak.sh` token extraction `grep|cut` → `jq -r '.access_token'`; `deploy-full.sh` post-Keycloak health check now polls Keycloak's own health endpoint (`KEYCLOAK_HEALTH_URL`) instead of backend; post-Recommender health check removed (no endpoint); `generate-spec.js` dead 44-line `toYaml()` function removed + wrapped in `try/catch` with `process.exit(1)`. (2) **Naming**: `uuid` npm import replaced with `randomUUID` from `node:crypto` in `Neo4jDatabase.js` and `surveyRouter.js`; `DbClient` renamed → `SparqlDbClient` in `SparqlDatabase.js`, `donateController.js`, and both test files for consistency with `Neo4jDbClient`. (3) **CI**: `deploy.yml` `actions/checkout@v6` → `@v4`; created `.github/actions/setup-node-app/action.yml` composite action (parameterized `working-directory`); created `.github/actions/setup-flutter/action.yml` composite action; all 5 Node.js jobs and 3 Flutter jobs in `ci.yml` now use composite actions, eliminating ~60 duplicate YAML lines. All 265 tests pass; flutter analyze: 0 issues; lint: 0 issues.
- Files changed: scripts/restore.sh, scripts/deploy-backend.sh, scripts/deploy-recommender.sh, scripts/deploy-keycloak.sh, scripts/deploy-full.sh, scripts/generate-spec.js, app/utils/Neo4jDatabase.js, app/utils/SparqlDatabase.js, app/routes/surveyRouter.js, app/controllers/donateController.js, app/tests/SparqlDatabase.test.js, app/tests/FusekiDatabase.integration.test.js, .github/workflows/deploy.yml, .github/workflows/ci.yml, .github/actions/setup-node-app/action.yml (new), .github/actions/setup-flutter/action.yml (new), tasks/review-clean-coherence.md (Resolution notes), prd.json (US-170 passes=true)
- **Learnings for future iterations:**
  - GitHub Actions composite actions live in `.github/actions/<name>/action.yml`; referenced as `uses: ./.github/actions/<name>` with `with:` inputs; shell: bash is required for run steps inside composite actions
  - `docker compose` (space, v2 plugin) is the correct form for Docker 27+ — `docker-compose` (hyphen, v1) is removed from Docker 27+; all deploy scripts now use v2 form
  - `jq -r '.access_token'` is the canonical token extraction pattern — brittle `grep -o | cut` fails silently when response format changes
  - `node:crypto`'s `randomUUID()` is available in Node 15.6+ and needs no npm dependency — prefer over `uuid` package for UUID v4 generation in server code
  - Composite actions that include `actions/checkout@v4` as a step work correctly in jobs without a prior checkout; the job step list simply starts with the composite action instead of a standalone checkout step
---

## 2026-03-22 - US-171
- What was implemented: Updated all documentation to reflect the clean code refactor cycle (US-162 to US-170). (1) docs/guides/flutter-architecture.md — folder structure updated with auth_interceptor.dart, dio_provider.dart (core/), offline_banner.dart (widgets/); Section 6 Auth Token Management rewritten to describe DioProvider + AuthInterceptor pattern instead of removed _authHeaders() pattern. (2) docs/guides/developer-onboarding.md — backend test count updated from 186 to 265; added "Seed the local database" step describing npm run seed (added in US-159 but undocumented). (3) docs/architecture.md — added "Node.js Backend Internal Module Structure" reference table documenting the new routes/services/db/models/middleware/utils layers. (4) CHANGELOG.md — new [1.2.0] entry documenting all clean-code refactor changes across Flutter, backend, Neo4j, and CI. (5) DOCUMENTATION.md — added missing docs/guides/local-dev.md entry (file was created in US-161 but not indexed). docs/api/openapi.yaml left unchanged — no API response shapes were altered during the refactor cycle.
- Files changed: docs/guides/flutter-architecture.md, docs/guides/developer-onboarding.md, docs/architecture.md, CHANGELOG.md, DOCUMENTATION.md, prd.json (US-171 passes=true)
- **Learnings for future iterations:**
  - When reviewing "no response shapes changed" — verify by checking git diff on openapi.yaml across the refactor commits before skipping it; if in doubt, read and compare
  - DOCUMENTATION.md is not automatically updated when new docs/ files are created — always add an entry when a new guide or reference doc is committed
  - Test counts in developer-onboarding.md drift over time; update whenever backend or Flutter tests change significantly
---

## 2026-03-22 - US-172
- What was implemented: Appended "Clean Code Audit — Cycle 2" section to AUDIT.md (dated 2026-03-22). Section includes: (1) Clean Code Scorecard for Flutter (22/25), Backend (25/25), Neo4j (25/25), CI/Scripts (22/25) across 5 dimensions (SRP, DRY, function size, naming clarity, dead code absence) — all dimensions 4/5 or 5/5. (2) Before/After metrics table: max function length reduced from 149 lines to ~30 lines; DRY violations reduced from 15 to 4; debug console.log in production reduced from 12 to 0; dead code 0. (3) 7 remaining deferred findings with reasons. (4) Full resolved findings list for US-167 through US-170 (36 resolved). All 265 backend tests pass; flutter analyze 0 issues; lint 0 issues.
- Files changed: AUDIT.md (178 lines added), prd.json (US-172 passes=true)
- **Learnings for future iterations:**
  - AUDIT.md uses a clean code scorecard with 5 dimensions (SRP, DRY, function size, naming clarity, dead code) distinct from the broader quality dimensions (test coverage, security) in the earlier audit sections
  - The "No scores below 4/5" acceptance criterion requires that all deferred findings are Minor, not Critical/Major — complete Critical/Major resolution before writing the audit
  - Before/After metrics section requires concrete line counts from the actual codebase — use `wc -l` on key files before writing the section
---

## 2026-03-22 - US-173
- What was implemented: Created MongoDB data model files for the study management feature set. (1) `app/models/study.js` — exports `COLLECTION='studies'`, `VALIDATOR` (JSON Schema with required fields: name, isDefault, isActive, groups, createdAt, updatedAt; groups is an array of {id, label, index:1-4}), and `ensureIndexes(db)` which creates a partial unique index on isDefault (partialFilterExpression: {isDefault: true} — ensures at most one default study). (2) `app/models/studyCode.js` — exports `COLLECTION='studyCodes'`, `VALIDATOR`, and `ensureIndexes(db)` which creates a unique index on code and an index on studyId. (3) `app/models/enrollment.js` — exports `COLLECTION='enrollments'`, `VALIDATOR`, and `ensureIndexes(db)` which creates a unique index on userId (one enrollment per user) and an index on studyId. (4) `scripts/seed-study.js` — idempotent script that creates all 5 indexes and seeds the default study ("HHH Default Study") with 4 groups (Group 1–4, indices 1–4). Added `"seed:study": "node ../scripts/seed-study.js"` to `app/package.json`. All 265 tests pass; lint passes.
- Files changed: app/models/study.js (new), app/models/studyCode.js (new), app/models/enrollment.js (new), scripts/seed-study.js (new), app/package.json (seed:study script added), prd.json (US-173 passes=true)
- **Learnings for future iterations:**
  - Partial unique index in MongoDB for "at most one document with isDefault: true": use `createIndex({isDefault: 1}, {unique: true, partialFilterExpression: {isDefault: true}})` — documents with isDefault: false are excluded from the uniqueness constraint
  - `VALIDATOR` objects in model files are for reference/documentation and for use in `db.createCollection({validator: VALIDATOR})` calls — they are not auto-enforced; callers must explicitly apply them
  - The script follows the same `createRequire(resolve(__dirname, '../app/package.json'))` pattern as `seed-local.js` for loading mongodb from app/node_modules
  - `ensureIndexes` is idempotent — MongoDB's `createIndex` is a no-op if the index already exists with the same definition
---

## 2026-03-22 - US-174
- What was implemented: Created `app/services/studyService.js` with 6 functions — `listStudies` (paginated, with enrollment count via $group aggregation), `createStudy` (generates ObjectId for each group), `getStudy`, `updateStudy` (additive groups: appends new groups, never removes), `softDeleteStudy` (409 if enrollments exist), `setDefaultStudy` (clears all isDefault, sets new one). Added 6 study routes to `adminRouter.js` under `/studies`: GET (list), POST (create, 201), GET /:id, PUT /:id, DELETE /:id, PUT /:id/default. Added 11 unit tests in `app/tests/unit/study.service.test.js` using an in-memory mock DB. All 276 tests pass.
- Files changed: app/services/studyService.js (new), app/routes/adminRouter.js (import + 6 routes), app/tests/unit/study.service.test.js (new), prd.json (US-174 passes=true)
- **Learnings for future iterations:**
  - `ObjectId` is exported from `app/models/survey.js` — import it as `import { ObjectId } from '../models/survey.js'`
  - Study group IDs are `new ObjectId()` (generated at creation time, not strings) — PUT /studies/:id/default cannot simply clear and re-set with `updateMany` on `isDefault: true` if the partial unique index is in play; clear first, then set
  - The mock DB's `aggregate()` for `$group` needs to reference `doc[field]` not the full `$field` string — strip the `$` prefix
  - Route pattern for admin sub-resources: add them inline to `adminRouter.js`, import the service at the top — no sub-router needed for small sets of routes
  - `softDeleteStudy` checks enrollment count before soft-deleting; `409 Conflict` is the right status when the business rule blocks deletion
---

## 2026-03-22 - US-175
- What was implemented: Created `app/services/studyCodeService.js` with 5 exported functions: `createCodes` (generates HHH-XXXXX codes using crypto.randomBytes, caps at 100), `listCodes` (paginated), `revokeCode` (409 if already redeemed), `redeemCode` (case-insensitive, 409 if already enrolled, 410 if exhausted/expired), `skipCode` (round-robin group assignment on default study, idempotent). Created `app/routes/studyEnrollRouter.js` with `POST /redeem-code` and `POST /skip-code` endpoints (authenticated, requires req.user.sub). Updated `adminRouter.js` to add `POST/GET/DELETE /studies/:id/codes` routes and import from studyCodeService. Updated `v1Router.js` to mount `createStudyEnrollRouter` at `/onboarding` after the `authenticate` middleware. Created `app/tests/unit/study.code.service.test.js` with 16 unit tests. All 293 tests pass; lint passes.
- Files changed: app/services/studyCodeService.js (new), app/routes/studyEnrollRouter.js (new), app/routes/adminRouter.js (added imports + 3 routes), app/routes/v1Router.js (added /onboarding mount), app/tests/unit/study.code.service.test.js (new), prd.json (US-175 passes=true)
- **Learnings for future iterations:**
  - Authenticated onboarding routes need to be mounted AFTER `router.use(authenticate)` in v1Router.js — the existing `/onboard` path is a public route; new authenticated onboarding routes were placed at `/onboarding` (with 'ing') to avoid conflict
  - `studyCodes` collection name comes from `COLLECTION` export in `app/models/studyCode.js`; `enrollments` from `app/models/enrollment.js`
  - Round-robin group assignment: aggregate enrollments by groupId for the target study, pick the group with the fewest enrollments
  - The in-memory test mock needs `insertMany`, `deleteOne`, and `$inc` support in `updateOne` — extend the standard `makeDb` helper pattern from `study.service.test.js`
  - Code case-insensitivity: call `.toUpperCase()` before all DB lookups — format is always stored uppercase in the DB
---
## 2026-03-22 - US-176
- What was implemented: Questionnaire library and custom questionnaire API. All routes were already implemented in adminRouter.js (GET/POST/PUT/DELETE /admin/questionnaires) and participantRouter.js (GET /participant/questionnaires). Fixed Prettier formatting in adminRouter.js and admin.questionnaire.library.test.js. Added `isLibrary: true` to mongo/seed/questionnaires.json for SLIQ and RAND-36. Added backfill updateMany in seed-local.js to retroactively set isLibrary on existing questionnaires. Wired up participantRouter to v1Router at /participant mount.
- Files changed: app/routes/adminRouter.js (prettier fix), app/routes/v1Router.js (mount participantRouter), mongo/seed/questionnaires.json (isLibrary: true on SLIQ and RAND-36), scripts/seed-local.js (backfill isLibrary), app/tests/integration/admin.questionnaire.library.test.js (new — 16 tests all pass), prd.json (US-176 passes=true)
- **Learnings for future iterations:**
  - The questionnaire CRUD routes were already committed to adminRouter.js in a prior iteration; US-176 was mostly about the isLibrary flag in the seed data and integration tests
  - participantRouter.js was an untracked file (not yet wired into v1Router.js) — always check git status for untracked files when a story depends on a prior router being mounted
  - When Prettier formatting check fails, run `npx prettier --write <file>` from the app/ directory; the format:check runs before lint and test so a formatting failure blocks everything
  - The mock DB `matchesFilter` for `$in` on ObjectId arrays: the mock uses `.toString()` comparison which works correctly for matching `questionnaires: [new ObjectId(id)]` filter patterns
---

## 2026-03-22 - US-177
- What was implemented: Push notification backend using Firebase Cloud Messaging (FCM). (1) Added `firebase-admin` and `node-cron` to app/package.json. (2) Created `app/services/notificationService.js` with: `getFirebaseMessaging()` (lazy Firebase init from FIREBASE_SERVICE_ACCOUNT_JSON env var; returns null in no-op mode when unset), `sendToTokens()` (batch FCM sends via sendEach, tracks sent/failed/invalidTokens), `removeInvalidTokens()` (purges stale tokens from deviceTokens collection), `sendStudyNotification()` (finds enrolled participants, fetches their tokens, sends, removes invalid), `dispatchDueNotifications()` (picks up pending scheduled notifications with scheduledAt ≤ now and marks them sent), `startNotificationScheduler()` (node-cron task running every 60 s). (3) Added `POST /api/v1/participant/register-token` to `participantRouter.js` — upserts FCM token by userId. (4) Added 4 admin notification routes to `adminRouter.js`: POST /admin/notifications/send (immediate), POST /admin/notifications/schedule (creates scheduledNotifications doc), GET /admin/notifications/scheduled (lists pending), DELETE /admin/notifications/scheduled/:id (cancels pending). (5) Wired `startNotificationScheduler` in `app.js` (not in the router to avoid keeping test processes alive). (6) Documented FIREBASE_SERVICE_ACCOUNT_JSON in `.env.example`. (7) Created 11 unit tests in `tests/unit/notification.service.test.js` — all pass.
- Files changed: app/services/notificationService.js (new), app/routes/participantRouter.js (register-token route added), app/routes/adminRouter.js (4 notification routes + imports), app/app.js (scheduler startup), app/package.json + package-lock.json (firebase-admin, node-cron), app/tests/unit/notification.service.test.js (new — 11 tests), .env.example (FIREBASE_SERVICE_ACCOUNT_JSON doc), prd.json (US-177 passes=true)
- **Learnings for future iterations:**
  - node-cron tasks keep the Node.js event loop alive — do NOT start the scheduler inside a router factory that gets imported in unit tests; start it in app.js instead
  - Firebase `admin.messaging().sendEach(messages)` (not deprecated `sendMulticast`) — one message per token object; `batchResponse.responses[i].error.code` identifies invalid/unregistered tokens
  - FIREBASE_SERVICE_ACCOUNT_JSON is the full JSON string of the service account key file; Firebase init reads it with `JSON.parse(raw)` then `admin.credential.cert(serviceAccount)`
  - `getFirebaseMessaging()` returns null when env var is absent — all notification calls are no-ops in dev/test without Firebase credentials
  - FCM data payload values must all be strings — use `Object.fromEntries(Object.entries(data).map(([k, v]) => [k, String(v)]))` when constructing the data map
  - Scheduled notification collection: `sent: false` field allows efficient polling with compound filter `{ sent: false, scheduledAt: { $lte: now } }`
  - Mock `makeDb` in tests: parameter destructuring uses short names (`scheduled`); tests must use the same short name, not computed keys like `[COLLECTION_SCHEDULED]`
---

## 2026-03-22 - US-178
- What was implemented: Created `admin/src/app/(admin)/studies/page.tsx` — full Studies management page with: paginated study list table (name, status, default badge, group count, participant count, created date); "New Study" button opening a create modal with Name, Description, number-of-groups select (1-4), and per-group label inputs; clicking a row opens the same modal pre-filled for editing (Save calls PUT); "Set as Default" button in edit modal calls PUT /api/v1/admin/studies/:id/default and is hidden when study is already default; "Deactivate" button calls DELETE and surfaces inline error message when API returns 409 (participants enrolled); all API errors shown via inline `errorMsg` div. Created matching `page.module.css`. Added "Studies" (🔬) nav item to `admin/src/components/sidebar.tsx` as the first entry.
- Files changed: admin/src/app/(admin)/studies/page.tsx (new), admin/src/app/(admin)/studies/page.module.css (new), admin/src/components/sidebar.tsx (Studies nav item added), prd.json (US-178 passes=true)
- **Learnings for future iterations:**
  - Studies API returns either an array directly or `{ studies: [] }` — guard with `Array.isArray(data) ? data : data.studies ?? []`
  - Modal footer uses `modalFooterLeft` (Set as Default / Deactivate) and `modalFooterRight` (Cancel / Save) with `margin-left: auto` on right group to push buttons to opposite ends
  - `handleDeactivate` catches the thrown error from `apiFetch` and returns `{ error: msg }` instead of re-throwing — caller checks the return value to show inline error without closing the modal
  - Admin build uses `npx tsc --noEmit` for typecheck then `npm run build` — both pass with the new page
---

## 2026-03-22 - US-179
- What was implemented: Added "Codes" tab to study edit modal in `admin/src/app/(admin)/studies/page.tsx`. Tab is shown only for existing studies. "Generate Codes" form has group select (from study.groups), quantity 1-100 input, optional max redemptions, optional expiry datetime-local. On submit, calls POST /api/v1/admin/studies/:id/codes and shows generated codes as chips with a "Copy All" button (writes to clipboard). Existing codes table shows code (monospace), group label, redemptions/max, expiry, and Revoke action (DELETE /api/v1/admin/studies/:id/codes/:code). Revoke button is disabled with strikethrough tooltip text if redemptionCount > 0. Paginated (10 per page) with prev/next buttons. Also fixed pre-existing bug: `StudySummary._id` → `id` to match the backend `listStudies` response which returns `id: s._id.toString()`. `StudyGroup` interface updated to include `id` and `index`. Added tabs CSS and codes-tab CSS to page.module.css.
- Files changed: admin/src/app/(admin)/studies/page.tsx (tabs + CodesTab component + _id→id fix), admin/src/app/(admin)/studies/page.module.css (tabs + codes CSS), prd.json (US-179 passes=true)
- **Learnings for future iterations:**
  - `listStudies` backend returns `id: s._id.toString()` (not `_id`) — frontend `StudySummary` should use `id`, not `_id`
  - `listCodes` backend returns `groupId` string only; group label must be looked up from `study.groups` in the frontend
  - POST /codes returns `{ codes: StudyCode[] }` where each code is a full object; extract `.code` string field for the clipboard display
  - Tab pattern: add `activeTab` state to modal; use `.tabs` + `.tab` + `.tabActive` CSS; only show footer when on "details" tab; Codes tab manages its own loading/error state independently
---

## 2026-03-22 - US-180
- What was implemented: Refactored the admin questionnaires page to split into Library and Custom tabs. Library tab shows SLIQ and RAND-36 in a read-only table with a Preview action that opens a modal showing all questions. Custom tab shows custom questionnaires with Create, Edit, Delete actions. Delete button is disabled (with tooltip) for questionnaires known to be assigned to an active study — the 409 from the backend marks the ID in a local Set and disables the button. Fixed PUT and DELETE API calls to use MongoDB ObjectId (`id` from GET response) instead of `slug` (the backend uses `/:id` routes, not `/:slug`). Added CSS for tabs, disabled button state, library badge, and preview modal.
- Files changed: admin/src/app/(admin)/questionnaires/page.tsx (refactored with tabs + preview modal + fix PUT/DELETE to use id), admin/src/app/(admin)/questionnaires/page.module.css (tab + preview + disabled styles), prd.json (US-180 passes=true)
- **Learnings for future iterations:**
  - Admin questionnaire backend uses `/:id` (MongoDB ObjectId) for PUT and DELETE, but `/:slug/active` for PATCH — always use `id` from the GET list for mutating operations
  - To spread a Set into another Set in TypeScript with `downlevelIteration` disabled, use `const next = new Set(prev); next.add(item); return next;` pattern instead of `new Set([...prev, item])`
  - The GET /admin/questionnaires response includes `isLibrary: boolean` — filter on this to split library vs. custom in the UI
  - Library questionnaire full questions must be fetched from the participant-facing `/questionnaires/:slug` endpoint since there is no GET /admin/questionnaires/:id endpoint
---

## 2026-03-22 - US-181
- What was implemented: Added a "Questionnaires" tab to the study detail modal in the Admin UI. The tab fetches all available questionnaires (library + custom) from `GET /api/v1/admin/questionnaires`, displays them as a checkable list grouped by type, pre-checks those already assigned to the study, and saves via `PUT /api/v1/admin/studies/:id` with the updated questionnaires array. Changes are reflected immediately without page reload. Also fixed the Details tab `handleSave` which was sending `questionnaires: []` (would have wiped assignments on every details save).
- Files changed: admin/src/app/(admin)/studies/page.tsx (QuestionnairesTab component + ModalTab type + tabs UI + StudySummary type + QUESTIONNAIRES_API constant + handleSave fix), admin/src/app/(admin)/studies/page.module.css (qSection, qList, qItem, qCheckbox, qTitle, qInactive, qFooter, savedMsg styles)
- **Learnings for future iterations:**
  - The `StudySummary` interface was missing the `questionnaires: string[]` field even though the backend `listStudies` already returned it — always check both the interface and the API response shape
  - Details tab `handleSave` was including `questionnaires: []` in the PUT payload — this would silently wipe questionnaire assignments on every details save; removed it so questionnaires are only modified via the Questionnaires tab
  - The `onSaved` callback in `QuestionnairesTab` refreshes the parent list without closing the modal — this is intentional so the user stays on the tab after saving
---

## 2026-03-22 - US-182
- What was implemented: Default study configuration across backend, frontend, and seed
  - `app/services/studyService.js`: `softDeleteStudy` now returns `{ isDefault: true }` when attempting to deactivate the default study
  - `app/routes/adminRouter.js`: DELETE /admin/studies/:id returns HTTP 409 with "Cannot deactivate the default study. Set another study as default first." when study is default
  - `admin/src/app/(admin)/studies/page.tsx`: "Set as Default" button triggers amber confirmation dialog inside the modal before saving; Deactivate button is disabled with `title` tooltip when `initial?.isDefault === true`
  - `admin/src/app/(admin)/studies/page.module.css`: added `.confirmDialog`, `.confirmMsg`, `.confirmActions` styles
  - `scripts/seed-local.js`: added `seedDefaultStudy()` — idempotent, connects to MongoDB, checks for existing `isDefault: true` study, then creates "Default Study" with Groups 1-4 and the SLIQ + RAND-36 questionnaire IDs; called in `main()` after `seedMongo()`
  - `app/tests/unit/study.service.test.js`: added test for softDeleteStudy isDefault guard (320 tests pass)
- Files changed: app/services/studyService.js, app/routes/adminRouter.js, app/tests/unit/study.service.test.js, admin/src/app/(admin)/studies/page.tsx, admin/src/app/(admin)/studies/page.module.css, scripts/seed-local.js, prd.json (US-182 passes=true)
- **Learnings for future iterations:**
  - Disabled HTML buttons don't show browser tooltips in Firefox — wrapping in a `<span title="...">` is more reliable cross-browser; here we used `title` on the button directly (works in Chrome/Safari)
  - Confirmation dialogs inside modals are simpler than new overlay modals: render as a conditional section between tabs and footer, styled with a distinctive background colour
  - `seedDefaultStudy()` must run AFTER `seedMongo()` so questionnaire documents exist before the study references their ObjectIds
  - When reading ObjectId from mongodb via appRequire, use `const { ObjectId } = appRequire('mongodb')` inside the function — not at module level — to avoid import order issues
---

## 2026-03-22 - US-183
- What was implemented: Added `listStudyParticipants` function to `app/services/studyService.js` — fetches paginated enrollments for a study, builds per-group summary, maps groupId to label using study.groups. Added `GET /api/v1/admin/studies/:id/participants?page=N&limit=N` route in `adminRouter.js`. Added `ParticipantsTab` React component in `admin/src/app/(admin)/studies/page.tsx` — shows summary pill row (total enrolled + count per group), paginated table (userId, groupLabel, enrolledAt, codeUsed or 'direct/default'), and Download CSV button that fetches all records (up to 500) and triggers a browser download. Updated `ModalTab` type to include `"participants"`, added tab button in modal header, wired tab rendering in modal body. Added CSS for participants tab styles in `page.module.css`. All 320 backend tests pass; `npx tsc --noEmit` and `npm run build` pass.
- Files changed: app/services/studyService.js (listStudyParticipants), app/routes/adminRouter.js (new route + import), admin/src/app/(admin)/studies/page.tsx (ParticipantsTab component + ModalTab update + tab rendering), admin/src/app/(admin)/studies/page.module.css (participants tab styles), prd.json (US-183 passes=true)
- **Learnings for future iterations:**
  - The `listStudyParticipants` function queries the `enrollments` collection by `studyId` (ObjectId) — sort by `enrolledAt: -1` gives most-recent-first
  - Group label lookup: `study.groups[].id` is an ObjectId stored in MongoDB; compare with `g.id.toString()` against `e.groupId?.toString()` from enrollment document
  - CSV download done client-side: fetch with `limit=500`, build CSV string, create Blob + object URL, programmatically click `<a>` — no server CSV endpoint needed
  - `enrolledAt` from MongoDB comes back as an ISO string in JSON responses — safe to pass directly to `fmtDate()`
  - `studyCodeUsed` null means the participant skipped code entry (enrolled in default study) — display as 'direct/default'
---
