apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus # fixed so alerting.yaml can reference it deterministically
    isDefault: true
    editable: false
