# Pinned lockfile (direct + transitive), generated against the exact
# python:3.11-slim image this service's Dockerfile builds on — reproducible
# builds regardless of when the image is built. Regenerate with:
#   docker run --rm -v "$(pwd)/requirements.in:/tmp/requirements.in:ro" \
#     python:3.11-slim bash -c "pip install --no-cache-dir -q -r /tmp/requirements.in && pip freeze"
# where requirements.in holds the direct deps:
#   fastapi>=0.111.0, uvicorn[standard]>=0.29.0, openai>=1.30.0, redis>=5.0.0,
#   httpx>=0.27.0, numpy>=1.26.0, python-multipart>=0.0.9, motor>=3.3.0,
#   neo4j>=5.19.0, pytest>=8.0.0, pytest-asyncio>=0.23.0, respx>=0.21.0
# Note: redis-py folded asyncio support into the base package as of 5.x —
# the `redis[asyncio]` extra used previously no longer exists (pip now warns
# rather than errors on an unknown extra), so it's dropped here.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.6.17
click==8.4.2
distro==1.9.0
dnspython==2.8.0
fastapi==0.139.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jiter==0.16.0
motor==3.7.1
neo4j==6.2.0
numpy==2.4.6
openai==2.45.0
packaging==26.2
pluggy==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
pymongo==4.17.0
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.2
python-multipart==0.0.32
pytz==2026.2
PyYAML==6.0.3
redis==8.0.1
respx==0.23.1
sniffio==1.3.1
starlette==1.3.1
tqdm==4.68.4
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1
