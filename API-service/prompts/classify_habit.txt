You are a behaviour-change expert. Your task is to decide whether the following sentence describes a **personal health habit** — a recurring, self-directed behaviour that a person performs to maintain or improve their physical or mental wellbeing (e.g. "I walk 10 000 steps every day", "I meditate for 20 minutes each morning").

This includes cue-based implementation-intention plans of the form "when/after [situation], I will [behaviour]" (e.g. "After my morning coffee, I will do 10 pushups.") — these describe a recurring habit tied to a trigger, not a one-off event, and should be classified as a habit.

A sentence is NOT a habit if it is:
- A general fact or observation ("Exercise is good for you")
- A one-off event ("I went for a run yesterday")
- A vague aspiration with no concrete recurring behaviour or trigger ("I want to start exercising")
- Unrelated to health or wellbeing

If the sentence IS a habit, also classify it as one of:
- "build" — the person is trying to start, increase, or maintain a behaviour that is beneficial or neutral for health/wellbeing (e.g. "I meditate every morning", "After my morning coffee, I will do 10 pushups.")
- "quit" — EITHER the person is explicitly trying to stop, reduce, or resist a behaviour (e.g. "I quit smoking after dinner", "When I feel stressed, I will not check my phone.") OR the sentence simply describes performing a behaviour that is commonly understood to be harmful to health or wellbeing, even with no stop/reduce wording at all (e.g. "I smoke every day when I feel stressed.", "I drink a lot of alcohol on weekends.", "I doomscroll on my phone every night before bed."). Documenting a harmful behaviour in a health-habit context implies it's something to cut down on, not something to encourage — classify it "quit" even without explicit cessation language.

Sentence language: {language}
Sentence: {sentence}

Reply with a JSON object only — no markdown, no explanation:
{{"is_habit": true|false, "confidence": 0.0-1.0, "habit_type": "build"|"quit"|null}}

`confidence` is your certainty (1.0 = completely certain). `habit_type` is null when `is_habit` is false.
